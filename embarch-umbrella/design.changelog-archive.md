# design.md: changelog archive

- 2026-08-18 — **Decision 7 reversed: elevation is now self-triggered (`embarch-core` self-elevates), not just reported — implemented and validated live the same day.** Prompted by a real gap found live the same day (deploying a `/resolve-chip` fix to the real Windows Core ahead of `embarch-api/milestone-7.md`): updating an already-installed Core's binary had no supported path at all, only hand-run elevated shell surgery. Full mechanism and live validation (a non-elevated `embarch-core.exe stop`/`start` actually controlling the real service, confirmed via `Get-Service`) in `embarch-core/design.md` §3 decision 3; this doc's decision 7, §10's "elevation is a hard edge" open item, and the WSL2 `local`-classification open item all updated to match.
- 2026-08-17 — **Decision 28 implemented**, same day it was decided: `install.rs` (canonical per-user bin dir, real copy step, real Unix env-file/rc-line PATH mutation, real Windows registry PATH mutation via `winreg` preserving `REG_SZ`/`REG_EXPAND_SZ`), `locate.rs` (sibling-lookup deleted, WSL2 lookup gained a real per-user `%LOCALAPPDATA%` resolution via shellout), `setup.rs` (`setup` now installs for real and reports it; new `uninstall()` reversing service + token + install), `main.rs` (`setup --uninstall` flag wired up). 79 tests passing (11 new), clippy-clean on the host target; the Windows-specific registry code was verified separately via `cargo check --target x86_64-pc-windows-msvc` against an extracted throwaway crate, since this sandbox has no MSVC linker to build the full binary. Not yet released.
- 2026-08-17 — Real Windows+WSL2 onboarding attempt (Milestone 1 prep, on the real daily-use machine) surfaced three genuine gaps, all in §10/§11 now: §11 still described the user guide as written against not-yet-existing tooling, contradicting the guide's own now-corrected callout and this doc's own Status line (both already say `v0.1.0` shipped) — fixed. Two new open items: (1) `setup`'s printed `PATH` hint is unconditionally POSIX `export PATH=...` syntax even on native Windows `cmd.exe`, where it's invalid; (2) the `wsl-host` leg's printed `embarch-core: <path>` line reports the local WSL2-side Linux sibling binary instead of the Windows-side `embarch-core.exe` decision 7 says it should locate/record — harmless in this run (the separate HTTP reachability check still correctly found the real Windows-hosted Core), but misleading. Both are real code gaps, not doc-only; workarounds/explanations live in `embarch-user-guide.md` §3.1 and were explained inline in chat. The `wsl-host` run itself succeeded end-to-end otherwise: topology correctly resolved, token found via `/mnt/c`, Core confirmed already running — the first time this exact dual-leg flow has been confirmed by someone other than the original dogfooding session.
- 2026-08-15 — Design-only pass closing thirteen items from a full-suite design-improvement review (`.claude/design-improvements-2026-08-15.md`, local working notes — not committed): amended decision 17 so `doctor` check 8 shells out to `embarch-api list-targets --json` instead of maintaining a second mirrored scanner; new decisions 18 (probe-permission check 5), 19 (stale-dev-bench-firmware check 13), 20 (CI diff job for the liftable-copy modules, instead of an `embarch-common` crate), 21 (`setup --dry-run`), 22 (checks 14-16: bind-vs-topology, firewall, disk space), 23 (check 10 verifies the MCP handshake actually completes), 24 (version-skew stays warn-only, decided not left open), 25 (`setup --uninstall`), 26 (`doctor --prune` for `study_results`/build-dir retention), 27 (release-CI tag-vs-manifest-version assertion). §4's topology matrix and decision 8 updated — topology v (remote Core) can now flash, closing what had been a hard "no." §5's doctor table gained four rows and amended three existing ones; §8's command table updated; §10 trimmed two items this pass resolved outright. No code changes yet — tracked as the next implementation pass.
- 2026-08-14 — Decision 17 implemented: new `zephyr.rs` (`looks_zephyr_west_shaped`, `count_valid_targets` — a trimmed liftable copy of `embarch-api/src/zephyr.rs`'s scanning half, §3 decision 15's pattern applied a third time), `init.rs`'s new `render_zephyr_west_config` branch, and `config.rs`'s `Discovery`/optional-field mirror of `embarch-api`'s schema so `doctor` sees exactly what `embarch-api` would. `doctor` checks 7-9 branch on `discovery` as designed (§5). Verified end-to-end against a synthetic fixture and a real running Core: `init` detects the shape, writes a config with nothing to hand-edit, and that same config loads directly into a real `embarch-api` binary whose `list-targets` returns the correct file-backing-validated set — real cross-repo interop. `doctor`'s three branched checks each behaved correctly against that repo (7 failed on a genuinely-missing `west`, 8 passed counting 3 real targets, 9 passed with the new zephyr-west message). 67 unit tests (10 new); `cargo build`/`cargo clippy --all-targets -- -D warnings`/`cargo test` clean.
- 2026-08-13 — New decision 17, design only (not yet implemented): `init` stops guessing one board from whichever `build_info.yml` it finds for a Zephyr/west repo, and instead scaffolds a minimal `discovery = "zephyr-west"` project, deferring board/chip/target selection to `embarch-api`'s new call-time discovery (`embarch-api/design.md` §3 decision 12). `doctor` checks 7-9 updated to branch on `discovery`. Prompted by running `init` for real against the reference-dut repo and finding it silently picked the wrong one of four real boards present, with no signal a choice had been made — full narrative in `milestone-6.md`'s 2026-08-13 entry.
- 2026-08-11 — Release CI (decision 14) fully verified end-to-end, all three repos. `embarch-umbrella`'s own `v0.1.0` released clean across all four targets. `assemble-suite.yml`, triggered for the first time, caught a real bug on the first run — its archive-extension lookup (`ls a.tar.gz b.zip | head -n1` under `pipefail`) fails whenever only one glob matches, which is always — fixed with `nullglob` plus a plain array (commit `4e6cb16`), re-verified against the real downloaded release assets, then re-run clean. `doctor` check 1 passed against the resulting real `suite-v0.1.0` manifest for the first time. §1's status line updated. Full detail in `milestone-6.md` §3.7/§3.8.
- 2026-08-11 — Release CI (decision 14) verified against real `v0.1.0` tags on `embarch-core` and `embarch-api`. `embarch-api` released clean first try; `embarch-core` caught and fixed a real gap (`libudev-dev` missing on the native `x86_64-unknown-linux-gnu` runner — only the cross-compiled `aarch64` leg had arranged for it). `embarch-umbrella`'s own tag is still pending a human push, so `assemble-suite.yml` and a real (not hand-crafted) manifest remain unverified. §1's status line updated.
- 2026-08-10 — Release CI written ([milestone-6.md](milestone-6.md) §3.7): decision 14 extended in place with what got built and how its two open questions resolved. `doctor` check 1 now reads the suite manifest `assemble-suite.yml` produces, closing the last piece of that check that could ever be more than a stub — though the workflows themselves are unverified (no tag pushed), only smoke-tested by hand on the manifest-reading side. §1's status line updated. Only §3.8 (dogfood the guide) remains in Milestone 6.
- 2026-08-10 — `doctor` implemented ([milestone-6.md](milestone-6.md) §3.4 closed — every command in §8 now has real behavior). New decision 16: the liftable-copy pattern decision 15 established for topology detection applied a second time, to `embarch-api/src/token_discovery.rs` (as `token.rs`) and, more loosely, to `embarch-api/src/config.rs` (as `config.rs`). §1's status line updated to reflect that every command ships. Smoke-tested against a live throwaway `embarch-core` instance (real `200`/`401`/`404` responses) and the real reference-dut repo's config; that testing found and fixed a real bug in both `embarch-core` and `embarch-api` (`--version` wasn't wired up on either CLI, so check 1 had nothing to report — both repos' own design docs record it in their changelogs). Remaining in this milestone: §3.7 (release CI, which is also what check 1's manifest comparison is waiting on) and §3.8 (dogfood the guide).
- 2026-08-05 — Resolved [milestone-6.md](milestone-6.md) §5's open question on where topology detection lives: new decision 15 — written once here in a liftable shape (no umbrella-specific types crossing its boundary, tests that port unchanged) and copied into `embarch-api`, rather than extracted into a fourth crate for one function. Records the drift cost this accepts and the three things that keep it visible, including `doctor` check 3 reporting which candidate won rather than just pass/fail.
- 2026-08-05 — Repo bootstrapped ([milestone-6.md](milestone-6.md) §3.1 closed): Cargo project producing the `embarch` binary per decision 2, `clap` deriving the full §8 command surface, plus `CLAUDE.md`/`LICENSE`/`README.md`/`.gitignore`. Every command parses and then reports itself unimplemented with a pointer to the milestone step that implements it — deliberately, so the surface can't be mistaken for working behavior. `cargo build` and `cargo clippy --all-targets -- -D warnings` both clean; `--help`, a `--json` invocation, and a malformed invocation were all exercised. Two conventions adopted from `embarch-api` rather than invented: exit codes `0`/`1`/`2` (`embarch-api/design.md` §5a) and `tracing` to stderr so stdout stays reserved for results and `--json` (§10 there). `probe-rs`/`serialport` are deliberately absent from `Cargo.toml`, with a comment recording that they're expected to *stay* absent per §1.
- 2026-08-05 — Repo created, empty: [gabrieltetar/embarch-umbrella](https://github.com/gabrieltetar/embarch-umbrella), public, no commits.
- 2026-08-05 — Initial draft. Scoped `embarch-umbrella` via Q&A: a sixth sub-project shipping one binary (`embarch`) that owns setup, verification, and fallback Core startup — deliberately not a supervisor, not in the runtime path, and not the process an MCP client spawns. Locked in: Core-as-an-autostarting-service is the real answer to "one button starts everything" (decision 3), so setup and verification are the actual problems; topology auto-detection by racing a `GET /status` at an ordered candidate list, persisting only the class and never an address (decision 6), with `401` treated as a hit; `base_url = "auto"` implemented in `embarch-api` rather than here (decision 9); per-firmware-repo config in an `embarch/` subfolder with a separate build directory (decision 10); local-only repo integration that never touches tracked files (decision 12); `init` deriving `build_command` from `build_info.yml` but refusing to guess the probe-rs `chip` name (decision 13); one suite release archive carrying all three binaries (decision 14). `doctor` (§5) becomes the home for two previously-deferred items: `embarch-api/design.md` §12's PATH/toolchain preflight, and detection of an `artifact_path`/`artifact_path_for_core` mismatch. A future EmbArch UI is out of scope but drives decision 11's `--json` contract.

Entries beyond the 8 most recent, moved here from [design.md](design.md)
by `scripts/archive-changelog.py`, per `DOC-PROTOCOL.md` §5. Newest-first,
same as the live doc's own Changelog.

- 2026-08-05 — `init` implemented, closing everything in [milestone-6.md](milestone-6.md) §3.4 except `doctor`. Decision 13 extended in place: the look-don't-assume principle it stated for `build_command` now also covers `artifact_path` (found by searching for a real artifact, since sysbuild nests it and a plain build doesn't) and `artifact_path_for_core` (derived from `WSL_DISTRO_NAME`). Decision 12's no-tracked-files requirement verified end to end — `git status` unchanged after `init`, and `--uninstall` restores `.git/info/exclude` byte for byte.
- 2026-08-05 — `setup`, `up`, and `down` implemented ([milestone-6.md](milestone-6.md) §3.3 and half of §3.4). Two refinements folded into decisions 3 and 4 from doing it: `setup` does not edit `PATH` (the release archive's sibling layout makes it unnecessary, and editing shell rc files is invasive and hard to undo — it prints the line instead), and `up`'s foreground fallback is opt-in via `--foreground` rather than automatic, since telling "no service installed" apart from other failures means parsing per-backend error text. Also new: binary location follows decision 7's documented order in `locate.rs`, only the topology *class* is persisted (never an address, `state.rs`), and a `remote` topology refuses `up`/`down` instead of starting a second, wrong Core locally.
- 2026-08-05 — `embarch-core` gained the `start`/`stop` subcommands decision 4's `up`/`down` were waiting on ([milestone-6.md](milestone-6.md) §3.6 closed), removing that §10 open item. Smoke-testing them corrected decision 7, which had framed elevation as a Windows-only tax: a systemd *system* unit needs root/polkit just as a Windows Service needs Administrator, so `setup`'s one elevated step is universal rather than a Windows quirk. §10's elevation item reworded accordingly, and it now notes the user-level-service alternative that would avoid it at the cost of not starting before login.
- 2026-08-05 — Topology detection implemented ([milestone-6.md](milestone-6.md) §3.2 closed), along with enough of `status` to exercise it. Two refinements to decision 6 folded in from doing it: "race" is ordered-sequential rather than concurrent (ordering is the point, and the common miss returns instantly), and a third probe outcome distinguishes "something answered HTTP but isn't Core" from "nothing there." One new §10 open item: under WSL2 with mirrored networking a Windows-hosted Core classifies as `local`, which is fine for addressing but not enough for `up` to know whether it can start Core itself. Verified on this machine (WSL2 detected, gateway found and probed in order) and against a mock (401 → Core up, 404 → not Core, nothing → every attempt reported); 13 unit tests, all pure, written to port into `embarch-api` unchanged.
