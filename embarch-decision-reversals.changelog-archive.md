# embarch-decision-reversals.md: changelog archive

Entries beyond the 8 most recent, moved here from [embarch-decision-reversals.md](embarch-decision-reversals.md)
by `scripts/archive-changelog.py`, per `DOC-PROTOCOL.md` §5. Newest-first,
same as the live doc's own Changelog.

- 2026-08-25 — **Rows 35 and 36, both from implementing Milestone 7 Phase B item 3.** 35: decision 29(a)'s own text listed four payload-interpretation deletions as still pending and all four had already been done in Phase A, so the item was purely additive and the net-negative-size expectation it carried was unmeetable — found by grepping for the code before deleting it. 36: the reserved `dev-bench` log tap could not have worked as designed, since Core resolved tap ids against the submitted `Study` and would have dropped every record from a tap dev-bench invented; it ships as Core rendering the `LogLine` frames it already receives, which closes the same asymmetry with no firmware at all.
- 2026-08-25 — **Row 28 added**, and it is a different failure from the ones around it. Rows 25–27 are decisions whose assumptions about the code did not survive contact with it; 28 is a decision that recorded the conclusion of its own argument as though it were the state of the tree — `embarch-core`'s `erase` documents a vendor-tool delegation and ships the sector-erase that decision says was declined. Found by reading `hardware.rs` while implementing something unrelated, which is the only way it was ever going to be found: both suites are green, no test asserts which mechanism runs, and the doc and the code each read as internally consistent. This is row 15's shape (documented-as-shipped, never implemented) recurring in a decision written the same day as the code it describes, which is the part worth noticing — proximity in time was not protection.
- 2026-08-25 — **Rows 25 and 26 added**, both found by *implementing* the four decisions rows 21–24 recorded, on the same day they were settled — which makes them the sharpest available demonstration of this table's own closing note. 25 is a cost estimate that did not survive contact with the code: `streams_crc` was priced against a span dev-bench's decoder was believed to already walk, and it never walked it, because the immediately preceding decision had appended the field so that it wouldn't have to. 26 is a testing rule scoped narrower than the failure it protects against — both-languages pinning applied to *new* wire records, so the oldest and most-sent one drifted for a full schema version with both suites green. Neither reverses a decision's substance; both correct what a decision assumed about the code it would land in.
- 2026-08-25 — **Rows 21–24 added**, all four from one pass settling the open questions Milestone 7 Phase A left behind ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §2), and all four found the way this table's own closing note predicts — by reading the real repos' actual files rather than the docs' summaries of them. 21 and 23 are cases where a doc's stated reason for an open choice did not survive contact with the code (`steps_crc` is recomputed on every submit, so nothing widening it could invalidate; `Step.power_sample` was already dead in all four consumers, so there was no live competitor to adjudicate). 22 is a versioning rule that fired on changes the version could not express, *and* would have broken a hop nobody was thinking about — the first answer reached was wrong in the opposite direction, which is why the constant splits rather than narrows. 24 is owed work queued ahead of anything that could trigger it.
- 2026-08-21 — Added row 16: dev-bench's ESP32-C5's native USB-Serial/JTAG peripheral was assumed to be a viable permanent link once one self-test `Study` ran over it (decision 18) — real Espressif-documented silicon quirks (boot-strap pins never re-sampled on reset, a genuine reset pulse wedges the USB CDC driver, a DTR/RTS-on-open gotcha reproduces that wedge) made it a genuinely bad fit; fixed by moving the link to the board's own second, dedicated UART port instead of root-causing any of the above further.
- 2026-08-20 — Added row 14: dev-bench's SEGGER-VID auto-detection heuristic, live-confirmed ambiguous the first time a J-Link-based DUT and dev-bench were both attached at once — fixed by falling back to `known_boards`'s own enrolled serial instead of requiring a second, separately-typed env var.
- 2026-08-19 — Added row 13: Milestone 2's target board retargeted from the nRF54L15DK to the ESP32-C5 — the interim substitute's already-successful self-test run made waiting on the nRF54L15DK's still-in-transit RMA replacement pointless, so the milestone's own scope moved to the board that actually validated.
- 2026-08-19/20 — Added row 12: `embarch-core`'s exclusion from flashing dev-bench firmware ("has no business" doing so) stopped holding once the ESP32-C5 interim board's on-chip debug probe turned out to be one `probe-rs` already speaks natively — reversed with zero WSL2↔Windows USB forwarding needed.
- 2026-08-18 — Added row 11: `serial_log` against a DUT's own console UART was never a real design goal — only `embarch-dev-bench`'s link is — closing `embarch-api/milestone-7.md` §3.10's empty-capture investigation as design-intent-clarified rather than an unroot-caused bug.
- 2026-08-18 — Added row 10 (and fixed it the same day): the installed Windows Core service can't reach `artifact_path_for_core`'s UNC path at all (Session 0 isolation) — every prior "confirmed working" claim was against a foreground Core only. Fixed by uploading bytes instead for `WslHost`/`Remote`, retiring the UNC mechanism entirely.
- 2026-08-18 — Added row 9: elevation self-triggered by `embarch-core` now, not just reported and left to a human's own elevated shell.
- 2026-08-15 — Initial version, closing item 65 of that day's design-improvement review.
| 59 | `CONFIG_LOG=n` was the right implementation of dev-bench decision 7 — the link is shared with a binary protocol, so the firmware has no logging subsystem | **Decision 7 forbids unframed log *text* on the wire, not the logging subsystem, and reading it as the latter cost three weeks of a blind bench.** A log backend that emits `LogLine`s satisfies decision 7 exactly, and the gap the `n` left is written down in the firmware's own source: `handle_hello`'s uptime/reset-cause diagnostic exists solely because "a reboot is completely silent from Core's side." Every `LOG_ERR` in the BT host and the entire Zephyr fatal dump compiled to nothing. The transferable part: a decision that constrains a *mechanism* can be implemented as though it banned the *capability*, and the wrong reading looks identical to the right one in the Kconfig | 2026-08-26, asked to send the bench's `CONFIG_LOG` output to Core | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 38, amending decision 7 |
| 60 | The ESP32-C5's `sram0_0_seg` is simply full at 98.5%, so a feature needing ~11 KB has to be trimmed until it fits | **~10.5 KB of it was one buffer sized for a message that cannot arrive.** `receive_message` staged *inbound* frames at `DBM_MAX_FRAME_LEN` — a bound set by `StepResult`, which dev-bench only ever **sends**; the largest thing Core sends dev-bench is a 9,415-byte `StudyStart`. Reclaiming it took the whole logging subsystem from 5,504 bytes over to 736 bytes net. The other half of the finding is where the 11 KB actually went: ~5.1 KB is *code*, forced into IRAM by Espressif's own linker script (`log_core`/`log_output`/`log_msg`/`cbprintf`), which no amount of Kconfig trimming reaches. Found by `ram_report`-diffing a `LOG=n` against a `LOG=y` build after four hand estimates were each wrong in a different direction | 2026-08-26, making decision 38 fit on the active board | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 38 |
| 61 | Once the firmware holds its boot log until the first `Hello` and flushes it behind `HelloAck`, that record reaches Core | **`GET /dev-bench/hello` returns the instant it has the ack and drops the link, so on a freshly reset bench the boot record was flushed onto a wire nobody was reading — and every later handshake then had nothing left to flush.** Reliably produced and reliably lost, which is worse than absent: the file read as though the bench had never said anything about coming up. Core now drains for a bounded 250 ms after the ack, in every path that opens the link. Found by looking in the debug file for a line that should have been there and was not — the build, the tests, and the firmware were all correct | 2026-08-26, first live validation of decision 38 | [embarch-core/design.md](embarch-core/design.md) §3 decision 37 |
| 62 | An absolute `k_cycle_get_32()` in every outpost record is worth its cost — [embarch-outpost](embarch-outpost/design.md) decision 4 argued it twice, priced it, corrected the price, and built it | **The cost was in the wrong place, and the repo owner reversed it rather than a bench doing so.** The read sat *inside the context switch and inside `_isr_wrapper()`*, which is the instrument charging its cost to the exact path it measures — the same class of objection decision 3 had already used to refuse an `irq_lock` there, applied one level down and not noticed. Layout 2 removes every timestamp; `embarch-core`'s per-frame receipt time replaces it, so a frame becomes the resolution of the whole instrument and **ISR duration stops being measurable at all**. Worth a row for two reasons beyond the reversal itself: the decision had been re-argued and re-priced without anyone asking *where* the cost landed, and a side effect nobody had written down as a gap closed with it — the trace now shares `core_rx_utc_ms` with every other stream, where DUT cycles had never had a sync point to host wall time | 2026-08-26, on the owner's call, before any hardware capture | [embarch-outpost/design.md](embarch-outpost/design.md) §3 decisions 4, 17, 18 |
| 63 | `OUTPOST_SLOT_BYTES 16` describes the ring slot it sizes the ring by — asserted in a comment, divided into `CONFIG_EMBARCH_OUTPOST_RING_BYTES`, and pinned by a unit test | **The slot was 20 bytes, so every ring had been 1.25× its configured size since the module was written.** The test asserted `slots * OUTPOST_SLOT_BYTES <= RING_BYTES`, which is true of the constant and says nothing about the struct; the doc's own §4 stated "a ring slot is 16 bytes" beside a five-field struct that is 20. Found only because removing the timestamp made the two agree, and the equality was then worth a `BUILD_ASSERT`. The transferable part is row 44's shape again: **a hand-mirrored constant guarded by a comment and a test written in the constant's own terms** | 2026-08-26, implementing layout 2 | [embarch-outpost/design.md](embarch-outpost/design.md) §4, `src/outpost_priv.h` |
| 64 | `embarch-ui`'s load repartition shipped working on 2026-08-26 — built, unit-tested against a real capture, six new tests green | **It had never once rendered.** The Load button and the load table's `<tbody>` both carried `id="trace-load"`, so `getElementById` returned the button and every summary row was injected into it. Every Rust test passed, because the defect is entirely in the DOM. Found by driving `renderTrace()` against a real serialized `TraceView` in headless Firefox while reworking the axis — i.e. by *looking at* the tab. The transferable part: **a view whose logic is exhaustively tested in Rust and whose output nobody has looked at is the exact shape that hides this**, and this was the most-tested, least-looked-at surface in the UI | 2026-08-26, rendering the Trace tab in a browser for the first time since the summary landed | [embarch-ui/design.md](embarch-ui/design.md) §3 decision 10's amendment (h) |
| 62 | Turning `CONFIG_LOG` on and forwarding it to Core (row 59's fix) delivered what was asked for | **It delivered it *always on*, and "always" was never the ask.** The task was "send the bench's log to Core"; the level was a detail, so it became a Kconfig line — which is a choice made **once, for every study, by whoever last edited `prj.conf`**. The one study that needed the BT host's account of a failure could only get it by rebuilding and reflashing the bench. Fixed by compiling everything in and filtering at runtime per study (`Study.dev_bench_log_level`), applied on dispatch and reverted on study end *and* `Hello`. The transferable part: when a feature's cost is per-use and its configuration is build-time, "on" and "always on" collapse into the same option, and nobody notices until the feature exists and someone asks for it once | 2026-08-26, one commit after row 59's fix shipped | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 39, amending 38 |
| 63 | A log level set at runtime can raise verbosity above what the build was compiled with | **`LOG_MODULE_REGISTER(x, LOG_LEVEL_INF)` compiles every `LOG_DBG` in that file out of existence, and runtime filtering can only reduce below the compiled level.** A study asking dev-bench for `Debug` got the bench's own step-by-step account of *nothing*, silently — the calls did not exist to be filtered. Both halves of a per-study level scheme have to be built for the loudest level anyone may ask for, and the Kconfig that looks like "the default" is really "the ceiling". Found by running the study on hardware and seeing no lines, after the same study had already been fixed twice for unrelated causes | 2026-08-26, validating decision 39 on the bench | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 39 |
| 64 | Filtering a log record when the backend forwards it is equivalent to filtering it when it is created | **Deferred logging puts up to a second between the two, and the level can change in the gap.** dev-bench's log thread wakes on a 10-record threshold or a 1 s timeout; a study body that runs in ~50 ms therefore finished and reverted to the idle level *before* its own records were examined, and they were dropped under a level that was not in force when they were made. Fixed by flushing the log before every level change, so no record straddles one. The general shape: any state a deferred consumer reads must be flushed, snapshotted, or carried with the item — reading it at consumption time is reading the future | 2026-08-26, the fourth cause of one `Debug` study producing nothing | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 39 |
| 65 | Forwarding the bench's log over the bench's own link is a one-way flow | **It is a feedback loop, and at `Debug` it is self-sustaining.** Sending a line takes `link_tx_mutex`; Zephyr's kernel traces `k_mutex_unlock` at DBG; that record is forwarded, which takes the mutex again. The capture came back as an unbroken alternation of `<dbg> os: z_impl_k_mutex_unlock` and `<wrn> log: 5 record(s) dropped`. Fixed with a ceiling on the `os` module specifically, applied to Zephyr's own filter so the record is never created. Worth a row because the general case is easy to miss: **instrumentation that travels over the mechanism it instruments can amplify itself**, and it only shows up at the verbosity nobody tests at | 2026-08-26, first `Debug` study on real hardware | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 39 |
| 66 | An `on_event` arm with no `otherwise` should stay in its state, saving a flow-controlled pump loop from declaring a self-transition on every arm — asserted in the type's own doc comment while writing `.eap`'s state machine | **Staying put and re-entering are different, and a pump loop needs the one that re-enters.** No `otherwise` consumes the frame and leaves the state's `on_enter` un-re-run, so the `NEXT_CHUNK` that asks for the *following* chunk is never re-sent and the stall watchdog keeps counting from the original entry. The first draft of the BDS fixture consumed all 700 bytes correctly, acked none of them, and stalled. `otherwise: goto <this state>` re-enters, re-sends and restarts the watchdog. Both behaviors are real and the author now says which. Worth a row for how it was found: the design draft **had** written the self-transition explicitly, and the "simplification" was introduced while implementing, by someone reasoning about the type instead of about the protocol — the worked example was right and the type comment was wrong | 2026-08-26, writing the worked protocols as tests rather than as documentation, which is the whole reason they are tests | [embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decision 60 |
| 67 | The design draft's `manifest_crc` — pin a protocol manifest to a `Study` by a CRC over its text, following the `steps_crc`/`streams_crc` integrity pattern and `StreamEncoding::OutpostTrace { manifest_crc }`'s precedent | **The precedent had been reversed the day before (row 37) and, independently, decision 52 had already ruled the mechanism out for the analogous case.** Half of row 37's reasoning does not transfer — an `.eap` file is engineer-authored and does exist at author time, unlike a manifest generated from a linked image — but the other half does exactly: a saved study's pinned CRC goes stale on the author's next edit, the write-ahead staleness pattern `embarch-topology` §3 decision 3 exists to eliminate. And decision 52 had settled the same question for payload layouts one day earlier for a reason that is not about staleness at all: **Core cannot read the firmware repo**, so a study naming a manifest renders on its author's machine and nowhere else. Resolved into the study at build time instead, indexed by a `u8`. Worth a row because the draft was written by a session that could see decision 40 and not 41–57: **a design grounded against a real firmware can still be stale against its own suite**, and the reversals list is the cheapest place to have caught it | 2026-08-26, checking a design draft against the live `design.md` before implementing it | [embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decision 58 |
| 68 | Decisions 58-62 shipped the `.eap` mechanism — the types, the parser, the validator, the three seals, `Action::RunProtocol`, `ProtocolOutcome`, 231 tests green and a `no_std` staticlib cross-compiling for the bench | **`DevBenchMessage::StudyStart` had no `protocols` field, so nothing a study declared could reach the node that executes it.** `Study.protocols` existed, `Study.protocols_crc` existed, `Action::RunProtocol { protocol, entry_state }` existed and indexed them — and the one hop between the host and the interpreter carried none of it, so the index pointed at something dev-bench had never been sent. Every test passed, because every test was on one side of that hop. The pass that shipped it even recorded the gap correctly in prose ("the real-time interpreter is `embarch-dev-bench`'s own scope") without noticing that the *wire* was missing too, not just the executor. The transferable part: **a message type is the one place a "this half is someone else's scope" split can hide a hole**, because the half you are not building is exactly the half whose input you never construct — and `--all-features` green across four repos said nothing about it | 2026-08-26, starting the firmware half and looking for the field to decode | [embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decision 58's amendment, [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 41 |
| 69 | `ProtocolDef` is 17,496 bytes `no_std`, so two of them are ~35 KB against 71 KB free — measured, not estimated, and the number the firmware has to fit | **The measurement was real and measured the wrong thing, in both directions at once.** A firmware does not hold a `ProtocolDef`: it holds what the interpreter acts on, and every cross-reference inside one is already an index — so dropping every name but `StateDef.name`, and packing an `Operand` whose `i64` literal wastes six bytes of alignment padding in the most-multiplied type in the grammar, puts the real C structure at **4,376 bytes**, a quarter of the figure. And the term nobody estimated turned out to dominate: three staging buffers scale with the *wire* bound, and eleven count ceilings multiply to ~7.4 KB of encoded manifest per protocol — mostly names the firmware discards — against a real worked protocol whose entire `StudyStart` is 398 bytes. The fix for that half is not a tighter count anywhere; it is a **disclosed byte cap over the span**, because a product of eleven ceilings states a capacity that is false. Final cost 81.12% → 87.04% of `sram0_0_seg`. The transferable part: **a `size_of` on the sending side is not a bound on the receiving side**, and a serialized worst case can be larger than the value it serializes | 2026-08-26, sizing the C structures before writing them, as the design pass asked | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 41, [embarch-study-designer/design.md](embarch-study-designer/design.md) §7 |
| 70 | Row 60's buffer-sized-for-a-message-that-cannot-arrive was an inbound finding, fixed in the inbound direction, and the general bound is fine for everything else | **The same defect sat in the outbound path the whole time, and this schema version was about to grow it.** `main.c`'s TX staging buffer was `DBM_MAX_FRAME_LEN` — the general bound, which is `StudyStart`'s — for a message dev-bench **cannot produce**, since Core is the only sender of one. That was ~6.6 KB, and would have become ~9.7 KB the moment `DBM_MAX_STUDY_START_LEN` grew to carry a protocol manifest. Row 60 fixed one direction and named the reasoning precisely; nobody applied it to the other, because the RX buffer was where the pain was. The transferable part is narrower than row 60's own and worth stating on its own: **when a finding is "this bound covers a case that cannot happen here", the mirror-image bound is worth checking in the same pass** — there are usually exactly two, and fixing one makes the other harder to see, not easier | 2026-08-26, hunting for SRAM to pay for the `.eap` interpreter | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 41, amending row 60's own fix |
| 71 | `embarch doctor`'s check 11 — "`study_designer_schema_version` agrees" — is a `Warn` because the version isn't available yet: `embarch-study-designer` "isn't wired into `embarch-core`/`embarch-api` as a dependency yet (embarch.md §3), so neither side has a version to compare" | **Both sides have had one for a long time, and the check that exists to catch a version skew is a hardcoded string that never looks.** `embarch-study-designer` is a direct dependency of both; Core has served `study_designer_schema_version` on `/status` since 2026-08-25 and `embarch-api` compares its own compiled-in copy before every submit. Meanwhile the live pair had been sitting at Core wire **v13** against a bench flashed to **v14** — refused by the handshake, correctly and loudly, but only to whoever typed `GET /dev-bench/hello` by hand; `doctor` said `WARN … not available yet` throughout. The transferable part: **a check stubbed out with its blocking reason written into the message ages into a check that asserts a falsehood**, and it reads as a known gap rather than a wrong answer, so nobody re-reads it. Recorded rather than fixed in the pass that found it — the pass had already moved the wire, and a doctor check that gates deploys deserves its own | 2026-08-26, checking what the live deployment was running before redeploying for schema v15 | [embarch-umbrella/design.md](embarch-umbrella/design.md) §7, `src/doctor.rs`'s `check_schema_version` |
| 72 | The pump loop in `bds_batch_download.eap` — count received bytes against the total a `PROGRESS{0,total}` announced, and leave the pumping state once `received >= expect_total` — is the shape a flow-controlled download takes, proven against a 700-byte/200-byte-chunk sequence through the reference interpreter | **It is the shape a download takes when no chunk is ever lost, and the first real firmware loses chunks by construction.** The reference-dut bursts `CONFIG_BDS_CHUNKS_PER_REQUEST` (4) notifications per control write into a bench that queues a protocol's notifications **four slots deep**; 275 were dropped in one 5-minute window. A byte count that misses one chunk is short *forever* — the state stalls to its watchdog and aborts a transfer the device fully intended to finish, three times in a row, each ending in the device's own `BDS_ERR_TIMEOUT`. The real firmware already publishes the answer the count was reconstructing: `bds.c`'s "offset == total is the transfer-complete sentinel", carried on the low-rate `status` characteristic. Reading completion off the device instead of inferring it took the same study from 0 completed transfers to **34 of 34** | 2026-08-27, first `.eap` protocol run against a real DUT | [embarch-study-designer/design.md](embarch-study-designer/design.md) §4.9 |
| 73 | A `StreamSource::GattNotify` tap captures the characteristic's notifications, and `StreamRef.truncated` is how a capture that lost data is prevented from reading as a whole one | **A tap on a characteristic that is also a `RunProtocol` source is fed from the interpreter's own dequeue, so every notification the four-slot queue drops is payload the tap never sees — and `truncated` stays `false` while it happens.** The two drop counters are not connected: the transcript's own drops set the flag (`gatt: truncated: true`, 3 entries), the protocol queue's 275 reach only dev-bench's log line "its final state is NOT trustworthy". The capture reported 28,548 bytes and claimed to be complete. Moving the bulk characteristic onto a `GattMonitorSelectedStart` window — whose callback emits straight from the BT RX thread with no four-slot queue in the way — while the protocol keeps only the low-rate control/status pair, took the same 5 minutes from 28,548 bytes with 275 silent losses to **714,927 bytes with none** | 2026-08-27, same run | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 41 |
| 74 | `WATCHDOG_GRACE_MS = 2000` is a placeholder, but a placeholder on the safe side — it sits on top of a step's own `timeout_ms`, which is the part that actually bounds the step | **The margin has to cover work that happens *after* dev-bench's clock expires, and that work scales with how much the step captured.** The bench decides a step is over, and only then tears down subscriptions, drains its transcript queue and encodes a `StepResult` — all queued behind whatever stream data is still in flight on the same serial link. A 300 s `RunProtocol` step that moved ~13 KB reported late, and Core discarded a study whose capture was sitting **complete on disk** and still served fine over `GET /study/{id}/stream/{name}`, which is the only reason the run was recoverable. Give-up at 05:23:04.299 was exactly `3000 + 300_000 + 2000` after the preceding `StepResult` | 2026-08-27, same run | [embarch-core/design.md](embarch-core/design.md) §3 decision 16 |
| 75 | `run_study`'s `reflash` builds and flashes the named firmware, then verifies — the same contract for the bench and for the DUT | **Only the bench half resets. The DUT half flashes and returns**, so `reflash = "dut"` reports a successful flash while the board keeps running the previous image — the exact coupling [embarch-dev-workflow.md](embarch-dev-workflow.md) §4a records as "Coupling 1a" and warns about for hand-driven flashing, sitting unfixed inside the one call that was supposed to make flashing automatic. Found by reading `reflash.rs` before trusting it, having been bitten by 1a the day before | 2026-08-27, preparing the first `.eap` run | [embarch-api/design.md](embarch-api/design.md) §3 decision 32 |
| 76 | `reseal` recomputes a submitted study's seals so a hand-authored `Study` can't be rejected for a stale CRC — "both seals (`steps_crc` over steps, `streams_crc` over streams) are recomputed and overwritten regardless of what's submitted" | **There are three seals, and it recomputes two.** `protocols_crc` shipped as a deliberate *sibling* of the other two (decision 58's "the study's third seal"), Core validates all three independently so a mismatch names which third is corrupt — and `embarch-api`'s reseal was never extended past the pair it was written against. A study carrying a real protocol is rejected `400` unless the submitter computes the third seal itself, which no documented authoring path does. The tool description still says "both seals" | 2026-08-27, submitting the first study with a non-empty `Study.protocols` | [embarch-api/design.md](embarch-api/design.md) §3 decision 32 |
| 77 | `embarch deploy-core` closes the gap row 9 opened by verifying the installed binary actually changed — "the one thing the manual procedure cannot do for you" | **It verified a byte count, and reported success after its own elevated half was explicitly cancelled.** The UAC child never started (`The operation was canceled by the user`), the command printed its own diagnosis of that — "the elevated child never started, which is what a UAC prompt that never rendered looks like" — and then printed `deploy-core: landed, and com.embarch.core is running` anyway. The installed binary was a *different build of the same size*, so the size check it does have could not tell (`d3c2a0fe…` built vs `796d5e16…` installed), and it said so in a `note:` it then ignored. Self-elevation itself is fine — both cancellations were nobody being at the machine, and a third run with the owner watching landed cleanly (hashes equal, service restarted). The command's diagnostic cannot tell a dialog nobody answered from one that never appeared, and the wrong one was believed for an hour | 2026-08-27, deploying row 74's fix | [embarch-umbrella/design.md](embarch-umbrella/design.md) §3 decision 32 |
| 78 | dev-bench reporting "disconnected during service discovery" names what the link was doing when it dropped, which is the useful half | **It names the only half it has, because `disconnected_cb` opens with `ARG_UNUSED(reason)`.** The HCI disconnect reason — supervision timeout vs. MIC failure vs. the peer hanging up vs. unacceptable connection parameters — is handed to the callback and discarded, so every `disconnected during X` failure in this suite is a diagnosis with its cause deleted. Two consecutive runs died this way and the reason had to be inferred from what else was in the log | 2026-08-27, same run | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4 |
| 79 | Bumping `embarch-outpost`'s pin in the reference-dut's `west.yml` is a one-line change plus a `west update`, and the thing to check first is whether record layout 2 breaks anything the DUT's own code depends on | **Every compatibility check passed, and the blocker was somewhere nobody had thought to look: the module checkout itself was not clean.** The public header was unchanged between the pinned commit and HEAD, the module's `Kconfig` had a zero-line diff, and the reference-dut had no C-level outpost API calls at all — its integration is a snippet's `.conf` and `.overlay`. The update was nevertheless unsafe, because `modules/embarch-outpost` carried **432 uncommitted lines across 10 files plus an untracked `src/outpost_time.h`** implementing the *opposite* design from canonical HEAD, and `west update` would have discarded it. The transferable part: **before an update that moves a pinned dependency, `git status` every module it will touch** — the compatibility question ("does the new revision fit?") and the safety question ("is there work here the move would destroy?") are different questions, and only the first one is the one people think to ask. The sibling `zephyr` checkout was dirty too and was *fine*, which is what makes the check worth running rather than assuming: its four modified files were exactly `patches/001-004.patch`, reapplied by the build's own patch driver | 2026-08-27, unblocking the outpost flash | [embarch-outpost/design.md](embarch-outpost/design.md) §3 |
| 80 | Record layout 2's finding — that reading `k_cycle_get_32()` inside the context switch and inside `_isr_wrapper()` is the instrument charging its cost to the path it measures — means **the DUT must stop keeping time**, and a frame's arrival stamp is the resolution the whole instrument now has | **The objection was to the lock, not to the clock, and only the lock had to go.** On a GRTC-timed nRF, `k_cycle_get_32()` resolves to `sys_clock_cycle_get_32()`, which takes a `k_spin_lock` around `nrfx_grtc_syscounter_get()`, which takes a *second* critical section around a 64-bit read in a retry loop — all to hand back 32 bits that are sitting in the SYSCOUNTER's low word. Reading that word directly costs no locks and keeps per-record ordering, which a host cannot reconstruct from frame arrival at all. Layout 2 paid for a real problem with a capability it did not need to spend. What it cost was visible the moment a real capture was re-examined under layout 3: five `isr_enter`/`isr_exit` pairs straddle a frame boundary with **identical enter and exit cycles** — they took no measurable time and the ring simply drained between the two records, and layout 2 could not tell those from ISRs that ran for a whole frame interval. The version went **2 → 3, not back to 1**, although the wire is byte-for-byte what 1 was: a version byte exists so a host can say "I decode up to N", and a number reused after an incompatible wire has worn a higher one cannot say that | 2026-08-27, reconciling two competing implementations | [embarch-outpost/design.md](embarch-outpost/design.md) §3, [embarch-study-designer/design.md](embarch-study-designer/design.md) §4 |
| 81 | `embarch-api build --snippet ble-shell --snippet outpost` builds that project with those snippets; the CLI help's "only meaningful for a `discovery = "zephyr-west"` project" is a note about which projects *have* snippets | **For a project configured with an explicit `build_command`, the flags are accepted, silently discarded, and the build reports `success: true, artifact_fresh: true` — having produced an image without the snippet in it.** The `.config` said `# CONFIG_EMBARCH_OUTPOST is not set` and `# CONFIG_TRACING is not set` while the tool said the build succeeded, which it had: it built the wrong thing, correctly. This is the same family as rows 73, 75 and 76 — an input accepted and quietly dropped, with a success report over the top — and it is the fourth in one day. The transferable rule this suite keeps re-deriving: **a flag that cannot be honoured must fail, not be ignored**; "only meaningful for X" in a help string is documentation, and documentation is not a gate | 2026-08-27, building the first real outpost DUT image | [embarch-api/design.md](embarch-api/design.md) §3 |
| 82 | `Route::Direct { port_serial }` identifies a signal's bridge the way `link_port_serial` identifies dev-bench's — "the serial already identifies exactly one device, so gating on VID could only ever exclude the right answer" | **The first real `Direct` bridge has no USB serial number at all.** The MCP2200 carrying the reference-dut's outpost UART reports none, so Windows falls back to a hub-location path (`5&3b87c881&0&6`) and Core's enumeration surfaces its first token: `serial_number: "5"`. The declaration works and resolves to exactly one port *today*, and a negative control with a bogus serial fails loudly and correctly — but the identifier is a location, not an identity, and it will change when the adapter moves to a different hub port. Decision 18's reasoning assumed a serial that identifies a device; this one identifies a socket. Recorded rather than fixed: the fix is either a second matching key (VID+PID+port) or an explicit "this bridge has no serial" declaration, and both are design work | 2026-08-27, declaring the first real signal | [embarch-topology/design.md](embarch-topology/design.md) §3 decision 18 |
| 83 | The outpost's remaining unknowns are the wire constants in §5.3 — "every wire constant is an unmeasured default" — so the first real UART will mostly be about whether 230400 baud and a 256-byte batch are the right numbers | **The first real UART said something else entirely: the DUT emits a burst at boot and then stops.** 355 bytes carrying four COBS frame delimiters arrived on the trace adapter after a flash-and-reset — the first outpost bytes ever to cross a real wire — and then nothing, across repeated resets and a 20-second capture opened *before* the reset. `CONFIG_EMBARCH_OUTPOST_HEADER_INTERVAL_MS` is 1000, so a quiet stream should still carry a header every second; it does not. The DUT is not dead — a `BleConnect` study against the same image passes — and every host-side half works: the manifest declares layout 3, Core accepts the flash, the signal resolves, the tap opens and is served. What real hardware had to say about this instrument was not about baud rates. Left with the repo owner, whose DUT-side it is | 2026-08-27, first outpost bytes on a real wire | [embarch-outpost/design.md](embarch-outpost/design.md) §7 |
| 84 | Row 83's own diagnosis: the outpost "emits a burst at boot and then stops", so Phase E is blocked on a **DUT-side emission stall** — a firmware bug in the module or its snippet | **The DUT never stopped, and this is not a new finding — it is 2026-08-26's "the blocker is physical", withdrawn on the strength of one 355-byte burst and now reinstated with much better evidence.** The nRF54L15 streams ~21 KB/s continuously, at 91% of line rate, and no serial port on the host receives a byte of it. SWD reads of UARTE20 show the instrument working as designed — `ENABLE=8`, `BAUDRATE=0x03B00000` (230400), `PSEL.TXD=0x20` (P1.00, the board's own console pin), `DMA.TX.PTR` at `frame_buf`, `DMA.TX.AMOUNT == MAXCNT` — and decoding the live record ring out of RAM shows a ~4.4 ms cycle whose UARTE ISR spans 4157 µs, which is what 92 bytes at 230400 takes to the microsecond. **What makes today's version of the claim stronger than the withdrawn one is a positive control**: the same all-port scan that saw zero from the DUT saw 2162 bytes on COM12 and 1562 on COM13 when dev-bench was reset, so the method demonstrably detects traffic; and the ordinary no-snippet console build (uart20, 115200, the owner's daily configuration) was re-run and produced zero on all five ports across a reset with `DMA.TX.AMOUNT = 14` proving it had transmitted. **The 355 bytes are still unexplained**, and that is the honest weak point: a burst-then-silence is what a stalling emitter looks like too, which is why one unexplained burst was enough to overturn a correct diagnosis for a day | 2026-08-27, SWD register + live-ring reads, a plain-console control build, and a positive control on the same scan | [embarch-outpost/design.md](embarch-outpost/design.md) §7 |
| 85 | Row 84's conclusion: the outpost emits correctly and "what remains is physical — nothing on the host is connected to P1.00", so closing Phase E needs a rewire | **Nothing was connected to P1.00 because P1.00 is not this board's debug pin. The DUT on the bench is `dut_dev` revision 7, and every build for three weeks had been `roadrunner@2`.** The owner supplied the one fact in a sentence — "my board is nff v7" — and it collapsed the whole investigation: `dut_dev@7` puts console uart20 on **TX P1.05 / RX P1.04**, and `dut_dev_npm1304_common.dtsi` had already written down the exact symptom being chased, in the tree, months earlier: *"transmitting (TXDRDY / ENDTX / TXSTOPPED all fire, DMA AMOUNT correct) — the byte simply lands on the wrong pad ... nets crossed"*. Rebuilt with `-b dut_dev@7/nrf54l15/cpuapp -S ble-shell -S outpost`, flashed, and the trace appeared instantly: **437,789 bytes in 20 s (21.9 KB/s)**, decoding to 43,948 records with ISR and thread names resolved from the ELF. A study then captured 92,724 bytes on its `outpost` tap and the UI's Trace view rendered it named and timed, 9205 rows over 766 frames, `records_lost: 0`. **Where the wrong board came from is the part worth keeping**: `embarch-api`'s `config.toml` had its board "corrected" on 2026-08-06 by reading the reference-dut repo's own stale `build/build_info.yml` — a derivation from an artifact, presented in that file's comment as authoritative. Nobody looked at the board, and the outpost snippet's own header had said `-b dut_dev@7/nrf54l15/cpuapp` in a code block the whole time. Rows 83 and 84 are both downstream of that: three diagnoses, two wrong, and the correct one needed a fact no amount of reading could supply | 2026-08-27, the owner naming the board | [embarch-outpost/design.md](embarch-outpost/design.md) §7; [embarch-api/design.md](embarch-api/design.md) §12 |
| 86 | Row 80's own account of what layout 3 bought — "what it cost was visible the moment a real capture was re-examined under layout 3: five `isr_enter`/`isr_exit` pairs straddle a frame boundary with **identical enter and exit cycles**" — treated as a fixture observation, with `embarch-ui`'s frame-arrival axis left in place as still-honest ("the view still places spans on a frame/ms axis, so it draws these five as crossing — which is honest about what it plotted") | **The observation was right and its scope was wrong: it was not five spans in a fixture, it was every span in every capture, and the host kept reading the coarse clock for a day.** `embarch-ui`'s `trace.rs` parses columns 0, 2, 5, 6, 7, 8 and never read `cycles` (3) or `us` (4); its module doc still said "an outpost record carries no timestamp at all". So every span was timed by its *frame's* arrival stamp, and a span whose two ends fall in adjacent frames — which is the outpost drain thread's normal state, since it switches in on one frame and out on the next — was charged the whole frame interval. On the reference reference-dut capture that reported **`drain_thread` at 78.1% of measured extent against a true 1.6%**: 3.9 ms charged for 85 µs of work, a **46× error**, on the single number the next session was briefed to go and reduce. It also reported **4286 of 4955 spans as below the resolution when none were**, every ISR in the system as effectively unmeasurable against a true 0.6% of window with all 4955 spans timed, and 10.6% of the window unaccounted for against a true 0.1%. **A second, independent half of the same fault was in Core**: `render()`'s doc comment claimed "decoding at the end also means a header frame that arrived late still names every record before it", and it did not — the manifest and `cycles_per_sec` were latched when the decode loop *reached* the header frame, so 488 of the capture's 9205 rows rendered with an empty `us` and an empty `name` inside a stream whose index said `named: true`. That alone would have held the view on the coarse clock even after the UI was fixed. **The transferable rule: a wire-format change is not done when the DUT emits it — it is done when every host that decodes it has been re-measured against it.** Row 80 is what happens when a new capability is verified to *exist* rather than verified to be *used* | 2026-08-27, re-measuring the reference capture's own bytes on both clocks | [embarch-ui/design.md](embarch-ui/design.md) §3 decision 10, [embarch-outpost/design.md](embarch-outpost/design.md) §3 decisions 4, 17 |
| 87 | [embarch-outpost/design.md](embarch-outpost/design.md) §3 decision 8: a thread created into a plain `static struct k_thread` "has no distinguishing symbol prefix, and identifying it would need DWARF type inspection. Those threads render as raw pointers" — an accepted limit, with `K_THREAD_DEFINE` offered as the engineer's fix | **Accurate about the limit and wrong about its size, and the DWARF inspection it named and declined takes about eighty lines.** The name match resolved **5 of the reference-dut's 20 thread objects**. The fifteen it missed are most of what a quiet trace does — `drain_thread` (the outpost's own, 778 runs in the reference capture), `bt_long_wq` and `bt_workq`, `k_sys_work_q`, `prio_recv_thread_data`, `recv_thread_data`, `app_sm_thread_data`, `shell_uart_thread` and four application work queues — and **four of them are not a `struct k_thread` at all**: a `struct k_work_q` starts with one, so a work queue's address *is* its thread's address, and no naming convention could ever have found them. "Declare it with `K_THREAD_DEFINE`" is not available for any of them; they are Zephyr's and the BLE stack's, not the engineer's. The fix joins two facts the build already recorded — DWARF says what a variable is, the symbol table says where it lives — and resolves 20 of 20 with nothing unmatched. Two details decided it: **type is taken from any DIE including a declaration**, because Zephyr's own kernel objects appear in this image *only* as `DW_AT_declaration` (typed, no address) and insisting on a DWARF location loses exactly the four a quiet trace spends most of its time in; and **first-member recursion stops at any non-zero offset**, because then the outer object's address is not the thread's. The same pass found the equivalent ISR gap: `_sw_isr_table` holds nothing for an `IRQ_DIRECT_CONNECT` line, and on this board the one line that does it is **the radio** | 2026-08-27, reading the reference-dut ELF's DWARF | [embarch-outpost/design.md](embarch-outpost/design.md) §3 decision 8 |
| 88 | Row 86's own framing of what was left after the clock fix — that the wire is the problem and the instrument's own 50.4% share of records is the thing to cut, so removing those records is "almost certainly the single biggest win" and "should cut record rate *and* improve resolution (fewer records → frames fill and flush sooner)" | **Removing half the records moved the link's duty cycle from 99% to 96%, and improved resolution not at all.** Both halves of the prediction fail for the same reason, and it is the drain loop's *shape* rather than its volume: `drain_thread_fn` sends, blocks on the transmit, and then drains exactly the records that arrived while it was blocked — so frame size settles at whatever the record rate produces during one frame's own wire time, and the duty cycle sits near 100% however few records there are. Fewer records make frames **smaller**, which makes them transmit faster, which makes the loop spin faster. Measured at 460800 with the instrument already excluded: **3.3 records per frame, 42.8 bytes**, of which ~9 bytes is frame type, seq, count, CRC-32, COBS stuffing and delimiter — **a fifth of the link carrying no records at all**. The fix is to wait for the batch to fill (§3 decision 20), which took frames to 20.7 records and the duty cycle to 37%, and it is **only free because layout 3 restored the per-record clock**: under layout 2 a frame's arrival time was the only clock a record had, so delaying a frame would have moved every event in it. The resolution half of the prediction was already moot for the same reason — frame size stopped affecting resolution the moment `cycles` came back, which also inverts §7's old advice to *shrink* `EMBARCH_OUTPOST_BATCH_BYTES` for finer frames | 2026-08-27, measuring each lever separately on real hardware | [embarch-outpost/design.md](embarch-outpost/design.md) §3 decisions 19, 20 |
| 89 | `EMBARCH_SIGNAL_BAUD`'s default of 1000000 and the outpost overlay's 230400 are both just numbers to tune, and raising the link rate is the straightforward lever whenever the wire runs short | **The link has a hard ceiling at 460800 and neither end chose it.** The MCP2200 bridge on this bench **refuses to open above 750000** — COM15 at 921600 and at 1000000 both fail with "a device attached to the system is not functioning", while 750000 and below open and carry bytes, tested straight against the port with no Core in the path. The nRF54L15's UARTE, meanwhile, has **no baud between 460800 and 921600**: `uart_nrfx_uarte.c`'s `NRF_UARTE_BAUDRATE` macro is a fixed ladder, so 750000 — which the bridge would take — is not a rate this SoC can produce. The two constraints intersect at exactly one useful value, and it is 2× the old one rather than the 4.3× that 1000000 would have been. Worth noting what that costs: **1000000 is `embarch-core`'s own `EMBARCH_SIGNAL_BAUD` default**, so the rate that would have needed no Core-side configuration at all is the one this bench cannot do, and `EMBARCH_SIGNAL_BAUD=460800` now has to be set and kept in step with the DUT overlay by hand | 2026-08-27, sweeping the bridge directly | [embarch-outpost/design.md](embarch-outpost/design.md) §7 |
| 90 | `embarch-core` §3 decision 30(a)'s signal tap "opens Core's own third serial port on its own thread" and writes what arrives verbatim — the capture starts when the port opens, and a `Route::Direct` DUT's bytes are the study's bytes | **A TX-only DUT has been transmitting the whole time, so opening the port hands you whatever the OS buffered before the study existed.** The first six records of a real study's outpost trace carried a cycle count **195 seconds ahead of the seventh**: the DUT had been reset in between and the pre-reset tail was still sitting in the driver's receive buffer. Core now clears the input buffer on open. **The part worth keeping is why this went unnoticed for two days of real captures**: against Core's own monotonic `rx_utc_ms` a stale prefix is *invisible* — it reads as a capture that happened to start earlier, and every number derived from it is plausible. It became visible within an hour of `embarch-ui` starting to time spans by the DUT's own counter, which restarts at reset: the 1.2-second capture's window came out as **11 ms** and two lanes claimed a **9159% share**, which is not a number anyone can look past. Second time in two days that row 86's clock rework surfaced a defect that was always there and always silent. The same investigation separated it from a *harmless* backwards step of 13 µs — a hook stamps the counter before it reserves its ring slot, so an interrupt preempting a thread publishes after it and is stamped before it — and the line between the two is drawn by the host clock rather than by a chosen threshold: a backwards step longer than the capture's own duration is two clocks contradicting each other | 2026-08-27, first study run with the DUT clock as the axis | [embarch-core/design.md](embarch-core/design.md) §3 decision 30, [embarch-outpost/design.md](embarch-outpost/design.md) §3 decision 17 |
| 91 | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4, 2026-08-27 morning: keeping the HCI disconnect reason in a step's failure text "earned itself immediately", turning `disconnected during service discovery` into `disconnected during service discovery (HCI 0x08, supervision tim…)` — strictly more diagnostic information, at no cost | **The extra information is what stopped the information arriving.** A longer reason pushed `StepResult` from ~30 bytes to 81-88, and only frames that long lose their tail: the frame reaches Core **exactly 16 bytes short** of what its own COBS code byte promises, Core refuses it and drops the link. So three consecutive studies that died of the *already-documented* ACL-exhaustion/supervision-timeout fault (§4 finding 3, `Unresolved`) reported no step failure at all — only `failed to decode DevBenchMessage ... The original data was not well encoded`, which reads as if the link, not the DUT, were at fault. The change that made the fault diagnosable is the change that stopped the diagnosis being delivered, and the two were made hours apart by different work. **Neither side was wrong on its own**, which is why nothing caught it: the encoder writes every field it should (`serial_protocol.c`'s `DBM_TAG_STEP_RESULT` case writes all four trailing options on the `Fail` path, checked), and the reason improvement is plainly correct. What is missing is a test at a *length* nobody had reason to pick, and Core's own posture — everywhere else in this suite an undecodable frame costs the frame and not the link, which is exactly why an outpost frame carries its own CRC | 2026-08-27, three studies reproducing it, then reading the COBS code byte against the arrival length | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4 |
| 92 | Row 91's own diagnosis, and the doc it produced: the short `StepResult` is *"a truncation in transit"* whose identifying fact is that **"the number that identifies it is 16"**, so the next step is to *"find the 16-byte boundary — dev-bench's COBS output staging, its UART TX path, or Core's read chunking"* by capturing the link directly | **There is no boundary, there is no 16, and the sender was not losing bytes — it was ceasing to exist mid-frame.** dev-bench **resets partway through transmitting** the frame: the link handshaked at `uptime 899843 ms`, the short frame landed at `17:25:39.647`, and the next handshake reported `uptime 46320 ms` — a reboot at `17:25:39.8`, inside 150 ms. Every fact that had looked like a transport fault is a consequence: the prefix is byte-perfect because transmission got that far; one run's last byte arrived `0xf8` where `0x38` was sent because the line went idle mid-byte and the remaining bits sampled as the idle `1`; **nothing follows at all** — no `StudyDone`, no `StreamClose`, thirteen seconds of silence on a live port — because the bench is rebooting; and only long frames are hit because short ones finish before the crash window. It is also **intermittent**, which no boundary can be: the identical 88-byte frame decoded fine on study `f13f6a82`, whose bench did not reset. **The 16 was an artifact of the diagnostic that reported it** — Core subtracted a delimiter-inclusive arrival length from a delimiter-exclusive COBS claim, and the corrected six occurrences read 13, 15, 17, 17, 13, 17, no constant anywhere. So an off-by-one in a debug message became the name of a bug and the shape of the investigation into it. **Two things are worth carrying forward.** First: the fix that found the cause was the *resilience* fix, not an investigation — making an undecodable frame cost the frame instead of the link let a run keep reading long enough to observe the silence, and the silence is the whole diagnosis; tolerating a fault is what exposed it. Second, on method: this is the **third** well-evidenced wrong answer on one fault (row 91's "the encoder forgot the trailing options", then "the wire or the staging buffer lost bytes"), and each was refuted by a check on the *other* end rather than by more reading of the same bytes — the encoder by its own source, the wire by dev-bench's own uptime counter | 2026-08-27, comparing dev-bench's uptime across the reset the fix let Core survive | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4, [embarch-core/design.md](embarch-core/design.md) §3 decision 40 |
| 93 | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4 finding 3's account of the DUT-side link loss, and the brief derived from it: *"the trigger for the failure is the `batchmgr-stop` / `batchmgr-start-ppg` pair preceding the NUS write"* — a specific pair of control writes, in a specific position | **The trigger is not a step, it is a *state*: once PPG is running, the reference-dut cannot service a GATT operation inside the supervision timeout, and whichever operation comes next is the one that dies.** Moving the batchmgr pair to *after* the NUS window moved the failure with it: `sensor01 start` then passed and returned `All SENSOR01 measurements started successfully`, and `batchmgr-stop` — which had passed as step 2 in every previous ordering — died instead, `disconnected during service discovery (HCI 0x08, supervision tim`. The BDS service discovery that fails in the reordered run is the *same* discovery that succeeded before, and the only thing that changed is that PPG was flowing. That also retires three experiments the doc records as inconclusive: the settle intervals (3 s, 5 s, 12 s), the `reset` between runs and the explicit `BDS_CMD_ABORT` first step all changed nothing **because none of them stops PPG**. The clean-run corollary is the useful half — the reordered study completed the four steps before PPG started and captured **691 KB of outpost trace** with no truncation and no link loss, so the tracer and the transport are both fine and the blocker is entirely the DUT | 2026-08-27, reordering the same eleven steps and reading which one died | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4 finding 3 |
| 94 | Implicit in every dev-bench link diagnosis to date, including rows 91 and 92's: a study that reports *"no message received from dev-bench before the deadline"* received nothing, so the link's silence is the whole of the evidence | **Core was holding 699 bytes of the bench's own boot banner in a private buffer and had no code path that could mention it.** `recv` accumulates until a `0x00` delimiter — and this board's `zephyr,console` **is** the protocol UART (its own overlay's choice, since `usb_serial` could not carry 1 Mbaud), so a rebooting ESP32 writes `I (soc_init): ESP Simple boot` and thirteen more lines onto the same wire as plain ASCII with **no null byte anywhere in it**. Never framed, therefore never decoded, never logged, never counted. The reset that row 92 spent an uptime comparison and an extra handshake to establish had been announcing itself in clear text on the wire the whole time. **The generalisable defect is the shape, not the board**: a framed protocol whose deframer discards non-conforming bytes will report "nothing arrived" for the one class of message most likely to explain a fault — the bytes that were never meant to be protocol. Fixed by reporting the unframed tail in the failure reason (capped) and in full in the study's own `dev-bench` file — in full because the line naming a reset cause sits **past** any cap a reason can carry: the real capture's first 192 bytes were still inside the bootloader's SPI-flash preamble | 2026-08-27, printing what the deframer had been dropping | [embarch-core/design.md](embarch-core/design.md) §3 decision 40 |
| 95 | This same day's row 93, and the brief built on it: the DUT-side link loss is triggered by *"a state — once PPG is running, the reference-dut cannot service a GATT operation inside the supervision timeout"* — i.e. a CPU/radio-load problem in the band | **The DUT is 93% idle when the link dies, and what collapses is the connection interval, not the CPU.** Its own outpost trace settles it: `rtc0_nrf5_isr` — the BLE controller's ticker — goes from 91 entries/s to **eight consecutive 626.4 ms holes**, metronomic, 6222 ms of 11609 ms with no tick, `swi_lll` and `radio_nrf5_isr` collapsing in lockstep; and *inside* the largest hole 504 SPI interrupts, 314 AFE-drain runs, 250 idle switches and 16 GRTC interrupts all fire. Interrupts on, kernel alive, radio simply not scheduled. **626.4 ms is the band's own configured connected-idle interval** — `lib/ble/Kconfig` defaults `BLE_CONN_INTERVAL_MIN/MAX_MS` to 540-660 with a 5000 ms supervision timeout, so 626.25 ms (501 × 1.25) is it exactly, and at that interval 5 s of timeout is **eight connection events** of margin. Spec-legal — `ble.c`'s own `BUILD_ASSERT` checks the 1320 ms minimum and passes — and thin. The lever to avoid it already exists in the firmware and fires **downstream of the exchange that needs it**: `lib/bds/bds.c` asks for the fast interval only once a backlog threshold is crossed, and its own comment admits *"small one-off transfers skip the fast switch entirely"* — but the write that dies is the control-point write, before any backlog exists. Confirmed by measuring rather than reasoning (`ble status` over NUS: 50 ms before, 30 ms after `ble speed fast`, still 30 ms with PPG running) and by fixing it: one `ble speed fast` step before the BDS work took the study from dying at step 5 of 11 to **14 of 14 steps and `completed`**, two-minute drain held, 2.4 MB of trace. **PPG running was a correlate all along** — it is what coincides with the slow interval taking effect | 2026-08-27, reading the DUT's own trace, then asking the DUT | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §4 finding 3 |
| 96 | [embarch-outpost/design.md](embarch-outpost/design.md) §7 and the record-layout-4 plan: the traced DUT wants **4141 rec/s delivered at 97% duty plus 5452 lost across 113 gaps**, roughly 4700 rec/s more than 460800 can carry, so *"the record must get smaller"* is a requirement for the demo rather than an optimisation | **That number was measured on a run whose BLE link was in the failed state, and it is not what a healthy traced capture does.** The first capture taken with the link actually holding — 140 s, PPG flowing, the full drain — runs at **1606 rec/s at 10.66 B/record**, about 17 KB/s against 460800 baud's ~46 KB/s, with **1.12% frame loss**. The instrument has roughly 2.5× headroom, not a 2× shortfall. What differs is the PPG path itself: `g_AFE_DRAIN_WORK_Q` ran **~500 times/s on the failing run and 36 times/s on the healthy one**, and `nrfx_spim_21` ~800/s against 61/s — consistent with a drain loop retrying against a link that cannot take the data, though the causal direction is inferred here rather than measured. **The transferable part is the sequencing error**: a capacity requirement was derived from a capture of a broken system, and would have bought a six-component wire-format change plus a version bump to fix a number that a one-line study change removed | 2026-08-27, first traced capture with the link healthy | [embarch-outpost/design.md](embarch-outpost/design.md) §7 |
| 97 | `embarch-ui`'s new step-row projection, built on the rule the rest of `trace.rs` already follows — anchor the DUT clock against the host's by taking each frame's arrival and the last DUT stamp in that frame | **A gap record's stamp is not one of that frame's stamps, and using it reordered the study steps on the first real capture.** A gap record is stamped when the *first dropped* record was lost rather than when the drain thread got around to reporting it (`outpost.c`'s emit site, and `embarch-ui/design.md` §3 decision 10(g) already says so about gap *bands*), so its stamp sits outside the run of stamps its own frame carries — measured here, **487 ms ahead** of every other record in the same frame, on a capture whose largest row-to-row step is 20 ms. One such record makes its frame's anchor jump forward and the next frame's read as a half-second backward step; **147 of that capture's 12,476 frames** were poisoned. The visible damage was in the step row: `batchmgr-start-ppg` vanished entirely (its two projected edges crossed, and the band was dropped) and `open-bds-data-window` came out starting 0.4 s *before* the step before it had ended. **The transferable part is that `parse` already filtered gap records out — twice.** `dut_backsteps` and `dut_step_max` both do it, for exactly this reason, and the new code was written against the shape of the data rather than against the two places that had already learned this. A rule that exists in two of the three places it applies is not a rule anyone will find; it is a coincidence | 2026-08-27, the first real capture taken with per-step stamps | [embarch-ui/design.md](embarch-ui/design.md) §3 decision 10(j) |
| 98 | `StreamClose { id, dropped }` is how a tap that lost data says so, and `dispatch_study` closes every tap still open at the end of a run by syncing scopes against one step past the last — "no scope covers it" | **No scope covers it *unless it is `WholeStudy`*, which covers every step index by design.** So a `WholeStudy` tap was never closed, never sent `StreamClose`, and never delivered its `dropped` count: `StreamRef.truncated` stayed `false` however much had been lost. The same drain study run twice minutes apart, differing only in its `gatt` tap's scope, went `Steps { 0, 10 }` → 16 dropped, `truncated: true`; `WholeStudy` → 31 dropped, **`truncated: false`** — the run that lost twice as much is the one that claimed to be complete. Its three load-bearing taps (`bds-data`, `bds-status`, `gatt`) are all `WholeStudy`, so all three flags were unfalsifiable while a PPG-continuity investigation rested on them. The suite's own test pinned both halves of the contradiction nine lines apart. Fixed by making end-of-study closing its own function rather than a step index chosen to fall outside every window | 2026-08-27, while widening a tap to answer a different question | [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 42 |
| 99 | `OUTPOST_EVT()` is safe to place anywhere, because the header no-ops when markers are compiled out and still checks the name — "the failure mode this is arranged to prevent does not depend on a Kconfig", in that header's own comment | **The header could not be *included* at all without the module, so its no-op path was unreachable from any application.** `zephyr_include_directories(include)` sat below `if(NOT CONFIG_EMBARCH_OUTPOST) return()`, and the cost landed exactly where a marker earns its keep: a hot path in driver or library code, compiled into both the tracing image and the shipping one. Reaching one meant the application wrapping every call site in its own `#ifdef` and hand-rolling the fallback the header already contained. The design was right and one line of CMake made it unreachable. Fixed by hoisting the include directory above the guard and letting the no-op degrade in three tiers instead of two — the third drops the name check, since with no registration header included there is no enumerator to check against | 2026-08-27, placing this module's first real markers into a DUT's PPG pipeline | [embarch-outpost/design.md](embarch-outpost/design.md) §3 decision 21 |
| 100 | `embarch-ui`'s chart aggregation, in its own comment: a lane's visible spans are found by a lower bound on `from` plus **one** step back, "exactly one, because spans within a lane do not overlap: the one before that ends no later than this one begins" | **A lane's spans overlap whenever the capture lost a record, and the committed reference fixture already contained the shape.** A lost `thread_switch_out` leaves its run open, and `trace.rs` draws every still-open span out to `t_to` — so later, shorter runs of the same thread sit *inside* it. Two of `0x08058240`'s 65 spans overlapped in `outpost-native-sim`. The one-step walk-back halted on the first nested span and the enclosing one vanished from every zoomed window past it; measured in headless Firefox, the old code drew **0** of a synthetic lane's spans where the new code draws 1. Fixed on both sides: the search is now over the lane's running maximum of `to`, and `trace.rs` no longer nests a thread inside itself | 2026-08-31, measuring the committed fixture during an `embarch-ui` bug-fix pass | [embarch-ui/design.md](embarch-ui/design.md) §3 decision 10(k) |
| 101 | `embarch-ui`'s Debug tab, in its own comment: the backlog fetch and the stream re-open are "kept in this order so a slow backlog fetch can never land lines into a console the user has since switched away from" | **Ordering cannot cancel a request already in flight.** `loadLogBacklog()` is not awaited and captured no identity, so the console its answer landed in was whichever was mounted when it *resolved*. Reproduced against the live Core with only embarch-core's `/recent` delayed: **204 lines under the "Live-tailed embarch-api log" heading, 200 of them embarch-core's**, in an order the timestamps contradict — which is the exact interleaving decision 13 refuses a merged stream to avoid. A generation token now invalidates a stale answer | 2026-08-31, driving the real app in headless Firefox against the live Core | [embarch-ui/design.md](embarch-ui/design.md) §3 decision 13 |
| 102 | `diff_new_lines`'s stated preference, for the live tail behind decisions 7 and 13: anchoring on the previous window's last line "falls back to replaying the whole new window rather than silently dropping content; a few duplicate lines in a debug viewer is a smaller problem than missing ones" | **The implementation did the thing the sentence rules out.** It found the anchor by scanning the new window *backward* for the line's most recent occurrence, so any log line repeating verbatim later in the window — a retry, a heartbeat, a stack frame — swallowed every line between the real anchor and that later copy. Its own test asserted the loss as intended behaviour. The window is a contiguous run of one append-only file, so the previous window's end is pinned by the whole suffix/prefix overlap, not by one line that happens to appear twice | 2026-08-31, reading the diff during an `embarch-ui` bug-fix pass | [embarch-ui/design.md](embarch-ui/design.md) §3 decision 13 |
