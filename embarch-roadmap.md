# embarch: feature roadmap

**Status:** draft, 2026-07-20. Restructured 2026-08-17 — see Changelog.

## Shipped foundation

The suite's original numbered milestones (Flash, Token, Study Designer scoping + implementation, Dev Bench scoping, Onboarding) are done and shipped as real `v0.1.0` releases across all three code-bearing repos (see Release, below) — they're recorded here as a foundation rather than as active numbered milestones, since none of them are still open work:

- **Flash** — EmbArch flashes a chip without USB-forwarding WSL2↔Windows, validated against a generic test board (`project-a-board`). Steps: [embarch-api/milestone-1.md](embarch-api/milestone-1.md), [embarch-core/milestone-1.md](embarch-core/milestone-1.md).
- **Token** — the insecure `dev-token-change-me` fallback replaced by an auto-generated, machine-wide token file Core persists and embarch-api discovers on its own. Full design: [embarch-token.md](embarch-token.md). Steps: [embarch-api/milestone-2.md](embarch-api/milestone-2.md), [embarch-core/milestone-2.md](embarch-core/milestone-2.md).
- **Study Designer, scoping + implementation** — `embarch-study-designer`, the shared `no_std` Rust library defining hardware-in-the-loop study types (BLE advertise/connect/data-exchange plus power profiling), designed and implemented with its full type model, wire-format tooling, and schema versioning. *(This entry originally also named validation actions and a `SignalCheck` evaluator; post-hoc validation was removed outright 2026-08-25 — `embarch-study-designer/design.md` §3 decision 48 — having never once run.)* Full design: [embarch-study-designer/design.md](embarch-study-designer/design.md). Steps: [embarch-study-designer/milestone-3.md](embarch-study-designer/milestone-3.md).
- **Dev Bench scoping** — `embarch-dev-bench`'s architecture scoped: Zephyr-based, cross-vendor (one shared application across per-vendor west workspaces), BLE + power sampling v1 scope. Full design: [embarch-dev-bench/design.md](embarch-dev-bench/design.md).
- **Onboarding** — `embarch-umbrella` (the `embarch` binary): `setup`/`init`/`doctor`/`status`/`up`/`down`, `base_url = "auto"` in API, `start`/`stop` in Core, and the suite's first real release binaries. Full design: [embarch-umbrella/design.md](embarch-umbrella/design.md); user-facing guide: [embarch-user-guide.md](embarch-user-guide.md). Steps: [embarch-umbrella/milestone-6.md](embarch-umbrella/milestone-6.md).

Two real gaps this foundation left behind, which is exactly what the new milestones below close: no shipped milestone ever flashed real hardware through a real client repo (`project-a-board` was a placeholder; the real `reference-dut-fw` repo was only ever used to validate config/discovery, never an actual physical flash — see `embarch-umbrella/milestone-6.md`'s 2026-08-13/14 entries), and `embarch-dev-bench` firmware has never been run on physical hardware at all (`embarch-dev-bench/design.md` §4).

## Milestones

Each milestone below uses a real umbrella-suite release plus the real `reference-dut-fw` client repo as its test target, rather than a placeholder project or a synthetic fixture. Numbered starting fresh at 1 — the shipped foundation above is intentionally not renumbered into this sequence. *(Filename note: when an execution doc is created for one of these, it's filed as `milestone-N.md` using N = 7/8/9/10 — continuing the on-disk numbering past the shipped foundation's 1–6 — specifically so it can't collide with those still-live historical files; only this doc's own prose uses the fresh 1–4 numbering.)*

### 1 - Flash & Build (real hardware)

Projects : API - Core
Use the real `reference-dut-fw` repo — not a placeholder project — to validate the full build→flash chain for real, with EmbArch API run from WSL2 and EmbArch Core run on Windows: the suite's actual daily-use topology. The shipped foundation's Flash milestone proved this chain against a generic test board; Onboarding validated `init`/`list_targets`/`doctor` against reference-dut's real config but never drove an actual physical flash through it. This milestone closes that gap: check out a real reference-dut board/variant, `embarch init`, `list_targets`, `build_and_flash` through the WSL2⟷Windows split, onto physical hardware.
Steps : **done, 2026-08-18** — [embarch-api/milestone-7.md](embarch-api/milestone-7.md) / [embarch-core/milestone-7.md](embarch-core/milestone-7.md), both docs' Definition of Done fully closed. Target board/variant/revision was deliberately not pre-selected in either doc — resolved via live `list_targets` discovery against the real repo (`dut_dev@7`/`reference-dut`/`ble-shell`), per decision 12.

### 2 - Dev Bench Self-Test

Projects : Core - Dev Bench - API *(expanded 2026-08-18 from the original Core+Dev-Bench-only scoping — see below)*
No client/DUT repo involved. Establish the dev-bench self-test as a standing precondition every study runs before touching a DUT: dev-bench firmware exercises its own peripherals and the Core⟷dev-bench transport — serial link, `Hello`/`HelloAck` handshake, real per-`Study` dispatch of a multi-step `Study` with `StepResult`/`StudyDone` round-tripping (`embarch-dev-bench/design.md` §3 decision 21) — end-to-end on physical hardware for the first time. Confirms dev-bench is ready for a task before Milestone 3 points it at a real DUT. Power sampling is explicitly out of scope here (Milestone 4). **Target board retargeted 2026-08-19 from the nRF54L15DK to the ESP32-C5-WROOM-1 DK** (`embarch-decision-reversals.md` row 13) — the nRF54L15DK's RMA replacement never arrived and closed this milestone; the ESP32-C5, originally only an interim substitute, already validated the entire dispatch/handshake/study mechanism end to end, so this milestone's own scope moved to it rather than staying blocked on the nRF54L15DK.
Steps : **done, 2026-08-20** — [embarch-core/milestone-8.md](embarch-core/milestone-8.md) / [embarch-dev-bench/milestone-8.md](embarch-dev-bench/milestone-8.md) / [embarch-api/milestone-8.md](embarch-api/milestone-8.md), all three docs' Definition of Done closed except one item: the host-side watchdog's lapse-produces-`"failed"` behavior stayed code-reviewed/unit-tested only, not live-confirmed — two live attempts (a human-timed physical USB unplug, and a software JTAG-reset race) both lost the same timing race against the self-test fixture's `BleAdvertise` step resolving in well under a second, faster than either could land inside the window. Everything else closed for real against the ESP32-C5 in this final pass: the stale-firmware `doctor` check (surfacing and fixing a real CMake staleness bug along the way), Core-crash-mid-study, dev-bench port auto-detection extended to cover this board's own VID directly, and the MCP-path repeat (surfacing and fixing a real MCP-specific schema bug the CLI path can't hit, plus confirming a previously-flagged stack-overflow risk is debug-build-specific). Scope decisions made in the original drafting pass, ahead of execution: Core implements the full four-endpoint `/study*` surface now rather than deferring `power-data`/`waveform-data` to Milestone 4; dev-bench closes `design.md` decision 8 (Rust cross-compile/west-module wiring) in this milestone rather than leaving `study_ffi.c` stubbed; the self-test `Study` is `BleAdvertise`-only steps (no DUT, no power hardware needed); and `embarch-api`'s already-locked-in `run_study`/`study_status` surface (`embarch-study-designer/design.md` §6) was pulled into this milestone's scope as the way the self-test actually gets submitted, rather than a raw HTTP call — the reason this milestone's Projects line grew a third repo.

### 3 - Study Designer: Feature-Branch Iteration

Projects : Study Designer - Core - API - Dev Bench
Use the full EmbArch chain to iterate a test of a feature branch on the real reference-dut repo: build and flash a feature branch via EmbArch, dev-bench BLE-connects to the running reference-dut DUT, exchanges GATT data per the branch's feature, and forwards the acquisition data over the Core⟷dev-bench serial link into `embarch-core`. One successful end-to-end run is the Definition of Done — not a repeated edit→rebuild→reflash loop.
**Design-questions pass, 2026-08-19:** the GATT interaction is generic, not tied to one known characteristic — new `embarch-study-designer` types `Action::GattDiscover` (live wildcard GATT-table discovery) and `Action::GattMonitorAll` (self-sufficient discover+subscribe-all+capture-everything) replace an original plan to target one specific characteristic (a reference-dut dock-bounce debounce fix, `fix/bouncing_dock`, whose actual fix is explicitly out of scope for this milestone — it has no commits past `main` as of this pass). A second new piece, answering "autodetect a DUT's GATT config" from source rather than live: a generic `GattConfigExtractor` trait plus one concrete implementation scoped to this one firmware's real macro conventions. Full detail: `embarch-study-designer/design.md` §3 decisions 31/32/33.
Steps : **done, 2026-08-24** — [embarch-study-designer/milestone-9.md](embarch-study-designer/milestone-9.md) / [embarch-core/milestone-9.md](embarch-core/milestone-9.md) / [embarch-api/milestone-9.md](embarch-api/milestone-9.md) / [embarch-dev-bench/milestone-9.md](embarch-dev-bench/milestone-9.md), all four docs' Definition of Done fully closed. Target board: the ESP32-C5-WROOM-1 DK (already validated in Milestone 2), not the still-unreplaced nRF54L15DK. A real `BleConnect`->`GattDiscover`->`GattMonitorAll` study against the real reference-dut DUT (`bugfix/gpio-debounce-ppg-work-queue`) completed clean via both CLI and MCP — the first physical BLE connection between two independent boards this suite has ever made, and the first real GATT discovery/monitoring against real hardware. Getting there needed a real production fix along the way: a debug-build stack-overflow risk `embarch-study-designer/design.md` §7 had only ever tracked against a tiny fixture crashed `embarch-api`'s `study_status` for real against this milestone's GATT-sized result, fixed by spawning its runtime on a dedicated big-stack thread (`embarch-api/design.md` decision 36) — which incidentally also fixed a separate, previously-unrelated-looking `--release`-build crash. Closed with one real gap left open rather than blocking: a ~50% flake rate connecting to and discovering the DUT's GATT table (one outright connection failure, one mid-discovery disconnect, inconsistent service counts across successful runs), not yet root-caused (`embarch-dev-bench/milestone-9.md` §6).

### 4 - Power-Sampling Study

Projects : Dev Bench - Study Designer
Acquire and wire in the provisional PPK2 power-measurement front end (`embarch-dev-bench/design.md` §3 decision 24) and exercise `PowerSampleWindow`/`StreamChunkBatch` end-to-end for the first time — the one dev-bench feature area Milestones 2 and 3 explicitly defer.
Steps : **deferred to the Later bucket, 2026-08-25** — the user's call in that day's suite-wide design pass, put as a direct question and answered "let's keep power profiling for the future." No front end gets ordered; decision 24's PPK2 pick stands as the pick if and when this resumes, rather than being withdrawn. The milestone keeps its number and its scope so the sequencing history stays readable — it is deferred, not cancelled or renumbered.

**This deferral settles four open questions elsewhere in one stroke, which is most of why it was worth asking rather than letting the milestone drift.** Each was downstream of hardware that is now not arriving: the physical dev-bench design and its DUT connector (`embarch-dev-bench/design.md` §4), `Sample.value`'s per-instant-vs-multi-lead grain and `data.csv`'s power columns, and the `SignalCheck` variant set plus its naive O(n²) DFT (`embarch-study-designer/design.md` §7). All four are now deferrals with one shared trigger — power profiling resuming — instead of four separate questions waiting on an answer nobody could give.

### 5 - Study Designer UI

Projects : Study Designer
Opened by a real gap Milestone 3's own closing session hit directly: a real `GattMonitorAll` run against the reference-dut DUT came back empty, because nothing in that `Study` ever wrote anything to make the DUT stream, and there was no way for whoever authored it to know what to write — that's DUT-specific knowledge no generic discovery can produce, and an attempt to answer it by reading the DUT firmware's own source and asserting a conclusion from it was flagged directly as destructive to the dev process — an inference from source isn't the same as knowing. This milestone builds the actual fix: an interactive, table-based `Study` builder (own local web UI, shipping inside `embarch-study-designer`) whose action list merges built-in `Action`s, live `GattDiscover`, and static `GattConfigExtractor` output, plus a user-authored custom-action registry — name and clickable enumerated parameter values only, never a semantic description this crate invents on its own. Full design: `embarch-study-designer/design.md` §3 decisions 34/35.
Steps : **done, 2026-08-24, except real-hardware validation** — [embarch-study-designer/milestone-11.md](embarch-study-designer/milestone-11.md). The UI (interactive table builder, live+static discovery, the custom-action registry, run-and-watch) is built and live-smoke-tested against the real `reference-dut-fw` repo; closing the loop for real — a real engineer-supplied action producing real captured GATT activity — needs a connected DUT/dev-bench and someone who actually knows that DUT's protocol, deliberately not invented by this milestone.

### 6 - Stimulate-and-Capture (NUS shell + exhaustive GATT logging)

Projects : Study Designer - Dev Bench - Core - API - UI
Opened 2026-08-25 by a concrete user goal — "send an NUS shell message to the DUT mid-study, stimulate it over BLE, and get exhaustive logs of everything that happened on the GATTs, from a study I can save and re-run, authored in the UI" — and by the gap that goal immediately exposed: **it wasn't expressible.** Steps run strictly in sequence and `GattMonitorAll` unsubscribes when its own step ends, so a stimulus write and a capture could never overlap; every ordering produced an empty capture and a `Pass`. Five pieces, all built: `Action::GattMonitorStart`/`GattMonitorStop` (a capture window that outlives its step), a streamed uncapped GATT transcript landing in `gatt.csv` and served by `GET /study/{id}/gatt-data` / `study_gatt_data`, a free-text payload path alongside the registry, a saved-study library under the firmware repo that doubles as a `run-study --study-file` input, and the Study Designer tab's frontend (which, it turned out, had never actually been built — only its backend). Full design: [embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decisions 36/37/38, [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 28.
Second pass, 2026-08-25, once hardware was connected: three more pieces, two of them fixes to defects that made the original five unusable on real hardware. `vendor.rs`, a built-in table of vendor-defined GATT service identities so Nordic's UART Service UUIDs aren't every engineer's to transcribe; `Step.delay_before_ms`, making *when* a stimulus fires authorable rather than implicit; and `Action::BleConnect.target_name`, because "connect to whichever DUT shows up first" reached a different peripheral on consecutive runs of the same study. Design: [embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decisions 41/42/43, [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decisions 30/31/32. Schema 5 → 7.
Steps : **the pipeline is built, deployed and exercised on real hardware; the end-to-end NUS run is not yet done.** Both boards are flashed and enrolled (DUT `dut_dev@7` + `-S ble-shell`, dev-bench ESP32-C5), Core is rebuilt and live on the Windows service, and the `Hello`/`HelloAck` handshake reports `schema_version: 7, compatible: true`. A study authored in the Study Designer UI, saved to the firmware repo, and re-run from the CLI now reaches the DUT's GATT table for real, and `gatt.csv` returns an exhaustive both-directions transcript. Tests: study-designer 80, Core 56, api 39, dev-bench ztest 24/24.

  **Running it is what found the real bugs**, none of which any test suite would have caught: (1) dev-bench's inbound link was *polled*, so every frame larger than the UART's 128-byte hardware FIFO was silently truncated and dropped — and this milestone's own four-step study, at 132 bytes, is the first one this suite ever authored large enough to cross that line, which is why the feature could never have worked; (2) 16-bit UUIDs were reported two bytes out of place, so a study authored against any 16-bit UUID could never match its own discovery output; (3) `BleConnect` with no target connected to arbitrary nearby peripherals and then reported `"service not found on DUT"` about devices that were never under test. All three fixed and verified against hardware. The ztest suite now also decodes the *real* bytes Core sends, not only what its own encoder produces — the blind spot that let (1) survive.

  **Still open, and reframed 2026-08-25 — the previous version of this paragraph was wrong about why.** It read that the DUT "advertises slowly (minutes, not seconds) and under a name that is *not* its configured `CONFIG_BT_DEVICE_NAME`", and treated that as the obstacle. The board was **bricked by EmbArch's own `--erase` flash** ([embarch-core/design.md](embarch-core/design.md) §3 decision 32, [embarch-decision-reversals.md](embarch-decision-reversals.md) row 19); the scan diagnostics built to explain its absence were explaining damage the suite had done. Once it was recovered with `west flash --erase` and advertising again as `the client S11 B9C3`, the real blocker showed itself: `connect` passes and `open-capture` then fails with `"disconnected during service discovery"`, deterministically, 4 of 4 — while the same discovery walks an Apple device, a `0x1910` device and two `7dfc*` devices without trouble. **The DUT requires an MITM-authenticated `BT_SECURITY_L4` link before it will tolerate discovery** (asked and answered by the engineer, not inferred), and dev-bench registers no pairing callbacks at all, which makes L4 unreachable from this bench no matter what a study asks for. Four design decisions came out of that: [embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decisions 44 (`Action::BleSecurity`) and 45 (`Study.gatt`), [embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 34 (central-side pairing), and [embarch-ui/design.md](embarch-ui/design.md) §3 decision 12. **None of them are code yet, and the attribution of the disconnect to the pairing gap is itself unvalidated** — the hardware was detached before it could be tested. The DUT also advertises a rotating resolvable private address (two addresses inside one 60-second scan), so `target_address` is not a usable handle for it and decision 43's name filter is the one that works. The shell command and terminator to send remain engineer-supplied and unconfirmed — deliberately, per decision 41's identity-only rule; the author will write them in the Study Designer UI. Also noted for follow-up, deliberately *not* concluded: one run whose `Read` step reported `Pass` yielded a `read_request` row in `gatt.csv` with no matching `read_response` row and a null `captured_data`. Both the schema variant (`GattEventKind::ReadResponse`) and the firmware emit call exist, so this is an unexplained observation needing diagnosis, not a missing feature — recorded here so it isn't rediscovered from scratch.

### 7 - MCU Load Tracing (embarch-outpost)

Projects : Outpost - Topology - Core - Study Designer - Dev Bench - API - UI
Opened 2026-08-25 by a goal nothing in this suite can currently serve: **"a study told me the DUT responded in 40 ms; I need to know whether that was one busy thread, forty context switches, or an ISR storm."** Every existing component observes the DUT from outside it, and none of them can see inside the MCU. This milestone builds the one that ships *inside*: a Zephyr module ([embarch-outpost/design.md](embarch-outpost/design.md)) compiled into the DUT's own debug firmware, implementing `CONFIG_TRACING_USER`'s kernel hooks plus engineer-placed markers, emitting compact ID-based records out a **TX-only** UART into a lock-free ring drained asynchronously, decoded host-side against a CRC-matched build-time manifest.

Three pieces land alongside it, none of them optional: **"dev-bench bypass"** becomes a real modelled route rather than a description of how things happen to be wired ([embarch-topology/design.md](embarch-topology/design.md) §3 decision 18) — the bench has neither the spare pins nor the pass-through firmware, so the outpost's UART goes to a USB-UART bridge on the Core machine, declared as `Route::Direct` and drawn in the diagram as a signal going *around* the bench; the **stream-pipeline proposal's inbound half is accepted** ([embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decision 39), which is what keeps this from becoming the suite's fourth near-identical capture pipeline; and [embarch-ui](embarch-ui/design.md) gains route declaration in the Topology tab plus a post-hoc Trace view.

**Sequence this before Milestone 4 (Power-Sampling Study).** Same argument the proposal made for itself, now sharper: Milestone 4 is exactly what makes the current stream wire shape load-bearing on real hardware, and Milestone 6's dev-bench firmware has not been flashed yet either. Nothing about the shapes being reshaped here has ever carried a real byte. That stops being true the moment either runs.

A fourth piece joined the same design pass, wider than the outpost that prompted it: **a `Study` now declares the dev-bench and DUT firmware versions it is meant to run against, with reflash as the operator's per-run choice** ([embarch-study-designer/design.md](embarch-study-designer/design.md) §3 decision 40). It was raised to give the outpost's trace manifest something to bind to, and closed a gap nobody had named — a `StudyResult` could not say what firmware it ran against, making two runs of one study against two builds indistinguishable afterwards.

Capture is **study-scoped and rendered post-hoc — there is no live feed**, reversing this milestone's own opening framing, deliberately ([embarch-outpost/design.md](embarch-outpost/design.md) §3 decision 10).
Steps : **Phases A and B closed 2026-08-25; Phase C and Phase D both closed 2026-08-26 — closing C shipped the two host halves its own trigger had parked, and D landed across five repos with one item deliberately scoped out; E not started, and it needs hardware that does not exist** — [embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md), phased A–E across seven repos. Phase A is the shared types both other host phases build against: `Study.streams`/`StreamTap` and the `StreamOpen`/`StreamChunkBatch`/`StreamClose` wire triple, `Study.requires`/`StudyResult.provenance`, `embarch-topology`'s `SignalLink`, and `STUDY_DESIGNER_SCHEMA_VERSION` **7 → 8** (one bump for decisions 39 and 40 together; the 5 → 6 both were written against went stale when decisions 42/43 shipped first). Every new wire record is pinned in both languages. **Nothing has captured a real byte yet** — `streams/`, the version gate, reflash and now dev-bench's own tap handling are all built as of Phase B, but none of it has run against hardware and the dev-bench half has never been flashed; the UI and the outpost module itself are still ahead. First consumer: `reference-dut-fw` (nRF54L15, already enrolled as `dut`).

**The five questions Phase A left open were settled 2026-08-25 as decisions, and shipped the same day as Phase B item 0** ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §3 item 0): a sibling `streams_crc` rather than a widened `steps_crc`; `Step.power_sample` retired so a `PowerFrontEnd` tap is the only way to author a power capture; a stream-fed `ValidationSource` naming the tap rather than a `DataChannel`; `POST /signals` + `GET /signals` on Core with no `embarch-topology` CLI mirror; and the durable `alerts.jsonl` record for a `SignalMismatch` closed as not-needed-yet with a named trigger rather than carried as owed work. Settling them surfaced a sixth thing none of them predicted: `STUDY_DESIGNER_SCHEMA_VERSION` was guarding two hops with different exposure and **split into `DEV_BENCH_WIRE_SCHEMA_VERSION` and `HOST_TYPE_SCHEMA_VERSION`, both 9**. Four of the six are recorded as reversals (rows 21–24) — all found by reading the real repos rather than the docs' summaries of them.

**Phase B items 1 and 2 shipped 2026-08-25, and item 2 was not the api-only item this milestone scoped it as.** Item 1 is Core's whole half with no schema change: `streams/` replacing the three fixed CSV paths (raw bytes always written before any decode), both retention knobs, the parameterised stream route with the three old ones as aliases, `provenance`/`streams` in `events.json`, the `409` version gate that runs no step, and Core's own `Route::Direct` signal port. Item 2 is `study_stream_data`/`list_study_streams`, `run_study --reflash`/`--allow-version-mismatch`, and **the two signals decision 40 created without giving either a place to land** — an override that had to be recorded in a `StudyResult` with no field for one, and a `VersionSource::FlashedThisRun` nothing could produce. Both cross `POST /study` as **query parameters**, which is "a run parameter, not a study field" written as a request; `Provenance` gained `overrides` at host schema **9 → 10**, the first bump that moves that constant alone. Two things nobody had anticipated came out of it: supplying the flashed version is also what makes `requires.firmware_version` checkable, so the DUT half of Core's gate is live for the first time; and `embarch-core/design.md` §3 decision 32's `erase` turns out to describe a vendor-tool delegation **that is not in the tree** (reversal row 28). **Only the manifest half of Phase B remains deferred**, under one trigger shared by Core's receiving half and api's sending half: Phase C emitting a real `outpost-manifest.json`. **Item 3 closed 2026-08-25, and closed Phase B — with both of its scoped premises wrong.** Taps are decoded and stored, opened and closed from their declared `StreamScope`, and the GATT transcript rides `StreamChunkBatch` on its declared tap; the retired tag 10 is gone from the encoder and from `main.c`, its value left burned rather than reused, because the "no such firmware was ever flashed" argument that licensed reusing tags 2/3/4 expires the moment this firmware is. But **the payload-interpretation deletion this item was named for had already happened in Phase A** — grep found none of the four things the decision said were still there — so the item was purely additive and the phase ends net *bigger*, +128 bytes here on top of item 0's +654, against a net-negative expectation (reversal row 35). And **the reserved `dev-bench` log tap was Core's, not dev-bench's**: `tap_for` resolved ids against the submitted `Study`, so a tap the bench invented would have been dropped as undeclared; it ships as Core rendering the `LogLine` frames it already receives, with no firmware at all (row 36). **Two of Phase B's four items were not the single repo their scope named** — item 2 moved three, item 3 moved two — which is a lesson about one-word item scoping rather than two accidents.

**Implementing item 0 produced two more reversals (rows 25, 26), and one of them inverts a stated bar.** The pass was expected to make dev-bench firmware *smaller*; it made it **+654 bytes bigger**, because the C decoder never walked the `streams` span the new seal covers — Phase A had appended `streams` after `steps_crc` precisely so it would not have to. The decision stands; the walker is item 3's cost arriving early. The second: `StepResult` had been silently writing two bytes for fields retired a schema version earlier, invisible because the both-languages pinning rule had been scoped to *new* records and `StepResult` is the oldest one. The schema bump was re-derived rather than copied, taking **8 → 9** and pushing decisions 44/45's own reserved 8 → 9 back to be re-derived — row 18's protocol working as intended for once. **Items 1–3 (Core's `streams/`, the api surface, dev-bench's payload-interpretation deletion) are not started, and nothing is deployed.**

**Phase B item 1 — Core's whole half — closed 2026-08-25, and the two open questions it carried were settled rather than guessed at** ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §3 item 1). `streams/` replaced the three fixed CSV paths with one file per declared tap, raw bytes always written before any decode is attempted; both retention knobs landed (`EMBARCH_STREAM_MAX_BYTES`, `EMBARCH_STUDY_RESULTS_KEEP` — the first EmbArch settings a plain operator may need to turn, so both got [embarch-user-guide.md](embarch-user-guide.md) rows); `GET /study/{id}/stream/{name}` shipped with the three old routes as aliases for one release; `StudyResult.streams` and `provenance` reached `events.json`, which had never carried either; and the version gate went live, rejecting a stale bench with a `409` **before `StudyStart` is sent**, so no step runs. A `Route::Direct` signal tap now opens Core's own third serial port — `resolve_signal_port`'s first caller anywhere, which is what [embarch-topology](embarch-topology/design.md) §5 had recorded as unwired infrastructure. No schema change, and no source change in `embarch-api`/`embarch-ui`/`embarch-umbrella`, which is what the aliases exist for.

**Phase C closed 2026-08-26 — the outpost module exists, and closing it fired the manifest trigger** ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §4). The Zephyr module builds for `native_sim` and for the real nRF54L15 reference-dut target; a `native_sim` run produces a stream that decodes cleanly (41 frames, 848 records, every record kind including gaps, no CRC, framing or sequence failures); the post-link manifest generator resolves **13 real ISRs and 5 real threads out of the reference-dut's own ELF**. Because that is exactly the precondition Core's and api's manifest halves were parked on, **both shipped in the same pass** rather than becoming recorded debt — Core stores what a flash bound (per chip, cleared by a flash of the same chip carrying none) and decodes a trace against it post-hoc, `embarch-api` sends it by finding it *beside the artifact* rather than taking it as a parameter, and a build-ID mismatch costs the **names** and never the capture.

**Eight reversals came out of Phase C (rows 37–44), and five of them are the same shape**: this suite's docs asserting how a *build system or toolchain* behaves, where the mechanism named was the one that looked right rather than the one that works — `select` on a Kconfig choice member, `zephyr_iterable_section` on the template-`.ld` path, `_sw_isr_table` naming a shared trampoline instead of a handler, a Cargo feature that only ever built via unification from another dependency, a hand-mirrored constant guarded by a comment rather than a mechanism. **Row 37 is the one to carry forward and is a different lesson**: `embarch-study-designer` had *shipped* a `StreamEncoding::OutpostTrace { manifest_crc }` encoding a mechanism `embarch-outpost` decision 9 had **already reversed in prose**, and only producing a real manifest made that visible. A decision reversed in a doc does not reverse the code that shipped against its earlier form. It is now a unit variant at schema **v11 wire / v13 host**.

**What Phase C did not do, and must not be read as having done**: no outpost byte has crossed a real UART on any board; every Kconfig wire constant is an unmeasured default (a host process writing to a pty sustains a rate no UART will, so the `native_sim` run proves correctness and nothing about cost); and the instrumentation's own overhead is deliberately uncharacterised, which was the user's call on 2026-08-25 — the milestone doc's own definition of done still listed "overhead measured" and that contradiction is struck rather than quietly satisfied.

**Phase D closed 2026-08-26 — routing is declarable by a human and a trace renders, across five repos rather than the one its scope named** ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §5). `embarch-ui` gains a sixth tab: signal-route rows in Topology with the diagram drawing a `direct` signal *around* the dev-bench node, and a post-hoc Trace view with lanes per thread/ISR/CPU-idle. The four things its one-word scope missed were all upstream of the UI — `embarch-core-client` had no `/signals` wrapper at all, and Core needed `GET /serial-ports` (which needed `embarch-topology` too), `GET /study/{id}/streams`, and `DELETE /signals/{name}`. The third is the one that matters: Phase C made Core record *why* a trace has no names and put it in a file with no HTTP caller, so over HTTP a refused trace and a named one were indistinguishable — exactly the confusion that refusal exists to prevent.

**Two things Phase D deliberately did not do.** Decision 11's reflash selector is `embarch-api` orchestration (`check → build → flash → submit`, over project config and build machinery `embarch-ui` does not have), so it is scoped out with the dependency-direction question recorded rather than answered under time pressure. And **nothing was deployed**: validation ran against a dev Core built from `main` inside a private mount namespace with a scratch `/var/lib`, which is also how Phase D discovered that the live Windows service predates all of Phase B and answers `404` on `/signals` — the plan's own "exercise this against the live Core" step was impossible (rows 45–48).

**What Phase D must not be read as having done**: no outpost byte has crossed a real UART, so the Trace view has rendered the committed `native_sim` capture and never a DUT's, and every wire constant in §5.3 is still an unmeasured default. A working timeline is not validation of the numbers underneath it.

**Record layout 2, 2026-08-26 — the DUT stopped keeping time, and it is a scope change to this milestone rather than a detail** ([embarch-outpost/design.md](embarch-outpost/design.md) §3 decisions 4, 17, 18). At the repo owner's call, on emit-path cost: the per-record `k_cycle_get_32()` read sat inside the context switch and inside `_isr_wrapper()`, so the instrument was charging its cost to the path it measures. Every timestamp comes off the wire; `embarch-core` stamps each *frame* with its own receipt time instead, persisted beside the capture and joined on at render time with a verified alignment. Five repos touched, all green.

What this milestone can now claim is narrower and worth stating in the same breath as the win. **It buys:** ~32% fewer bytes per record (9.25 → 6.32 measured on the same workload), a ring that is finally the size its Kconfig says, and — the part nobody had written down as a gap — a trace on the *same clock* as every other stream in a study, where DUT cycles had no sync point to host wall time at all. **It costs:** a frame is now the resolution of the instrument, so **ISR duration is no longer measurable** — this wire counts interrupt entries and refuses to time them — and per-thread load excludes a fourth class of span for the same reason. Milestone 7's opening goal ("was it one busy thread, forty context switches, or an ISR storm?") is still answered; "how long did that one ISR take" is not, and is now out of scope for this instrument rather than pending a measurement.

**Still true, and now the headline measurement this milestone owes:** no outpost byte has crossed a real UART, so nobody knows how many milliseconds a frame actually takes to arrive or how much that jitters — and that number is the resolution of everything the trace says.

**The manifest half was settled and deliberately not built** — until Phase C fired its trigger a day later; the record of *why* it waited is kept because the trigger mechanism is the part worth preserving: its transport is an optional `manifest` part on `POST /flash` (not a route of its own, so the flash that binds a manifest is the request that carries it), and building storage/selection/verification/refuse-to-render now would be four mechanisms with nothing able to exercise any of them until Phase C emits a real manifest — which is the named trigger. That is the third time this milestone has closed something with a trigger instead of leaving it as owed work. **One more reversal (row 27) came out of building it**: decision 30(a) said a signal tap's port opens "at `StreamStart`", a trigger that cannot exist for the one stream source dev-bench does not mediate — the decision had borrowed a lifetime signal from precisely the sources it was defining itself against.

## Next

Not yet numbered milestones — the buckets [embarch.md](embarch.md) and [embarch-features.md](embarch-features.md) refer to as "Next".

- **`embarch-promptu`** — the curated library of firmware-specific skills, subagents, and prompt patterns. Planned, no repo ([embarch-promptu/design.md](embarch-promptu/design.md)).
- **`embarch-ui`** — one consolidated human-facing UI, having replaced `embarch-topology`'s `ui` subcommand, `embarch-study-designer`'s `study-designer-ui` binary, and `embarch-core`'s `/enroll` page outright (all three deleted, 2026-08-24). `milestone-1.md` **CLOSED**: repo created, its first five tabs (Dashboard/Topology/Study Designer/Enroll/Debug) live-validated against the real deployed Core, VS Code launcher extension built/packaged/installed for real and user-confirmed working (internal `.vsix` distribution, decided) ([embarch-ui/design.md](embarch-ui/design.md), [embarch-ui/milestone-1.md](embarch-ui/milestone-1.md)). **Pushed, and a sixth tab added by Milestone 7 Phase D (2026-08-26)**: signal-route declaration in Topology and a post-hoc **Trace** view. Supersedes this file's old "EmbArch UI" placeholder entry, previously in the Later bucket below.

## Later

- **Core on a Raspberry Pi, reachable over the LAN.** Detected and verified as of Milestone 6, but flashing from a separate machine stays blocked on the artifact-transfer gap (`embarch-api/design.md` §9), and cross-machine token distribution stays manual (`embarch-token.md` §8). A deliberately late milestone.
- **macOS validation.** Milestone 6 ships macOS support reasoned-only, with no machine to test on; validating it means a Mac-only engineer walking the user guide.
- **Power profiling (Milestone 4).** Deferred out of the near sequence 2026-08-25 by explicit decision; the milestone above keeps its number and scope. Resuming it is what unblocks four open questions across `embarch-dev-bench` and `embarch-study-designer`.
- **A real `.eap` protocol run on real hardware.** The mechanism shipped 2026-08-26 (`embarch-study-designer/design.md` §3 decisions 58–62) and closes the *authoring* half of what [embarch-study-designer/milestone-11.md](embarch-study-designer/milestone-11.md) §3.8 has been blocked on since 2026-08-24 — an engineer can now state a DUT's protocol instead of the tool guessing it. **The first of the two things standing between that and a run is done** — `embarch-dev-bench` grew the real-time C interpreter on 2026-08-26 (`embarch-dev-bench/design.md` §3 decision 41, wire schema v15), pinned against this crate's reference one by a literal `StudyStart` frame carrying the worked BDS protocol; Core gained the third pre-flight seal alongside it (`embarch-core/design.md` §3 decision 39). **What is left is the one §3.8 always named**: a real engineer authoring a real manifest for a real DUT. Nothing has executed a `.eap` file against a radio yet, so expect the first real run to find something in `execute_run_protocol`'s BLE half — discovery, three concurrent subscriptions, and the notification queue under a real chunk burst are the parts no host test reaches. Also owed before that run: a Core redeploy and a dev-bench reflash **in the same sitting**, since the wire moved (`embarch-dev-workflow.md` §4a).
- **`embarch-atlas`** — static analysis and graph visualization of a firmware codebase, for agents and engineers. Paused, no repo (see [embarch.md](embarch.md) §2).

## Release

*Updated 2026-08-15, closing item 64 of that day's design-improvement review (`.claude/design-improvements-2026-08-15.md`, local working notes): this section had gone stale the moment real releases shipped, still naming a placeholder ("Rochambeau, Date: ?") with no mention of the real `v0.1.0`s or the assembled suite archive.*

### v0.1.0 — shipped 2026-08-11

Real, per-repo `v0.1.0` tags and GitHub Releases exist for every component with code (`embarch-core`, `embarch-api`, `embarch-umbrella`), plus a real assembled `suite-v0.1.0` archive (`embarch-umbrella`'s `assemble-suite.yml`) that `doctor` check 1 has validated a manifest against. Scope in practice: everything through Milestone 6's release-engineering step, which turned out to include Milestones 1, 2, and 6 together — not the Milestone-1-and-2-only scope "Rochambeau" (below) originally named. Full detail: [embarch-umbrella/design.md](embarch-umbrella/design.md) §3 decision 14, [milestone-6.md](embarch-umbrella/milestone-6.md) §3.7.

### Rochambeau — superseded, kept for history

The original working name for "the first release, Milestone 1 + 2 only." Never actually cut under that scope — release engineering itself turned out to be part of Milestone 6's work, and what shipped (above) covers substantially more than Milestones 1+2 alone. Recorded here as superseded rather than silently deleted, so the name doesn't get reused for a future release by someone who didn't know it was already spent on a plan that didn't survive contact with what actually got built.

## Changelog

- 2026-08-31 — **The dev bench is an nRF54L15DK again**, reversing the 2026-08-19 retarget below ([embarch-decision-reversals.md](embarch-decision-reversals.md) row 13's counter-reversal). Not a milestone item — the repo owner acquired a replacement DK and plugged it in. Recorded here because every milestone entry from 2 onward names the ESP32-C5 as *the* bench, and those entries stay as written: they are accurate about what ran when. What is true now is that `workspaces/nordic` is the live bench, `workspaces/espressif` is a working alternative kept in the tree, and neither is retired. Keeping the nordic workspace through the ESP32-C5's five-week tenure is what made the swap cost one overlay property instead of a bring-up; the real work was three host-side assumptions that had never met a second board ([embarch-dev-bench/design.md](embarch-dev-bench/design.md) §3 decision 43, [embarch-api/design.md](embarch-api/design.md) §3 decision 45, [embarch-topology/design.md](embarch-topology/design.md) §3 decision 20).

- 2026-08-26 — **A study can ask for BLE security, and the DUT's GATT table has been walked for the first time.** Not a milestone item: the repo owner tried to author his first real study and could not express the thing the study exists to do. `embarch-study-designer` §3 decision 44 — **written 2026-08-25 and never implemented** — plus a new decision 50 for `Action::BleUnbond`, at wire schema **v12 / host v14**; `embarch-dev-bench` decision 34's pairing callbacks implemented, with a new decision 37 for the three things hardware changed; `embarch-ui` decision 14 for the Study Designer's runtime project picker and "Create study". Validated against the real DUT across three identical runs: L4 reached and reported per step, and `GattDiscover` returning **7 services** where every prior attempt had returned a deterministic `"disconnected during service discovery"` — which closes [embarch-decision-reversals.md](embarch-decision-reversals.md) row 20's own explicitly-flagged unvalidated attribution.

  Three findings the scope did not name, all recorded rather than absorbed. The work was scoped as new design and **decisions 44 and 34 had already specified it in full** — same variant name, same discriminant, same semantics, including an `L1` value the fresh implementation had refused as nonsensical; caught only by reading the section the change landed in. `bt_conn_set_security` returning `-EBUSY` was read as a failure, which blamed the DUT for a link that was one second from being secured (row 56). And "Open project"'s first acceptance rule counted a bare `embarch/` subdirectory, which accepted `$HOME`, because this bench's own suite parent folder is `~/embarch` (row 57).

  Also shipped, on the owner's prompt mid-session: **`embarch deploy-core`** (`embarch-umbrella` §3 decision 32), which turns [embarch-dev-workflow.md](embarch-dev-workflow.md) §4a's five-step manual deploy into one command *and checks that it landed* — the supported `update` path exits `0` having done nothing when its UAC prompt never renders. Row 58 is the process half: §4a was written because the step kept being rediscovered, and it kept being re-typed by hand for two more weeks anyway.

- 2026-08-27 — **Core stops flashing Nordic RRAM parts with probe-rs, suite-wide** ([embarch-core/design.md](embarch-core/design.md) §3 decision 36). Not a milestone item — a defect found while trying to finish Milestone 7 Phase E, and the reason every Phase E flash "succeeded" while nothing ever ran. Core had exactly one flashing implementation and used it in preference to the runner every Zephyr board declares for itself; on the nRF54L15, whose code lives in RRAM rather than NVMC flash, probe-rs models the part as one flat NVM region with no RRAM-aware erase or write. probe-rs is now **refused** for that family and the board's own tool runs instead, detected rather than bundled (licensing), with an error naming all three tools and their install URLs when none is present. It also finally implements the delegation decision 32 chose on 2026-08-25 and never shipped — recorded as shipped for two days while the tree held the rejected option ([embarch-decision-reversals.md](embarch-decision-reversals.md) rows 54, 55). Unvalidated on hardware: the DUT is unresponsive.

- 2026-08-26 — **Milestone 7 Phase E attempted on real hardware; the milestone does not close, and the reason it does not is *not* the one first recorded** (see the 2026-08-27 entry above, which withdraws it). Every software half is done: the live Windows Core redeployed from `main` (further behind than recorded — its `Step` still carried `power_sample`, so it computed a different `steps_crc` for identical steps), dev-bench reflashed to schema v11 and handshaking `compatible: true`, the **load repartition** built into `embarch-ui`'s Trace view, the **outpost snippet** written for the real reference-dut, the module pinned in that repo's `west.yml` at a full SHA, and the tracing image **built through `embarch-api` and flashed onto the real DUT**, where it still advertises, connects and serves GATT with the tracer compiled in.

  **What blocks it was first recorded as physical, and that is withdrawn** — **the DUT's trace UART reaches the host over no serial port** — is WITHDRAWN 2026-08-27. Its load-bearing half was "the flashed console build was *proved running*, because dev-bench connected to it over BLE and read its GATT table". That connect went to a different peripheral: the saved study used `target_address: null` with no `target_name`, and a later name-pinned run reported **no `the client S11` on air at all** — only unrelated pods, one of which owns the `0x1800`/`0x1801`/`0x1910` table every discovery kept returning. So nothing established the DUT was executing during the port scan, and a silent port proves nothing about a wire when the board may not be running. The repo owner reports the board needs reflashing and does not look alive. The honest state is that the wire is untested: the settling test is the tracer's own 1 Hz header frame on the DBG port at 230400, which needs the outpost image flashed and the board alive.

  **Closed en route, from a different milestone:** decision 33's watchdog test ran against the pre-fix Core and then the fixed one — the same one-step study failing at ~2s and passing at 22.8s — closing [embarch-core/design.md](embarch-core/design.md) §10's host-side-watchdog item, open since 2026-08-20 after two live attempts lost the same race. It was only runnable *before* the redeploy, because the fix removes the failure.

  Five reversals ([embarch-decision-reversals.md](embarch-decision-reversals.md) rows 49–53), four of them the same shape: a plausible mechanism asserted in a plan or a doc that a bench disproved in minutes. The fifth is the instructive one — nobody claimed threads and idle were mutually exclusive, it is simply what anyone would assume, and it was caught only because the assumption was written down as an assertion and run.

- 2026-08-26 — **Milestone 7 Phase D closed: routing declarable in the UI, and a trace that renders with honest gaps.** Five repos, not the one the phase's scope named — `embarch-topology` (an unnarrowed port enumeration, because a wire's carrier cannot be VID-gated the way a device's link is), `embarch-core` (three routes, one of which closes a blind spot Phase C created by construction), `embarch-api` (the `/signals` client wrappers that had never existed), `embarch-ui`, and `embarch-doc`. Two findings from the real `native_sim` capture changed the design rather than just confirming it: a gap band is **not** an empty interval (16 surviving records fall inside the first one), and gaps must be placed by their own timestamp rather than by position. Reflash in the run dialog is deliberately not built and is recorded as an open architecture question. Nothing deployed, and the Trace view has still never seen a DUT.

- 2026-08-26 — **Milestone 7 Phase C closed: `embarch-outpost` has code, and closing it shipped the two manifest halves its own trigger had parked.** The module builds for `native_sim` and for the real nRF54L15 reference-dut target; a `native_sim` run decodes cleanly; the generator resolves 13 real ISRs and 5 real threads out of a real ELF — including the handler behind Nordic's shared `nrfx_isr` trampoline, which the design never anticipated and which defeats 8 of those 13 on its own. Core and `embarch-api` shipped their halves in the same pass, at schema **v11 wire / v13 host** after `StreamEncoding::OutpostTrace` lost a `manifest_crc` the firmware cannot produce. **Eight reversals (rows 37–44)**, five of them toolchain assumptions that looked right and were not. `embarch-study-designer` 77 tests, `embarch-core` 116 on Linux and native Windows, 34/34 dev-bench ztests with `native_sim` and `esp32c5` both building, every other repo green. **Nothing deployed — the live Core is three schema steps behind with a wire bump in the gap, so a Core redeploy and a dev-bench reflash still have to happen in the same sitting** ([embarch-dev-workflow.md](embarch-dev-workflow.md) §4a). Phases D (the UI trace view) and E (the wiring, and the first real byte) are what remain.

- 2026-08-25 — **Post-hoc validation removed suite-wide** (`embarch-study-designer/design.md` §3 decision 48), so the Shipped-foundation Study Designer entry no longer claims a `SignalCheck` evaluator among what it delivered. It was built and never called: Core wrote a hardcoded `"validations":[]` into every `events.json`. Directly downstream of this file's own same-day deferral of power profiling — the hardware that would have produced data worth checking is not coming soon, and the placeholder check set was never going to be validated against anything else.

- 2026-08-25 — **Milestone 4 (Power-Sampling Study) deferred to the Later bucket** by explicit user decision in the suite-wide design pass. It keeps its number and scope rather than being renumbered or cancelled, so the sequencing history stays readable. The deferral closes four downstream open questions across `embarch-dev-bench` and `embarch-study-designer` by giving them one shared trigger instead of four separate unanswerable asks.

- 2026-08-25 — **Milestone 6's "Still open" rewritten; it had been wrong about its own blocker.** The DUT that "advertised slowly and under an unexpected name" had been bricked by EmbArch's own `--erase`, and a session of BLE scan diagnostics went into explaining damage the suite caused. Recovered, the real blocker is a DUT that requires an MITM-authenticated L4 link before it permits service discovery against a bench that registers no pairing callbacks and therefore cannot reach L4 at all. Four design decisions written in response (study-designer 44/45, dev-bench 34, ui 12), plus core 32 and api 41 for the erase capability itself; none implemented. Two reversal rows (19, 20). The milestone stays open.
- 2026-08-25 — **Milestone 7 Phase B item 3 implemented, closing Phase B.** dev-bench decodes and stores its declared taps, opens and closes them from their `StreamScope` as steps advance, and sends the GATT transcript as `StreamChunkBatch` records on the tap that declared it — retiring tag 10 from the wire for real, with its discriminant left burned. 34/34 ztests on `native_sim`, `native_sim` and `esp32c5` both building, `embarch-core` 106 tests on Linux and native Windows, `embarch-study-designer` 75, every other repo green and unchanged. **Both of the item's premises turned out to be wrong**, and both are recorded rather than absorbed: the payload interpretation it was scoped to delete was already deleted in Phase A (so the phase ends net bigger, not smaller — reversal row 35), and the reserved `dev-bench` log tap needed Core rather than dev-bench (row 36). Not deployed. **The live Core is now three schema steps behind `main`, and a `DEV_BENCH_WIRE_SCHEMA_VERSION` bump is in that gap — so the Core redeploy and a dev-bench reflash have to happen in the same sitting** ([embarch-dev-workflow.md](embarch-dev-workflow.md) §4a).

- 2026-08-25 — **Milestone 7 Phase B item 2 implemented — across five repos, not the one the milestone scoped.** `embarch-api` got what item 2 was written for: `study_stream_data`/`list_study_streams` with the three old tools and subcommands kept unchanged as aliases, and `run_study --reflash none|dev-bench|dut|both` / `--allow-version-mismatch`, sequenced check → build → flash → submit, never running `git checkout` anywhere. But decision 40's override and `VersionSource::FlashedThisRun` had **nowhere to land** — so `embarch-study-designer` gained `Provenance.overrides` at host schema **9 → 10** (`DEV_BENCH_WIRE_SCHEMA_VERSION` stays 9, the first time the split actually separates the numbers) and `embarch-core` gained two query parameters on `POST /study`. Two unanticipated results: the DUT half of Core's version gate became live, since supplying a flashed version is what makes `requires.firmware_version` checkable at all; and the two reflash halves sequence *differently* — the bench is flashed then read back, the DUT is verified then flashed — which is decision 40's verification asymmetry appearing as control flow, and means a wrong tree fails without touching the board. Reversal row 28 came out of reading unrelated code on the way past: Core's `erase` documents a vendor-tool delegation the tree does not contain. Green across all seven code repos, Core on Linux and native Windows; **not deployed, and the live Core is now two schema steps behind — Core still deploys before api.**

- 2026-08-25 — **Milestone 7 Phase B item 1 implemented** — Core's whole half of decisions 30/31: `streams/` replacing the three fixed CSV paths (raw-before-decode), both retention knobs, `GET /study/{id}/stream/{name}` with the three old routes as aliases, `provenance`/`streams` in `events.json`, the `409` version gate that runs no step, and Core's own `Route::Direct` signal port. No schema change and no source change in any other consumer. The manifest half is settled-and-not-built on a named trigger (Phase C emitting a real manifest); reversal row 27 came out of it. Green on Linux and in a native Windows build; not deployed.

- 2026-08-25 — **Milestone 7 Phase B item 0 implemented** — the five settled leftovers plus the schema split, at schema **v9** across `embarch-study-designer`, `embarch-core`, `embarch-api`, `embarch-ui`, and `embarch-dev-bench` (`embarch-topology` needed no change). Two new reversals came out of building it (rows 25, 26): the dev-bench firmware got **bigger, not smaller**, because the C decoder never walked the span the new seal covers, and `StepResult` had been writing two retired bytes for a whole schema version because the both-languages pinning rule was scoped to *new* records. A third finding is not a reversal but is worth the same attention: decision 12's `/status` schema handshake had been documented since 2026-07-28 and never built on either side, so this pass built it — and `embarch-api` now refuses to submit to a Core serving no version, which the live one is. **Core deploys before api.** Items 1–3 not started; nothing deployed.

- 2026-08-25 — **Milestone 7's five Phase-A leftovers settled as decisions** (implementation deferred to Phase B): `streams_crc` as a second seal, `Step.power_sample` retired, a tap-named `ValidationSource`, Core-only `POST`/`GET /signals` with no CLI mirror, and the signal-mismatch alert record closed as not-needed-yet with a named trigger. A sixth fell out of settling them — `STUDY_DESIGNER_SCHEMA_VERSION` splits in two, having been guarding both the dev-bench serial hop and the `embarch-api`→Core HTTP hop with one trigger list. Rows 21–24 in [embarch-decision-reversals.md](embarch-decision-reversals.md); the pattern worth carrying forward is that two of the four were doc-stated reasons for leaving a question open that the actual code contradicted, which is an argument for checking the code *before* deciding an open question is genuinely open.

- 2026-08-25 — **Milestone 7 Phase A closed**: the shared types every other phase builds against are real, green in both crates and all four consumers, with the cross-language wire pinning in place. Two things worth carrying forward. The schema bump landed as **7 → 8**, not the 5 → 6 decisions 39 and 40 were written against — decisions 42 and 43 were implemented in between and took v6/v7, so a bump number recorded in a design doc ahead of implementation is a fact with a shelf life. And the timing risk this milestone opened with **held**: Milestone 6's dev-bench firmware is still unflashed, so the reshape stayed a reshape. It is still live for Phase B, which is where the reshape actually reaches dev-bench's behavior.
- 2026-08-25 — Milestone 7 gained a fourth piece: **`Study.requires` (dev-bench + DUT firmware versions) with operator-selected reflash and result provenance** (`embarch-study-designer/design.md` §3 decision 40). Raised to anchor the outpost's manifest binding; it turned out to close a wider, unnamed gap — study results could not say which firmware produced them.

- 2026-08-25 — **Added Milestone 7 (MCU Load Tracing), and a new sub-project to go with it: [embarch-outpost](embarch-outpost/design.md).** The suite's first component that ships inside the DUT — a Zephyr module emitting a thread/ISR/marker timeline out a TX-only UART. Sequenced **before** Milestone 4 for the same reason the stream proposal gave for itself, now sharper: Milestone 4 is what makes the stream wire shape load-bearing, and neither it nor Milestone 6's dev-bench firmware has put a real byte through the path being reshaped. Three companion changes landed in the same design pass: `dev-bench bypass` as a real modelled route (`embarch-topology` decision 18), the stream proposal's inbound half accepted (`embarch-study-designer` decision 39, partially superseding a one-day-old decision 36 and marking `embarch-dev-bench` decision 22 superseded), and `embarch-ui` route declaration plus a post-hoc Trace view.

- 2026-08-24 — **`embarch-ui/milestone-1.md` CLOSED — the user confirmed the installed VS Code extension works for real.** Start/stop/open-in-browser all function correctly in their actual Windows VS Code (Remote-WSL, attached to the WSL2 instance it was installed into). Every item in the milestone's Definition of Done is now met.
- 2026-08-24 — **`embarch-ui`'s VS Code launcher extension entry updated: built/packaged/installed for real, distribution decided (internal `.vsix` only).** `embarch-ui/milestone-1.md` §4.8 closed end to end via a portable Node install; only a live click-through in a running editor is left, needing the user's own interaction.
- 2026-08-24 — **`embarch-ui`'s Next-bucket entry updated: all five tabs live, VS Code launcher scaffolded, and the three surfaces it replaces actually retired (`embarch-ui/milestone-1.md` §4.1–4.9).** `embarch-topology`'s `ui` subcommand, `embarch-study-designer`'s `study-designer-ui` binary, and `embarch-core`'s `/enroll` page are all deleted now, not just planned for deletion — each repo's own `design.md` updated in the same pass. `embarch.md`/`embarch-features.md` had the same "no tabs live yet"-class staleness, corrected in the same pass per `DOC-PROTOCOL.md` §4.
- 2026-08-24 — **`embarch-ui` moved from the Later bucket's old "EmbArch UI" placeholder into Next, as a real in-progress sub-project.** Repo created, design settled against five reviewed mockup artboards, `milestone-1.md` execution plan drafted — see [embarch-ui/design.md](embarch-ui/design.md)/[embarch-ui/milestone-1.md](embarch-ui/milestone-1.md) for full detail; `embarch.md`'s own changelog has the suite-level account.
- 2026-08-24 — **Milestone 5 (Study Designer UI) implemented, except real-hardware validation.** The interactive table-based `Study` builder, the merged live+static action list, and the user-authored custom-action registry (name + clickable enumerated byte choices, never a semantic description) all shipped inside `embarch-study-designer` the same day they were designed — `embarch-study-designer/milestone-11.md`. Live-smoke-tested against the real `reference-dut-fw` repo; closing the actual motivating gap (real captured GATT activity from a real DUT) needs a connected DUT/dev-bench and a real engineer-supplied action, deliberately left for whoever has that knowledge rather than guessed at here.
- 2026-08-24 — **Milestone 3 (Study Designer: Feature-Branch Iteration) closed.** All four companion docs' Definition of Done fully met: a real `BleConnect`->`GattDiscover`->`GattMonitorAll` study against the real reference-dut DUT completed clean via both CLI and MCP, `hw_lock`/`study_lock` confirmed independent in the harder concurrent ordering, and `events.json` round-trips the real result correctly. The milestone's own dev-bench code (implemented and hardware-validated back on 2026-08-20) had been sitting uncommitted in the working tree the whole time — committed closing this out (`embarch-dev-bench@a38bbf3`). A real production bug found and fixed along the way, not just a milestone-scoped workaround: `embarch-study-designer/design.md` §7's already-tracked debug-build stack-overflow risk, previously only reproduced against a tiny fixture, crashed `embarch-api`'s `study_status` for real on this milestone's own GATT-sized result — fixed by spawning `embarch-api`'s runtime on a dedicated 512 MiB-stack thread (`embarch-api/design.md` decision 36), which turned out to incidentally fix a second, separate-looking `--release`-build crash the same way. Closed with one real gap deliberately left open: a ~50% flake rate on the DUT-connect-and-discover path (one outright `BleConnect` failure, one mid-discovery disconnect, inconsistent live service counts across successful runs) — met the milestone's own "one clean run" bar twice, but not yet root-caused (`embarch-dev-bench/milestone-9.md` §6).
- 2026-08-19 — **Milestone 3 (Study Designer: Feature-Branch Iteration) execution plans drafted**, following a design-questions pass: [embarch-study-designer/milestone-9.md](embarch-study-designer/milestone-9.md) / [embarch-core/milestone-9.md](embarch-core/milestone-9.md) / [embarch-api/milestone-9.md](embarch-api/milestone-9.md) / [embarch-dev-bench/milestone-9.md](embarch-dev-bench/milestone-9.md), replacing the "Steps: not yet planned" placeholder. Board: the ESP32-C5-WROOM-1 DK, continuing from Milestone 2 rather than waiting on the still-unreplaced nRF54L15DK. Scope calls made in this pass: dev-bench's `BleConnect`/`DataExchange` dispatch (already implemented in `ble_bridge_real.c` since 2026-08-04 but never wired into `main.c`'s per-`Study` dispatch) is closed as part of this milestone, not treated as a prerequisite; the milestone's GATT interaction was redirected away from one specific known characteristic (a reference-dut dock-bounce debounce fix, `fix/bouncing_dock` — its actual fix stays explicitly out of scope, the branch has no commits past `main` as of this pass) toward two new generic `embarch-study-designer` types instead: `Action::GattDiscover` (live wildcard GATT-table discovery) and `Action::GattMonitorAll` (self-sufficient discover+subscribe-all+capture-everything); a separate, static GATT-config extraction tool (a generic `GattConfigExtractor` trait, one concrete implementation scoped to this one firmware's real macro conventions) answers "autodetect a DUT's GATT config" from source rather than live. Full detail: `embarch-study-designer/design.md` §3 decisions 31/32/33.
- 2026-08-20 — **Milestone 2 (Dev Bench Self-Test) closed**, same posture as Milestone 1: all three companion docs' Definition of Done closed except the host-side watchdog's lapse behavior, which two live attempts (a human-timed USB unplug, a software JTAG-reset race) both failed to exercise — the self-test fixture's `BleAdvertise` step resolves in well under a second, faster than either approach could land inside the timeout window. Real gaps found and fixed closing this out: a CMake staleness bug (`embarch-dev-bench/design.md` decision 25's own update) that silently kept a stale firmware-version string across an incremental build; dev-bench port auto-detection extended to recognize the ESP32-C5's own USB VID directly (`embarch-core/design.md` §5); and an MCP-specific schema bug in `run_study` that the CLI path structurally can't hit (`embarch-api/design.md` decision 31), found running the MCP-path repeat this milestone's own scope called for. Also removed a stale **Next** item found during this close-out's pre-close staleness grep (`DOC-PROTOCOL.md` §4): "`embarch-core`/`embarch-api` actually depending on `embarch-study-designer`" had already been resolved 2026-08-18 (both consume it as a real Cargo dependency, `embarch-study-designer/design.md`'s own changelog) but was never removed from this list.
- 2026-08-19 — Milestone 2 retargeted from the nRF54L15DK to the ESP32-C5-WROOM-1 DK (`embarch-decision-reversals.md` row 13): the nRF54L15DK's RMA replacement never arrived, and the ESP32-C5 interim substitute had already validated the whole dispatch/handshake/study mechanism end to end, so continuing to gate this milestone's Definition of Done on the unreplaced nRF54L15DK stopped making sense. `workspaces/espressif` is now this milestone's board; `workspaces/nordic` is no longer targeted. Companion docs updated: [embarch-core/milestone-8.md](embarch-core/milestone-8.md), [embarch-dev-bench/milestone-8.md](embarch-dev-bench/milestone-8.md), [embarch-api/milestone-8.md](embarch-api/milestone-8.md), [embarch-dev-bench/design.md](embarch-dev-bench/design.md).
- 2026-08-19/20 — First-ever physical-hardware run of `embarch-dev-bench` firmware — not yet on Milestone 2's own nRF54L15DK target (its replacement is still in transit after the original broke), but on a temporary ESP32-C5 interim substitute board, via new `embarch-core` ESP-JTAG flashing support that reverses `embarch-dev-bench/design.md` decision 13 (Core's probe-rs access was assumed permanently out of scope for flashing dev-bench itself). A real `BleAdvertise` self-test `Study` completed end to end for the first time on physical BLE hardware. Full detail: [embarch-dev-bench/design.md](embarch-dev-bench/design.md) decisions 13/26, [embarch-core/design.md](embarch-core/design.md) §3 decision 18, [embarch-dev-bench/milestone-8.md](embarch-dev-bench/milestone-8.md) §6a, [embarch-decision-reversals.md](embarch-decision-reversals.md) row 12. Milestone 2's own Definition of Done is unaffected — it stays nRF54L15DK-scoped and unmet until that board's replacement arrives.
- 2026-08-18 — Milestone 2's execution plans drafted, following a design-questions pass: [embarch-core/milestone-8.md](embarch-core/milestone-8.md) / [embarch-dev-bench/milestone-8.md](embarch-dev-bench/milestone-8.md) / [embarch-api/milestone-8.md](embarch-api/milestone-8.md), replacing the "Steps: not yet planned" placeholder. Milestone's Projects line grew from Core+Dev-Bench to Core+Dev-Bench+API — `embarch-api`'s already-locked-in `run_study`/`study_status` surface is now how the self-test study actually gets submitted, rather than a raw HTTP call against Core. Other scope calls made in the same pass: Core builds its full four-endpoint `/study*` surface now (not deferring `power-data`/`waveform-data` to Milestone 4); dev-bench closes `design.md` decision 8 (Rust cross-compile/west-module wiring for `embarch-study-designer`'s FFI staticlib) as part of this milestone rather than shipping with `study_ffi.c` still stubbed; the self-test `Study` fixture is `BleAdvertise`-only steps, needing no DUT and no power-sampling hardware.
- 2026-08-18 — Milestone 1 ("Flash & Build, real hardware") closed: both execution plans' Definition of Done fully met, including the two items left open the prior session — MCP-triggered `build_and_flash` (first for any project in this suite) and `serial_log`'s empty-capture finding, root-caused as a design-intent correction (`embarch-decision-reversals.md` row 11) rather than a bug. Full detail in [embarch-api/milestone-7.md](embarch-api/milestone-7.md) / [embarch-core/milestone-7.md](embarch-core/milestone-7.md) §6.
- 2026-08-17 — Milestone 1's execution plan drafted: [embarch-api/milestone-7.md](embarch-api/milestone-7.md) / [embarch-core/milestone-7.md](embarch-core/milestone-7.md), replacing the "Steps: not yet planned" placeholder. Deliberately doesn't pre-select a board/variant/revision in either doc — both defer to live `list_targets` discovery against the real repo at execution time, per decision 12, rather than hardcoding one of the combinations Milestone 6 found by hand. DoD scoped to require config-only `build_and_flash` (no `--firmware-path` override, unproven against real hardware for any project until now) and the MCP path (also unproven against real hardware for any project), both per user decision.
- 2026-08-17 — Milestones restructured at the user's request: the original numbered milestones (1–6, Flash through Onboarding) folded into a new "Shipped foundation" section rather than staying numbered, since none are still open work. Four new milestones take the freed-up 1–4 numbering, each keyed to a real umbrella-suite release plus the real `reference-dut-fw` repo as test target rather than a placeholder project or synthetic fixture: 1 (Flash & Build, real hardware — WSL2 API / Windows Core split, real reference-dut repo, closing the gap that no shipped milestone ever drove an actual physical flash through that repo), 2 (Dev Bench Self-Test — Core⟷dev-bench transport and real per-`Study` dispatch proven on physical hardware for the first time, no client repo, power sampling explicitly deferred), 3 (Study Designer: Feature-Branch Iteration — dev-bench BLE-connects to a real reference-dut DUT running a feature branch and forwards acquisition data to Core; one successful run is the Definition of Done, not a repeated iteration loop), 4 (Power-Sampling Study — acquire and wire in the provisional PPK2 front end, the one area 2/3 defer). Next bucket's "Dev-bench on real hardware" bullet removed, since Milestones 2/3 now cover exactly that gap. Execution docs for the new milestones, when created, are filed as `milestone-7.md`/`8.md`/`9.md`/`10.md` on disk (continuing the counter past the shipped foundation's 1–6) so they can't collide with those still-live historical files, even though this doc's own prose numbers them 1–4.
- 2026-08-15 — Release section rewritten, closing item 64 of that day's design-improvement review: replaced the stale "Rochambeau, Date: ?" placeholder with the real `v0.1.0` release (all three repos, real GitHub Releases, a real assembled `suite-v0.1.0` archive — all shipped 2026-08-11) and recorded "Rochambeau" as a superseded working name rather than deleting it outright.
- 2026-07-20 — Initial draft, Milestone 1 (Flash).
- 2026-07-21 — Added Milestone 2 (Token); added it to Rochambeau's includes list.
- 2026-07-27 — Added Milestone 3 (Study Designer): design-only scoping of `embarch-study-designer`, a new shared library sub-project. Not added to Rochambeau's includes list since this milestone ships no code.
- 2026-07-28 — Milestone 3 substantially closed (`embarch-study-designer/milestone-3.md` §3.1–3.5, most of §3.6): data model, wire format, Core endpoint surface, and result storage all resolved; repo created (empty). Only the nRF54 cross-compilation toolchain/build-wiring question remains, blocked on real dev-bench hardware.
- 2026-07-28 — Milestone 3 further closed (`embarch-study-designer/milestone-3.md` §3.7–3.8): the crate's data types now use fixed-capacity `heapless` collections rather than assuming a heap allocator on dev-bench firmware, and `embarch-api`'s future `run_study`/`study_status` tool/CLI names and param shapes are locked in ahead of implementation. Still only the nRF54 cross-compilation toolchain/build-wiring question (§3.6) remains open, blocked on real dev-bench hardware. Still not added to Rochambeau's includes list — no code shipped.
- 2026-07-29 — Added Milestone 4 (Study Designer Implementation): `embarch-study-designer` now has real, tested code implementing Milestone 3's design. Not added to Rochambeau's includes list yet — `embarch-core`/`embarch-api` don't consume it as a dependency yet, and `embarch-dev-bench` firmware (the next step) hasn't started.
- 2026-08-05 — Added Milestone 6 (Onboarding): the new `embarch-umbrella` sub-project plus `base_url = "auto"` in API, `start`/`stop` in Core, the suite's first release binaries, and the rewritten user guide. Also added the **Next** and **Later** buckets this file was missing entirely — [embarch.md](embarch.md) §2 and [embarch-features.md](embarch-features.md) already referred to "`embarch-roadmap.md`'s Later bucket" and "`embarch-roadmap.md` Next" as if they existed, so those were dangling cross-references (`DOC-PROTOCOL.md` §5). Not added to Rochambeau's includes list yet — no code shipped.
- 2026-07-29 — Added Milestone 5 (Dev Bench Scoping): design-only scoping of `embarch-dev-bench`, resolved via Q&A directly into `embarch-dev-bench/design.md`. Not added to Rochambeau's includes list — no code shipped.
