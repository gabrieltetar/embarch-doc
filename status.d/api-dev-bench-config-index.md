**Target:** embarch.md — "Per sub-project" table, `embarch-api` row's Interface reference cell
**Was:** "config.md, tools.md, studies.md — one table for both front ends" (three interface files linked)
**Now:** add `dev-bench-config.md` (`embarch-api/interfaces/dev-bench-config.md`) to the list — a
fourth interface file, split out of `config.md` by `tasks/api/081` and now linked from `spec.md`,
`decisions.md` and `tools-dev-bench.md` (`tasks/api/082`).
