**Target:** suite/user-guide.md — the "Core bound where you cannot reach it" row of the `doctor` troubleshooting table
**Was:** the row names `bound-narrow` and `bind-not-the-cause` only, which was the whole of check 17's Fail-side vocabulary
**Now:** check 17 gained `bind-unproven` on 2026-09-06 — Core answered at loopback, the recorded class needs `0.0.0.0`, and no service registration could be read, so whether the guest's gateway route works is unknown; it is a warn, and its fix is to ask from the guest rather than to reinstall blind. Note also that `bind-too-narrow` (Core answering, registration narrow) must **not** be fixed with `embarch setup`: something answered, so `setup` installs nothing and rewrites the recorded class, greening the check with the bind untouched. `bound-narrow`'s `setup` advice in this row stays correct — nothing is running on that path.
