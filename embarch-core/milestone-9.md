# embarch-core: milestone 9 — Study Designer: Feature-Branch Iteration

**Status:** done, 2026-08-24 — every remaining §3/§4 item closed for real; see §6's final entry. §1's "no new Core code" claim turned out false, twice over: the `probe_serial` gap (§6) is fixed and deployed to the live Windows Core; a second, deeper real-hardware finding (§6) then paused §3's own steps entirely — flashing/resetting for real surfaced a hardware scare (a DUT needing manual recovery, dev-bench's USB port going unresponsive), root-caused to real `probe-rs`/target quirks and fixed (`design.md` decision 21), but that investigation surfaced a genuine, still-open vulnerability (`design.md` decision 22: nothing today confirms a probe answering to a recorded serial is still wired to the board it's labeled as) that's now blocking any further real-hardware flash/reset in this milestone until closed. **Decision 22 is now implemented, deployed, and both real boards are enrolled — §3 unblocked on that front.** Decision 21's own fix, finally live-tested for real (§6, 2026-08-21), surfaced a *third* real-hardware problem that turned out to be moot rather than needing a root cause: a genuine `target_reset` pulse against dev-bench's ESP32-C5 wedged its native USB CDC/serial interface, and a DTR/RTS-on-open gotcha kept reproducing it — but the actual fix was sidestepping the native USB port entirely via the DevKitC-1's second, dedicated UART port. **Resolved for real, same day**: a real self-test `Study` completed end to end over that new link — `HelloAck`→`StepResult`(Pass)×2→`StudyDone{completed:true}`, confirmed via Core's own log and a direct `GET /study/{id}` call — after three more fixes (the CP2102N bridge chip's Windows driver, `board_gate::enforce_for_role` since a plain UART bridge has no JTAG capability to enroll under, and a missed `reset_dev_bench` call after the rebuild). `dev_bench.rs`'s new `SILABS_VID` auto-detection fix is now deployed to the live Windows service too, live-confirmed both branches: correctly ambiguous with the reference-dut DUT's J-Link also attached (no silent wrong pick), and resolving cleanly to dev-bench's own port with `EMBARCH_DEV_BENCH_SERIAL` set. §3's remaining real-hardware steps are unblocked. Execution plan for [embarch-roadmap.md](../embarch-roadmap.md)'s Milestone 3 ("Study Designer: Feature-Branch Iteration" — filed on disk as `milestone-9`). Companion to [embarch-study-designer/milestone-9.md](../embarch-study-designer/milestone-9.md) (the new wire types), [embarch-dev-bench/milestone-9.md](../embarch-dev-bench/milestone-9.md) (dispatching them on real firmware), and [embarch-api/milestone-9.md](../embarch-api/milestone-9.md) (driving both the DUT flash and the study). See [design.md](design.md) §3 decision 15 (the `hw_lock`/`study_lock` split) for the durable decision this milestone exercises for real, for the first time, rather than closes anew.

## 1. Goal, restated for Core

Unlike Milestones 1 and 2, this milestone needs **no new Core code** by default — `study.rs`'s passthrough is already generic over whatever `Action`/`StepResult` shape `embarch-study-designer` defines (`design.md` §9's own framing: Core links the crate as a real dependency and relays/(de)serializes its types verbatim, never hardcoding a column or field). The two new `Action` variants and `StepResult` fields ([embarch-study-designer/milestone-9.md](../embarch-study-designer/milestone-9.md) §3.2) should reach `events.json` correctly the moment Core is rebuilt against the updated crate — no `study.rs` changes anticipated. This milestone's real job is **validating a scenario Core has never actually been exercised against: a DUT probe connection and a dev-bench study running in the same session**, and confirming that passthrough claim empirically rather than by inspection alone.

## 2. Scope for this milestone

- **No new endpoints, no new decision anticipated.** If §3.2 finds `events.json` doesn't actually carry the new fields correctly (a real serde/postcard mismatch, not just a hypothetical), that becomes real, unplanned work — tracked here as a risk (§5), not pre-scoped as a step.
- **The concurrency scenario is new: `hw_lock` (DUT probe, via `embarch-api`'s `build_and_flash`) and `study_lock` (dev-bench serial link, via `run_study`) are exercised together for the first time.** `design.md` §3 decision 15 already states they're independent locks over independent physical connections — this milestone is the first time both are actually held in the same real session (flash the reference-dut DUT, then run a study against dev-bench while that DUT is powered and running), not just reasoned about.
- **Out of scope:** any new `/study*` endpoint behavior; power-data/waveform-data (still correctly empty for this milestone's fixture, same as Milestone 2); the DUT's own serial console (`design.md` §5's existing note that `/serial-log` was never meant to reach a DUT's console, unaffected here).

## 3. Steps

### 3.1 Rebuild against the updated `embarch-study-designer` dependency

Bump to whatever version/path [embarch-study-designer/milestone-9.md](../embarch-study-designer/milestone-9.md) produces; `cargo build`/`cargo test`/`cargo clippy --all-targets -- -D warnings` clean, with no `study.rs` source changes expected (§1).

### 3.2 Confirm `events.json` carries `gatt_services`/`gatt_activity` correctly, for real

Run the milestone's actual `Study` (via [embarch-api/milestone-9.md](../embarch-api/milestone-9.md)) and inspect the resulting `events.json` by hand: `StepResult.gatt_services`/`gatt_activity` present, correctly shaped, and matching what [embarch-dev-bench/milestone-9.md](../embarch-dev-bench/milestone-9.md) §3.7–3.9 actually captured — not just "the build succeeded."

### 3.3 Flash the reference-dut DUT via `hw_lock` while dev-bench's `study_lock` is idle, then run a study while the DUT stays powered

Sequence: `build_and_flash` the reference-dut DUT (acquires and releases `hw_lock`), then `run_study` against dev-bench (acquires `study_lock`) while the just-flashed DUT is still powered and advertising — confirming the two locks really do govern independent physical connections in practice, not just in the design's own reasoning (`design.md` §3 decision 15).

### 3.4 Attempt a `/flash` (or `/reset`) call against the DUT *while* a study is in flight against dev-bench

The inverse ordering from §3.3 — confirm a `hw_lock`-guarded call succeeds (or fails only for reasons unrelated to `study_lock`) while `study_lock` is held by an in-progress `GattMonitorAll` capture, proving the two really don't contend.

### 3.5 End-to-end validation via `embarch-api`

The actual Definition of Done for this milestone as a whole: [embarch-api/milestone-9.md](../embarch-api/milestone-9.md)'s full sequence (flash the DUT, connect, discover, monitor, forward into `events.json`) run for real, not just this doc's own steps in isolation.

## 4. Definition of done

- [x] Core rebuilt against the updated `embarch-study-designer` dependency, no source changes needed in `study.rs` (§3.1) — confirmed true; every later pass (including this milestone's own real runs) built clean with no `study.rs` changes required for the new `Action`/`StepResult` shapes.
- [x] `events.json` correctly carries `gatt_services`/`gatt_activity` for a real run, matching what dev-bench actually captured (§3.2) — confirmed 2026-08-24: `GET /study/{id}` (which reads a completed study back from `events.json` on disk, decision 24) round-tripped a real 8-service `gatt_services` result correctly through `embarch-api`'s `study_status`.
- [x] `hw_lock` (DUT flash) and `study_lock` (dev-bench study) confirmed independent in both orderings — flash-then-study and study-during-flash-attempt (§3.3, §3.4) — confirmed 2026-08-24: flash-then-study exercised routinely across every real run this milestone; study-during-flash-attempt confirmed directly — a `build_and_flash` against the DUT issued while a 25s dev-bench `BleAdvertise` study was in flight, both completed successfully, neither blocked or erroring on the other.
- [x] Full path validated via `embarch-api` against real hardware on both sides (a real DUT, a real dev-bench) (§3.5) — confirmed 2026-08-24: real `BleConnect`->`GattDiscover`->`GattMonitorAll` studies against the real reference-dut DUT, via both CLI and MCP.
- [x] Any real gap found folded back into `design.md` per `DOC-PROTOCOL.md` §5 — done (§6).
- [x] **`design.md` decision 22 (`known_boards`/`POST /probes/enroll`) implemented and both real boards enrolled** — implemented, deployed, and both real boards enrolled as of 2026-08-20/21 (§6). **Superseded 2026-08-21**, not reverted: this logic (and `hardware_id.rs`/`board_gate.rs`/`dev_bench.rs`) moved wholesale into the new shared `embarch-topology` crate (`embarch-topology/design.md` §3 decisions 2/3, 22/23 here narrowed to record that move) — Core now calls `embarch_topology::hardware` rather than owning this table itself. The underlying capability this item asked for is real, live, and exercised throughout this milestone's real runs; it just isn't this repo's own code any more.
- [x] **Decision 21's `target_reset`-before-reset fix, and the whole `HelloAck` investigation it kicked off, resolved for real 2026-08-21** — not by root-causing the native USB port's DTR/RTS/CDC-wedge quirks, but by moving dev-bench's runtime link off that peripheral entirely, onto the ESP32-C5-DevKitC-1's second, dedicated UART port. A real self-test `Study` completed end to end over the new link: `HelloAck` (`compatible: true`) → two `BleAdvertise` steps, both `Pass` → `StudyDone{completed:true}`, confirmed via Core's own log and a direct `GET /study/{id}` call. Getting there needed the CP2102N bridge chip's Windows driver installed, `board_gate::enforce_for_role` (a plain UART bridge has no JTAG capability to enroll under), and a missed `reset_dev_bench` call after the rebuild. Full narrative in `design.md` decision 21's own final update and `embarch-dev-bench/design.md` decision 26.
- [x] **`dev_bench.rs` auto-detection updated to match: gained `SILABS_VID`, dropped `ESPRESSIF_VID` as a link candidate, fixed the dev-bench-role serial fallback for the new split link/probe topology.** Built, tested (64 passed), clippy-clean, and deployed to the live Windows service. **Live-confirmed both branches on the real machine**: with the reference-dut DUT's own J-Link (COM5) and dev-bench's bridge chip (COM13) both attached, `detect-dev-bench` correctly reports genuine ambiguity (not a silent wrong pick, and not a misleading "excluded by an env var") — and with `EMBARCH_DEV_BENCH_SERIAL` set explicitly to dev-bench's own serial, it resolves cleanly to `COM13` (`detected_by: silabs-vid-match`). `embarch-core/design.md` §5 and `embarch-dev-bench/design.md` §4 both updated.

## 5. Open questions / risks carried into execution

- **The "no new Core code" claim (§1) is a prediction, not yet confirmed** — §3.2 either confirms it or surfaces the first real counter-example; if `events.json`'s JSON shape doesn't actually match what `embarch-study-designer`'s `serde` derive produces byte-for-byte the way `data.csv`'s own column-ownership discipline assumes, that's new, unplanned work for this milestone.
- ~~**Whether the DUT probe (reference-dut) and dev-bench's own on-chip debug probe (ESP32-C5) can be plugged into the same host machine simultaneously without a USB/driver conflict**~~ — **answered 2026-08-20: yes, no USB/driver conflict** — `GET /status` reports both simultaneously without issue. The real problem found wasn't a driver conflict but a Core-side *selection* bug: `probe_serial` disambiguation was documented as implemented and wasn't, so flashing picked the wrong probe outright. See §6.
- **Whether `EMBARCH_DEV_BENCH_PORT`'s manual-override requirement (still needed for the ESP32-C5 alongside a second, DUT-facing serial/JTAG connection) creates any port-selection ambiguity now that two USB-JTAG-class devices are live on the same machine** — `dev_bench.rs`'s VID-matching (`design.md` §5) was validated with only dev-bench plugged in; a second board's own USB descriptor showing up alongside it is a new condition to actually observe.
- ~~**A physical replug does recover the write-level USB wedge, but the underlying `HelloAck` failure is most likely `embarch-dev-bench/design.md` decision 26's own DTR/RTS-on-open gotcha, not the stuck-in-download-mode theory this investigation started from.**~~ — resolved 2026-08-21 by sidestepping it, not by sweeping DTR/RTS combinations: the fix was moving dev-bench's link to the DK's second, dedicated UART port entirely, where a real self-test `Study` then completed end to end. The DTR/RTS sweep this item called for was never attempted, and is no longer needed — see the DoD items above.
