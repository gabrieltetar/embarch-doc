# embarch-dev-bench: design

**Status:** draft, 2026-07-29. Scoping in progress — repo exists ([gabrieltetar/embarch-dev-bench](https://github.com/gabrieltetar/embarch-dev-bench), created empty), no code yet. Companion to [embarch-study-designer/design.md](../embarch-study-designer/design.md), which this doc treats as a given: the `Study`/`DevBenchMessage` types and the COBS/postcard wire protocol are locked in there, not re-litigated here.

## 1. Purpose and scope

`embarch-dev-bench` is the physical rig that plays the DUT's BLE counterpart during a `Study` (`embarch-study-designer/design.md` §1) — it advertises, connects (as central or peripheral), exchanges GATT data, and samples power, per whatever `Study.steps` `embarch-core` relays to it over a serial link. It is Zephyr-based C firmware, cross-vendor by design: "one firmware project" doesn't mean one board or one silicon vendor, it means one shared application implementation that different west workspaces build for different vendor targets (§3).

**v1 scope, explicitly bounded:**
- **BLE + power sampling only** — exactly what `embarch-study-designer`'s `Action`/`GattOperation` enum already models (`BleAdvertise`, `BleConnect`, `DataExchange`, `PowerSampleWindow`, `GattOperation::StreamCapture`). `embarch.md`'s original one-line description ("actively stimulates DUT inputs and senses outputs") is broader than this — GPIO/analog stimulus (driving a button pin, injecting an analog sensor signal) is real future scope, not v1, and isn't modeled in `embarch-study-designer` today. Adding it later means a new `Action` variant there, not just dev-bench firmware work.
- **No physical DUT connector, no power-sampling hardware yet.** v1 is BLE-proximity-only; a DUT is whatever's in the DUT's own physical BLE range. `PowerSampleWindow`/`GattOperation::StreamCapture` stay real types that dev-bench firmware will eventually serve, but the actual sampling hardware (a shunt + ADC, or similar) is out of scope until a specific board/BOM decision is made.
- **No on-board status indication** (LEDs, etc.) — dev-bench's state is observable only through `embarch-core`/`study_status`, not by looking at the board.

## 2. Architecture overview

```
embarch-dev-bench/                     (single repo, one shared application)
├── app/                                <- shared C source, vendor/RTOS-API-agnostic
│   ├── src/
│   │   ├── main.c
│   │   ├── ble_bridge.h                (internal API mirroring Action/GattOperation, decision 16)
│   │   ├── ble_bridge_real.c           (Action -> Zephyr BT host calls; workspaces/nordic/)
│   │   ├── ble_bridge_stub.c           (canned Outcomes, no BLE host; workspaces/native_sim/)
│   │   ├── serial_protocol.[ch]        (COBS framing, hand-written postcard-compatible
│   │   │                                DevBenchMessage encode/decode)
│   │   └── study_ffi.[ch]              (stub returning fixed decode results until decision 20's
│   │                                    real FFI wiring lands; same call signature either way)
│   ├── tests/serial_protocol/          (ztest unit tests for serial_protocol.c, run on native_sim)
│   ├── boards/                         (per-board devicetree overlays/Kconfig, shared; flat
│   │   │                                filenames matching Zephyr's own board-conf-fragment
│   │   │                                convention, auto-merged with no extra CMake wiring)
│   │   ├── nrf54l15dk_nrf54l15_cpuapp.conf
│   │   └── native_sim.conf
│   └── CMakeLists.txt                  <- referenced by every workspace below, not copied; picks
│                                           ble_bridge_real.c vs _stub.c via CONFIG_ARCH_POSIX
└── workspaces/
    ├── nordic/                         <- west topdir #1 (real hardware)
    │   ├── manifest/west.yml           (manifest: nRF Connect SDK's Zephyr fork + Nordic HAL;
    │   │                                lives in a manifest/ subdirectory, not at this workspace's
    │   │                                own root, because `west init -l <dir>` places `.west/`
    │   │                                *next to* <dir> — nesting the manifest one level down is
    │   │                                what makes `.west/`, zephyr/, modules/ land inside
    │   │                                workspaces/nordic/ itself, keeping this topdir genuinely
    │   │                                independent of native_sim's, per decision 2)
    │   ├── zephyr/                     (checked out by `west update`, gitignored)
    │   ├── modules/                    (checked out by `west update`, gitignored, includes
    │   │                                embarch-study-designer as a west-managed project, §3 decision 8)
    │   └── app -> ../../app            (points at the shared application, not a copy)
    └── native_sim/                     <- west topdir #2 (host-side, no hardware, §3 decision 16)
        ├── manifest/west.yml           (manifest: vanilla Zephyr, native_sim board support; same
        │                                manifest/-subdirectory reasoning as nordic/ above)
        ├── zephyr/                     (checked out by `west update`, gitignored)
        └── app -> ../../app            (same shared application; CMakeLists.txt picks
                                          ble_bridge_stub.c over ble_bridge_real.c, decision 16)
```

`embarch-dev-bench` talks to `embarch-core` over a plain UART (§3 decision 6), carrying COBS-framed `DevBenchMessage`s exactly as `embarch-study-designer/design.md` §3 decisions 10/12/20 already define. `embarch-core` never talks BLE or samples power itself (`embarch-core/design.md` §1) — that stays entirely inside this firmware.

## 3. Locked-in design decisions and rationale

1. **Zephyr as the one common RTOS across every board/vendor — not a custom multi-SDK HAL abstraction.** "One firmware project for any board" is interpreted as "any board a Zephyr flavor supports," not "every vendor's own native SDK (ESP-IDF, STM32Cube, Pico-SDK) behind a hand-written abstraction layer." The latter is a materially bigger undertaking — effectively building a small cross-platform embedded framework from scratch (Arduino-core/PlatformIO/mbed's job) — and buys nothing `embarch-dev-bench` specifically needs, since Zephyr already provides vendor-portable BLE host, GPIO, and ADC APIs. This also means `embarch-study-designer/design.md` §3 decision 7 (dev-bench firmware is C, not Rust, because the BLE host stays a C API) doesn't need to be reopened for a broader vendor set — every mainstream vendor's BLE host under Zephyr is the same Zephyr Bluetooth host API regardless of silicon.
2. **One repo, multiple west workspaces (one per vendor family), sharing one application source tree — not one single west workspace, and not one repo per vendor.** A west manifest (`west.yml`) pins exactly one Zephyr revision at a fixed path; a vendor's own Zephyr fork (e.g. Nordic's nRF Connect SDK, decision 3 below) pins its *own* revision, incompatible with mixing into the same manifest as vanilla Zephyr or another vendor's fork. Resolved by giving each vendor family its own west topdir under `workspaces/`, each with its own `west.yml`, while the actual application implementation (`app/`) is written once and referenced (not copied) by every workspace's build. This achieves the part of "one firmware project" that actually matters — one shared implementation, one repo, one git history — without fighting west's one-manifest-one-Zephyr-revision model.
3. **Vendor SDK forks are accepted per family (not insisted-on vanilla Zephyr everywhere) — but application code is restricted to portable Zephyr APIs regardless.** A vendor fork (e.g. NCS) is often the only place brand-new silicon's HAL/BLE controller support lands first — insisting on vanilla-only would risk blocking on upstream support timing for every new board. Accepted trade-off: `app/src/*.c` only calls stable, vendor-neutral Zephyr subsystem APIs (`bt_*`, `gpio_*`, `adc_*`) — never anything NCS-proprietary — so the same source keeps working if a given workspace's pinned revision is later swapped toward vanilla, or if a second workspace picks a different flavor entirely. The cross-vendor Zephyr-revision-reconciliation question (could two families ever share one workspace after all?) is deliberately not solved now — see §4.
4. **First board: nRF54L15DK.** Chosen over nRF54H20DK specifically to avoid taking on `embarch-study-designer/design.md` §3 decision 7's already-flagged nRF54H20 complexity (mandatory multi-core `sysbuild` build) for a board whose job here — running the Zephyr BLE host in both central and peripheral roles, bridging serial to Core — doesn't need multiple cores. Single-core, standard (non-`sysbuild`) Zephyr build, well-trodden path.
5. **First workspace (`workspaces/nordic/`) pins nRF Connect SDK (NCS), not vanilla Zephyr.** Follows directly from decision 3: nRF54-family support is an NCS-first (often NCS-only, for the newest parts) proposition. BLE controller for this workspace specifically: **Nordic's SoftDevice Controller** (proprietary, shipped via NCS) over Zephyr's open-source BLE controller — best feature/support coverage on Nordic silicon specifically, at the cost of that specific choice being Nordic-only (a future non-Nordic workspace picks its own controller independently; this isn't a cross-workspace decision).
6. **Core link: a plain UART over the nRF54L15DK's on-board J-Link VCOM, not USB CDC ACM — one interface, no separate log/console interface.** Originally specified as USB CDC ACM; revised once implementation research found the nRF54L15 SoC has no USB device peripheral at all (USB only arrives with the newer nRF54LM20 family) — the DK's USB connector belongs entirely to its on-board SEGGER J-Link debug-probe chip, not the target SoC. That J-Link chip already bridges one of the nRF54L15's real UART peripherals to a virtual COM port on the host (the same VCOM used for the DK's existing debug console) — firmware-side this is a completely ordinary Zephyr UART (`uart_poll_out`/interrupt-driven `uart_*` calls on a physical UART peripheral), with no SEGGER-specific code and no USB device stack anywhere in this firmware. Dev-bench still appears as a plain serial port (`/dev/ttyACM*`/a COM port) when plugged in, still bus-powers the board, still matches Core's own existing DUT serial-log pattern (`embarch-core/design.md` §5) and requires no separate network stack (`embarch-study-designer/design.md` §3 decision 4's reasoning for the hop, unaffected by this revision) — only the physical/peripheral mechanism changed, not the wire protocol or Core-facing behavior. A second interface for dev-bench's own debug/log output was considered and rejected in favor of decision 7 below, same reasoning as before, now just "a second UART instance" rather than "a second CDC ACM interface."
7. **Dev-bench's own log output travels as a new `DevBenchMessage` variant, not as raw interleaved bytes on the shared line.** Literally sharing one byte stream between free-text log lines and COBS-framed `DevBenchMessage`s would corrupt the protocol — COBS frames are self-delimiting, plain log text isn't, so a receiver parsing for frame boundaries would choke on stray log bytes it wasn't expecting. Resolved the same way every other kind of dev-bench-to-Core data already travels: a log line is just another properly-framed message. Implemented as an append-only `DevBenchMessage::LogLine { text: heapless::String<{limits::MAX_LOG_LINE_LEN}> }` variant in `embarch-study-designer` (same discipline as that crate's §3 decision 10) — a small, forward-compatible addition to that crate, not a redesign.
8. **The `embarch-study-designer` Rust staticlib is pulled in as a west-managed module, with its cross-compilation invoked automatically as part of `west build`.** A CMake custom command (triggered from `app/CMakeLists.txt`) invokes `cargo build --release --target <triple> --features ffi`, keyed off whatever target architecture the active board implies, rather than requiring a separate manual build/publish step before `west build` can succeed. Chosen over a pre-built/fetched artifact so there's exactly one build command a developer runs, matching how the rest of a Zephyr application already builds. The concrete `<triple>` mapping per board, and the actual west-module wiring (`module.yml`), aren't implemented yet (§4) — `embarch-study-designer/design.md` §7's nRF54 cross-compilation toolchain gap is the same gap this decision needs closed to actually work.
9. **No CI for now.** Matches `embarch-study-designer`'s own precedent (no CI set up there either) — revisit once there's more than one board actually building, so a compile-only `west build` matrix check has more than one target to be useful across.
10. **A second vendor family/board is deliberately left unspecified.** The `workspaces/`-per-vendor-family convention (decision 2) is designed to obviously extend, but no second vendor, board, or Zephyr flavor is committed to yet — keeps the multi-workspace layout honest (built to extend, not pre-built for a hypothetical) rather than speculatively scaffolding for a board that doesn't exist.
11. **BLE pairing: Just Works, bonds kept RAM-only and cleared on `Hello` — no NVS/settings persistence across reboots.** Some real DUTs require pairing/encryption before certain characteristics are accessible, so v1 needs at least Just Works (Zephyr's `CONFIG_BT_SMP`) rather than unencrypted-only. Persisting bonds to flash (Zephyr's Bluetooth settings subsystem) was considered and rejected: a study should always start from a known, clean pairing state rather than accumulate stale bonds across unrelated studies or reboots. Resolved by extending `embarch-study-designer/design.md` §3 decision 12's existing "`Hello` doubles as a hard reset" — a fresh `Hello` now also clears dev-bench's in-RAM BT bonding table, not just its study execution state. A single study's own internal reconnects (if a `Study`'s steps disconnect and reconnect the same DUT) still don't have to re-pair, since nothing clears the bond table mid-study.
12. **Auto-detection via the on-board J-Link's own SEGGER VID plus a product-string/serial-number match, not a custom VID/PID — revised from the original plan once decision 6 moved off USB CDC ACM.** A custom VID (`0xEBAC`)/PID (`0xDB01`) was originally locked in for `embarch-core` to identify dev-bench programmatically among other serial devices (e.g. the DUT's own debug console), the same auto-detection goal as before — but that descriptor would have to be advertised by dev-bench's own USB CDC ACM stack, which decision 6 established doesn't exist on this SoC; the port Core actually sees is enumerated by the on-board J-Link chip, which advertises SEGGER's own USB descriptor (VID `0x1366`), not anything dev-bench firmware controls. `embarch-core` instead identifies dev-bench's port by matching SEGGER's VID plus a product-string or serial-number heuristic (e.g. matching against the specific J-Link's serial number recorded once at setup, since a lab may have more than one SEGGER-probed board plugged in) — entirely an `embarch-core`-side change, no firmware-side VID/PID logic at all. `embarch-core/design.md` needs a matching update once that auto-detection logic is actually implemented there (not yet — tracked as an open item, §4).
13. **Flashing dev-bench firmware is a direct `west flash` (J-Link/nrfjprog), entirely outside `embarch-core`.** `embarch-core`'s probe-rs access is explicitly scoped to the DUT (`embarch-core/design.md` §1) — it has no business flashing the bench that's testing that DUT. A human plugs in dev-bench's own on-board debugger and runs the standard Zephyr/NCS `west flash` workflow, same as any other Zephyr application.
14. **No local hardware watchdog.** `embarch-study-designer/design.md` §3 decision 16's Core-side host watchdog (added specifically because a hung dev-bench can't self-report its own `TimedOut`) is treated as sufficient detection; a self-resetting local `CONFIG_WDT` was considered and deferred rather than rejected outright — it's a cheap addition to revisit if a hung dev-bench in practice needs power-cycling too often for Core's detection alone to be a good enough experience.
15. **One dev-bench, one DUT connection at a time — no multi-connection support.** Matches `embarch-study-designer/design.md`'s existing "one study in flight at a time" model (that crate's §5.1) exactly: testing multiple DUTs in parallel means multiple independent Core+dev-bench pairs, not one dev-bench juggling several BLE connections. `CONFIG_BT_MAX_CONN` stays at its default single-connection value; nothing in `embarch-study-designer`'s types models addressing more than one concurrent DUT connection per `Study` anyway.
16. **A `native_sim` workspace, alongside `nordic/`, for host-side iteration without real hardware.** Zephyr's `native_sim` board runs a Zephyr application as a native Linux process — useful here for exercising the serial/COBS protocol parsing and the `embarch-study-designer` FFI glue (`study_ffi.c`) without needing the physical nRF54L15DK plugged in. Scoped narrowly: no virtual-PTY serial transport (protocol-parsing/FFI-glue logic is unit-tested directly, in-process, not through a literal serial link — framing correctness itself is `embarch-study-designer`'s own round-trip tests' job, not this workspace's). BLE is stubbed as a **separate source file, not an `#ifdef`-gated branch inside one file**: `ble_bridge_real.c` (Zephyr BT host calls, built by `workspaces/nordic/`) and `ble_bridge_stub.c` (canned `Outcome`s, built by `workspaces/native_sim/`) both implement the same internal C API that `main.c`/`serial_protocol.c` call into, and each workspace's `CMakeLists.txt` picks which one to compile — keeping the real BLE-host-calling code free of stub-only branches (and vice versa), at the cost of the two files needing to be kept in signature-sync by hand, acceptable since that shared internal API is small (mirrors the `Action`/`GattOperation` surface, §1). This keeps the full study-execution flow (serial → dispatch → "BLE" action → result) runnable and testable end-to-end on a host machine, just without a real radio at the end of it.
17. **BLE address: a static random address, fixed at build/boot time — not a resolvable private address (RPA), and not runtime-configurable by Core.** Simplicity and test reproducibility over BLE-privacy realism: dev-bench isn't emulating a privacy-conscious consumer device, it's a test fixture, and a stable address makes logs/captures easier to correlate across runs. Plain Zephyr default behavior, no custom addressing logic needed.
18. **`HelloAck` gains a firmware-identity field: `firmware_version: heapless::String<{limits::MAX_FIRMWARE_VERSION_LEN}>`, a single free-form string, not a structured multi-field type.** `schema_version`/`compatible` alone (`embarch-study-designer/design.md` §3 decision 12) tell Core whether the wire shape matches, but not which actual firmware build is running on the other end of the link — a real gap when something misbehaves and the question is "did this dev-bench even get reflashed with the fix." A single string is the simplest shape that still answers that question; whatever the dev-bench build embeds into it (`git describe` output, a board ID, a build timestamp, some combination) is dev-bench build-tooling's own concern, not fixed at the wire-type level — the append-only discipline (`embarch-study-designer/design.md` §3 decision 10) leaves room for a future structured field alongside this one if a single string ever proves insufficient. Implemented via a new `limits::MAX_FIRMWARE_VERSION_LEN` constant and `STUDY_DESIGNER_SCHEMA_VERSION` bump (v1 → v2) in `embarch-study-designer` — same discipline as any other wire-relevant change to that crate (§3 decision 12), not a new kind of change. Bumped in the same pass as decision 7's `LogLine` addition (one schema-version bump covering both, per §4's prior note).
19. **Dev-bench firmware validates `steps_crc` itself before running any step — restating, not reopening, `embarch-study-designer/design.md` §3 decision 17.** That decision already specifies this exact behavior at the Core↔dev-bench hop; this firmware's `study_ffi.c` is expected to call the crate's existing `essd_study_decode_and_verify` FFI function (`embarch-study-designer`'s `src/ffi.rs`) for it directly, rather than reimplementing CRC verification — the function already exists and already returns exactly the "did decode succeed / does the CRC match" answer this step needs.
20. **Initial implementation bring-up compiles `study_ffi.c` against a stub, not the real `embarch-study-designer` FFI staticlib, until decision 8's west-module/cross-compile wiring exists.** Mirrors the reasoning already accepted for `native_sim`'s BLE stub (decision 16): getting `main.c`/`ble_bridge_real.c`/`serial_protocol.c` building and running on real nRF54L15DK hardware (even against fixed/fake decode results) doesn't need to wait on the separate, harder problem of cross-compiling and linking a Rust staticlib per board target — that problem is decision 8's to close, and closing it is gated on `embarch-study-designer/design.md` §7's own nRF54 toolchain gap. `study_ffi.c` starts as a stub returning fixed results, gets swapped for real calls into the compiled staticlib once decision 8 lands; no interface change expected at that point, since `study_ffi.c`'s call signatures are dictated by `embarch-study-designer`'s existing FFI surface (that crate's §3 decision 7), not invented by this stub.

## 4. Open questions / future work

- **`embarch-core`'s SEGGER-VID-plus-product-string auto-detection logic (decision 12, revised)** — not implemented anywhere yet; `embarch-core/design.md` needs a matching update once it is. Worth checking in practice whether a product-string/serial-number match is actually sufficient to disambiguate dev-bench's VCOM from the DK's own debug-console VCOM if the J-Link exposes both over the same USB connection (common on Nordic DKs — often two or more CDC interfaces off one on-board debug chip); may need to match on a specific USB interface index/COM-port ordinal too, not just VID+product string. Flagged here since it surfaced during dev-bench firmware bring-up, but it's squarely `embarch-core`'s implementation to do.
- **The west-module wiring for the Rust staticlib (decision 8)** — the actual `module.yml`, the CMake custom command, and the board-to-Rust-target-triple mapping aren't designed yet, let alone implemented. This is the same nRF54 cross-compilation gap `embarch-study-designer/design.md` §7 already tracks; closing it here and there is the same piece of work. Decision 20 means this no longer blocks starting implementation — `study_ffi.c` ships as a stub until it's closed.
- **Cross-vendor Zephyr-revision reconciliation (decision 3)** — whether a second vendor family ever gets its own `workspaces/` entry versus somehow sharing one with Nordic's NCS-pinned workspace is unresolved, and deliberately deferred until an actual second board makes it a real question rather than a hypothetical one.
- **GPIO/analog stimulus** (§1) — real future scope per `embarch.md`'s broader description of this sub-project, but requires a new `embarch-study-designer` `Action` variant first; not started.
- **Power-sampling hardware and any physical DUT connector** (§1) — both entirely unscoped; depends on a BOM/hardware decision that hasn't been made.
- **On-board status indication** — deliberately deferred (§1), not forgotten; revisit if `study_status` polling proves insufficient for bench-side debugging in practice.
- **Local hardware watchdog (decision 14)** — deliberately deferred, not rejected; revisit based on real dev-bench hang experience.
- **Detecting "dev-bench isn't actually connected" (unplugged, wrong port) is entirely `embarch-core`'s responsibility** — not this document's concern; noted here only so the split of responsibility is explicit.

## 5. Changelog

- 2026-07-20 — Placeholder created alongside the embarch-doc per-sub-project restructure.
- 2026-07-29 — Initial scoping pass, resolved via Q&A rather than a written proposal-first draft: repo created empty at [gabrieltetar/embarch-dev-bench](https://github.com/gabrieltetar/embarch-dev-bench). Locked in: Zephyr as the common cross-vendor RTOS (not a custom multi-SDK HAL); a single repo with multiple west workspaces (one per vendor family) sharing one application source tree, resolving the "a west manifest pins one Zephyr revision" constraint against vendor forks like NCS; NCS (not vanilla Zephyr) for the first, Nordic-family workspace, with application code restricted to portable Zephyr APIs regardless; nRF54L15DK as the first board (over nRF54H20DK, to avoid its `sysbuild` complexity for a role that doesn't need multiple cores); Nordic's SoftDevice Controller for that workspace's BLE controller; USB CDC ACM as the Core link, one interface; dev-bench's own log output modeled as a new `DevBenchMessage::LogLine` variant rather than a second interface or raw shared-wire text; the Rust FFI staticlib pulled in as a west module with its cross-compilation invoked automatically via a CMake custom command; no CI and no second vendor/board committed to yet. GPIO/analog stimulus and power-sampling hardware explicitly scoped out of v1.
- 2026-07-29 — Second scoping round, closing six more open items: BLE pairing needs Just Works (decision 11), with bonds kept RAM-only and cleared on `Hello` (amending `embarch-study-designer/design.md` §3 decision 12 to also wipe the BT bonding table, not just study execution state) rather than persisted to flash; a custom USB VID/PID for Core to auto-detect dev-bench rather than manual port selection (decision 12); flashing dev-bench is a direct `west flash`, entirely outside `embarch-core` (decision 13); a local hardware watchdog is deferred rather than added, since Core's existing host-side watchdog (`embarch-study-designer/design.md` §3 decision 16) is treated as sufficient for now (decision 14); confirmed one dev-bench/one DUT connection at a time, no multi-connection support (decision 15); added a `native_sim` workspace for host-side iteration on the serial-protocol/FFI-glue logic without real hardware, with `ble_bridge.c` stubbed to return canned `Outcome`s rather than either a virtual-PTY serial transport or excluding BLE code from that build entirely (decision 16); BLE address is a static random address fixed at build time, not RPA or Core-configurable (decision 17).
- 2026-07-29 — Third scoping round: `HelloAck` will carry a firmware-identity field (build version/board ID) for diagnostics, exact shape deferred to `embarch-study-designer` (decision 18); confirmed dev-bench firmware itself validates `steps_crc` via that crate's existing `essd_study_decode_and_verify` FFI function rather than reimplementing the check (decision 19, restating `embarch-study-designer/design.md` §3 decision 17 rather than reopening it); confirmed detecting an absent/wrong-port dev-bench is entirely `embarch-core`'s responsibility, out of this document's scope. User confirmed no further open items for this scoping pass.
- 2026-07-30 — Verified the first implementation pass against a real `west build`, per the user's explicit one-time override of the standing "never build firmware autonomously" rule: `west init`/`west update` (Zephyr `v4.4.0` + modules) and `west build -b native_sim app` surfaced two real bugs the design/implementation pass below hadn't caught without an actual toolchain — `CONFIG_CONSOLE=n` is a hard Kconfig error on native_sim (its board Kconfig unconditionally force-selects console support); fixed by disabling `CONFIG_BOOT_BANNER` instead, which was the thing actually writing unwanted bytes to the link UART. `native_sim`'s `zephyr,console` node has no real `uart_poll_in`/`uart_poll_out`-capable driver unless `CONFIG_SERIAL`/`CONFIG_UART_NATIVE_PTY` are explicitly enabled (silently falls back to a raw POSIX console otherwise, failing at link time) — added both to `app/boards/native_sim.conf`. With both fixes, `west build -b native_sim app` builds and links, and feeding a `Hello` frame in via `-uart_stdinout` produces the full expected `HelloAck` + demo `BleAdvertise`/`StreamStart`/`StreamChunk`×3/`StreamEnd`/`LogLine` sequence; `west build -b native_sim app/tests/serial_protocol` builds and all 8 ztest cases pass. `workspaces/nordic` remains unbuilt (needs real hardware + the NCS toolchain).
- 2026-07-30 — First implementation pass: `embarch-dev-bench` (previously empty) gained a shared `app/` C application — `serial_protocol.[ch]` (a hand-written COBS+postcard-compatible encoder/decoder for every current `DevBenchMessage` variant, since `embarch-study-designer`'s FFI surface doesn't cover this), a stubbed `study_ffi.[ch]` per decision 20, and `ble_bridge.h`/`ble_bridge_real.c`/`ble_bridge_stub.c` per decision 16 (the real side wires up `bt_enable`/`bt_le_adv_start`/`bt_unpair` for a fixed demo `BleAdvertise` action and decision 11's bond-clearing-on-Hello; `BleConnect`/`DataExchange` are left unimplemented, noted in-code) — plus a `main.c` implementing the `Hello`/`HelloAck` handshake and a fixed demo action+streaming sequence standing in for real per-`Step` dispatch (no `DevBenchMessage` variant carries an actual `Study` payload yet, a gap newly noted in §4). `app/tests/serial_protocol` adds ztest coverage for the hand-written wire format specifically (not re-deriving the format's own soundness, which is `embarch-study-designer`'s job per decision 16). `workspaces/native_sim` is a complete, buildable west manifest (vanilla Zephyr, pinned `v4.4.0`); `workspaces/nordic` is manifest-only (NCS pinned `v3.4.0`) and explicitly not built or run this pass, per decision 20 and this session's scoping. §2's tree gained two corrections found while making the layout actually work with `west`: each workspace's `west.yml` lives in a `manifest/` subdirectory rather than at the workspace root (`west init -l <dir>` places `.west/` next to `<dir>`, so `<dir>` must be a child of the intended topdir, not the topdir itself — placing it inline as originally drawn would have put `.west/`/`zephyr/`/`modules/` a level too high, shared between both workspaces, breaking decision 2's independent-topdir requirement); board-specific Kconfig fragments use Zephyr's standard flat-filename auto-detection (`boards/nrf54l15dk_nrf54l15_cpuapp.conf`, `boards/native_sim.conf`) rather than nested per-vendor subdirectories, needing no extra CMake wiring to be picked up. See the fifth-scoping-round entry directly below for the USB→UART design change this implementation work surfaced before any of the above was written.
- 2026-07-30 — Fifth scoping round, surfaced by implementation research rather than a design review: the nRF54L15 SoC has no USB device peripheral at all (confirmed against Zephyr's own devicetree source and Nordic's datasheets — USB only arrives with the newer nRF54LM20 family), so decision 6's original USB-CDC-ACM Core link and decision 12's custom VID/PID were both unimplementable on the chosen board. Revised decision 6: the Core link is a plain UART over the nRF54L15DK's existing on-board J-Link VCOM (the same physical mechanism already used for the DK's debug console) — ordinary Zephyr UART calls, no USB device stack, no SEGGER-specific firmware code. Revised decision 12: auto-detection moves to `embarch-core` matching the J-Link's own SEGGER VID (`0x1366`) plus a product-string/serial-number heuristic, dropping the custom `0xEBAC`/`0xDB01` VID/PID entirely since dev-bench firmware no longer owns a USB descriptor to put it in. New §4 open item: `embarch-core`'s side of that auto-detection logic isn't implemented, and may need to disambiguate more than VID+product-string if the J-Link exposes more than one VCOM interface. Board choice (decision 4) and every other locked-in decision are unaffected — this is a link-mechanism fix, not a board or architecture change.
- 2026-07-30 — Fourth scoping round, closing four implementation-blocking gaps identified while reviewing the doc for build-readiness: `study_ffi.c` starts as a stub and is swapped for the real FFI staticlib once decision 8's west-module wiring exists, rather than blocking all implementation on that unsolved cross-compile problem (new decision 20); locked concrete USB VID `0xEBAC`/PID `0xDB01`, unregistered but accepted since dev-bench never ships as a product (decision 12); `HelloAck`'s firmware-identity field is a single free-form `firmware_version` string, not a structured type (decision 18); `native_sim`'s BLE stub is a separate `ble_bridge_stub.c` file selected per-workspace by CMake, not an `#ifdef` branch inside one `ble_bridge.c` (decision 16, also renamed the real-hardware file to `ble_bridge_real.c` in §2's tree). `LogLine` and `firmware_version` are now noted in §4 as sharing one future schema-version bump in `embarch-study-designer` rather than two.
