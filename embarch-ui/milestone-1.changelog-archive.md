# milestone-1.md: changelog archive

Entries beyond the 8 most recent, moved here from [milestone-1.md](milestone-1.md)
by `scripts/archive-changelog.py`, per `DOC-PROTOCOL.md` §5. Newest-first,
same as the live doc's own Changelog.

- 2026-08-24 — **Milestone CLOSED: the user confirmed the installed VS Code extension works** ("It did work perfect") — start/stop/open-in-browser all function correctly in their actual Windows VS Code (Remote-WSL, attached to the WSL2 instance this session installed into). This was the last open item in §5's Definition of Done; every checklist item is now met.
- 2026-08-24 — **4.8 closed for real, same day: Node installed portably, extension built/packaged/installed into a real VS Code, distribution decided.** No `node`/`npm` existed in this sandbox — installed a portable Node build to `~/.local/node` (user-local, no root). `npm install && npm run compile` clean; `npx @vscode/vsce package` initially refused over a broken relative `SEE LICENSE IN ../LICENSE` field, fixed by copying `LICENSE` into the subdirectory and using a plain `"MIT"` field, then produced a real `.vsix`. Distribution resolved via `AskUserQuestion`: internal `.vsix` only, never the Marketplace. Installed for real via `code --install-extension` (confirmed via `--list-extensions`) rather than a disposable Extension Development Host, since this environment's remote-CLI `code` shim doesn't support `--extensionDevelopmentPath` — a real install exercises the chosen distribution path directly. One piece left needing the user's own interaction: clicking through start/stop/open-in-browser in a running editor window.
- 2026-08-24 — **Steps 4.8/4.9 done — VS Code launcher extension scaffolded, and the three surfaces this project replaces retired.** 4.8: a real TypeScript extension (`embarch-ui/vscode-extension/`, resolved as a subdirectory not a sibling repo) — start/stop the `embarch-ui` subprocess, open the system browser, a status-bar toggle, settings for binary path/host/port/config path/auto-start; not build/run-verified (no `node`/`npm` anywhere in this sandbox — resolved in the very next changelog entry above). 4.9: deleted `embarch-topology`'s `bin/ui.rs` (413 lines) and its `Ui` subcommand (kept every read-only function it called: `list_enrolled`, `recent_alerts`, `list_attached_probes`, etc.; dropped `axum`/`futures-util`/`tokio-stream`/multi-thread-tokio from the `bin` feature since nothing under it serves HTTP any more), `embarch-study-designer`'s `tools/study_designer_ui.rs` and its `[[bin]]` target (kept `registry`/`merged_actions`/`study_builder`; narrowed the `study-ui` feature to drop `axum`/`tokio`/`serde_json`), and `embarch-core`'s `src/enroll_page.rs`/`GET /enroll` route (kept `POST /probes/enroll`; `build_router` no longer has a public/protected split, every route requires the bearer token). Each repo's own `design.md` updated in the same pass (`embarch-topology` decision 5's amendment plus a new open question for the now-orphaned `push_live`/`fix_it_url` marker-file mechanism; `embarch-study-designer` decision 34's re-reversal, §1's "not a service" stance restored; `embarch-core` decision 25's amendment, §4's endpoint table). `cargo build`/`test`/`clippy --all-targets -- -D warnings` clean in all three repos after the deletions (`embarch-topology` 34/34, `embarch-study-designer` 34/34, `embarch-core` 54/54), plus `embarch-api`/`embarch-umbrella` (both `embarch-topology` dependents) rebuilt clean against the trimmed `Cargo.toml`.
