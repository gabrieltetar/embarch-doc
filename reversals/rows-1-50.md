# embarch reversals: rows 1-50

**Status:** active, 2026-09-02.

Rows 1-50. Index, numbering rule and owner key: [../embarch-decision-reversals.md](../embarch-decision-reversals.md). Other ranges: [rows-51-72.md](rows-51-72.md), [rows-73-92.md](rows-73-92.md), [rows-93-109.md](rows-93-109.md).

| # | Assumed → what reality showed | Owner |
|---|---|---|
| 1 | Dev-bench's Core link would be USB CDC ACM → **the nRF54L15 has no USB device peripheral at all**; it is a plain UART over the onboard probe's VCOM | bench 6 |
| 2 | Detection would use a custom VID/PID → follows from row 1: **the firmware owns no USB descriptor to put one in** | bench 12 |
| 3 | The DK build would be non-sysbuild → **NCS wraps every application in sysbuild regardless**, so the image lands a directory deeper | bench 4 |
| 4 | `CONFIG_BT_MAX_CONN` would default to one connection → **it defaults to 2, and forcing 1 silently zeroes the central role's slots** — every central connect breaks with no build error | bench 15 |
| 5 | A cross-platform service crate gave Core one code path to become an OS service → **true only for *registering* it**; Windows also needs a prompt dispatcher handshake or the start is killed at 30 s. **Core had never actually been installed as a boot service** — every prior test used a throwaway foreground instance | core 3 |
| 6 | Service elevation was a Windows-only tax → **a systemd *system* unit needs root or polkit too** | core 3, umb 7 |
| 7 | A Zephyr board's declared boards/variants/revisions describe what is buildable → three real-repo bugs at once: **a declared combination can have no `.dts` at all**, a SoC with named variants can still have a real variant-less build the board file never surfaces, and "the default revision is always backed" is wrong for a variant with its own revision coupling | api 12 |
| 8 | `Cargo.toml`'s version tracks the release tag by construction → **it had silently drifted**, caught only by a hand comparison that day. Now a release-CI assertion | umb 27 |
| 9 | Elevation stays rare, so printing the command for a human to paste is acceptable friction → **updating an already-installed Core's binary has no supported path at all**, degrading to hand-run stop/copy/start surgery | core 3, umb 7 |
| 10 | The Windows-visible artifact path works however Core runs → **only with Core in an interactive-session foreground run**; the installed service has no access to the per-session network provider. **Every earlier "confirmed working" claim had only been tested against a foreground Core** | api, core 10 |
| 11 | `serial_log` against a DUT's own console was an intended goal, stated explicitly → **Core's serial link was only ever meant to reach dev-bench.** Nothing stops it; it was an unenforced possibility, not a supported goal | api 26 |
| 12 | Core "has no business flashing" dev-bench firmware — a permanent separation → **it stopped holding the moment a bench board arrived whose on-chip probe `probe-rs` already speaks**, flashed directly with no USB forwarding and no elevation | bench 13, core 18 |
| 13 | The bench board is the DK permanently; the C5 was interim → the DK broke, the milestone retargeted to the C5 → **counter-reversed, it is a DK again.** The Nordic workspace was kept on exactly that bet and built first try twelve days later. Rows 103–105 are what the round trip cost, all host-side | bench 43 |
| 14 | The vendor-VID heuristic would only disambiguate the bench's probe from noise → **a genuinely different same-vendor probe — the DUT's own — matches identically**, and Core correctly refused to guess rather than picking wrong | core |
| 15 | Core's rolling logfile and `logs` subcommand shipped, as the prose read → **neither existed**; stderr-only was the whole story. Same "documented as implemented, wasn't" pattern as two neighbouring decisions that session | core 16 |
| 16 | The C5's native USB-serial/JTAG, having worked once, was a viable permanent link → **a bad fit per the vendor's own docs**: core-only reset never re-samples boot straps, a real reset pulse wedges the host driver, and **the board ships a second dedicated UART port for exactly this reason** | core 21, bench 26 |
| 17 | Each capture kind gets its own channel, and dev-bench decodes payloads into samples → **one pipeline, and dev-bench never interprets a payload.** The bench had been decoding every notification as little-endian floats — **an assumption about what a DUT's bytes mean, living in firmware, that no engineer ever declared** | sd 39 |
| 18 | A schema bump number written into a doc ahead of implementation stays valid → **two decisions written the same day were implemented first and took those numbers.** **A *derived* value recorded ahead of the work that derives it is a fact with a shelf life, unlike the decision it sits inside** | sd 39 |
| 19 | An `--erase` flash is a stronger, safer flash needing no validation of its own → **it bricks the nRF54L15**, four runs for four, recoverable only by the vendor's own erase-all. **What the erase clears beyond the declared NVM region was never established**, so the correlation is four-for-four and the mechanism is still a guess | core 32, api 41 |
| 20 | dev-bench needs no BLE pairing callbacks — in the firmware's own header as settled design → **both halves fail against a DUT requiring an authenticated link, and this one does.** With no auth callbacks the stack reports no I/O capability and **refuses the level outright, regardless of what a study asks.** From the bench it looked like a deterministic "disconnected during service discovery": **a failed elevation is invisible to a bench with no concept of elevation** | bench 34, sd 44 |
| 21 | Widening a seal "would invalidate every saved study's sealed value" → **no saved seal is ever trusted**; the API recomputes and overwrites on every submit. **The stated objection did not exist.** A second seal still won, on the real objection: the first sits *between* the two spans on the wire | sd 39 |
| 22 | One schema constant guards one thing, so one trigger list serves it → **it guards two hops with different exposure, and the list was wrong at both ends.** Dropping validations from it — the first answer reached — **would have created a real undetected failure**, since the study *including* validations crosses api→Core with that constant as its only drift check | sd 12 |
| 23 | A step's power-sample field and a power tap were two competing ways to author a capture → **only one was ever live**; the field is vestigial in every consumer. **No competition to adjudicate, only a field to retire.** Found by grepping four repos rather than reasoning from the type model | sd 39 |
| 24 | Recording a signal mismatch durably was owed work → **nothing can raise one.** The validation surface had no caller, no signal was declarable, no direct route was physical. **The durable half was queued ahead of anything that could produce the alert** | topo 18 |
| 25 | A second seal costs dev-bench "one varint read and one CRC over a span its decoder already walks" → **the decoder walks no such span** — the field was appended *after* the seal precisely so the C decoder could stop early. **An append chosen so a decoder need not walk a field is what makes a later seal over it expensive**: +654 bytes | sd 39, bench 29 |
| 26 | Both-languages wire pinning covered the records that needed it → **it covers *new* records, and the message dev-bench sends most is not new.** In that gap the C encoder wrote two option bytes for fields the Rust type had retired a version earlier. **Both suites stayed green, because each language's round trip only proved that language self-consistent** | bench 29, sd 39 |
| 27 | A signal tap's port opens "at study start", like every other tap's announced lifetime → **the wire's open signal is announced *by dev-bench*, precisely the node a direct route bypasses.** The one tap Core opens itself is the one nothing tells Core to open. **A decision borrowed a lifetime trigger from the sources it was defining itself against** | core 30(a) |
| 28 | Core's erase shells out to the vendor tool, the sector-erase alternative having been declined → **the rejected option is what is in the tree.** **A decision written in the same session that considered both recorded the conclusion of the argument rather than the state of the tree** | core 32 |
| 29 | The watchdog window is the step timeout plus grace → **it never accounted for the step's own declared pre-delay**, which the bench sleeps *before* the step. **That is the intended path, not an edge case** — the delay exists to make stimulus timing authorable. Invisible because every fixture used zero. **The field came from one milestone and the watchdog from another** | core 33 |
| 30 | Shrinking the firmware's max-steps constant is a local memory mitigation → **it silently forked the suite's step limit**, so a study passes every host check and is refused on the wire, **reconciled by nothing.** A capacity constant is a contract when both sides have one | bench 35 |
| 31 | The alert push and fix-it URL degrade gracefully with no marker file, so deleting the UI left them inert → **"harmlessly inert" was doing a lot of work**: the URL is now guaranteed dead rather than dead-unless-someone-started-the-UI. **Graceful degradation hid a retirement** | topo 19 |
| 32 | Open-questions sections describe what is genuinely unresolved → **two had been answered by shipped work and never closed. The pre-close grep and the staleness check both watch *status tables*, and neither looks at open-questions bullets** | DOC-PROTOCOL §4 |
| 33 | Post-hoc validation is shipped: designed in full, evaluators behind a Cargo feature Core enables, fields on the wire both ways → **nothing ever ran it.** Core's writer emitted a hardcoded empty array for the feature's entire life, at a cost of **97% of the struct it sat in.** A feature can be fully typed, wired into two repos, enabled by a feature flag, advertised to every agent that ever called it, **and still be inert** | sd 48 |
| 34 | The stack-overflow risk was fixed by heap-allocating the field that had crashed a debug build → **it fixed the smaller half**; the *result* type was 1,293,608 bytes and the real release service overflowed on that path. **Picking the field with a reproduction attached, rather than measuring every field of the same shape, left the biggest instance in place** | sd 49 |
| 35 | dev-bench still interpreted stream payloads — a decision listed four such places and a milestone item was priced around deleting them → **all four were already gone.** The item was purely additive and its net-negative-firmware expectation could not be met. **The stale claim sat in the exact place a reader would go to find out what was left to do** | bench 29(a) |
| 36 | The reserved log tap is dev-bench's to open → **Core could not have accepted it**: tap ids resolved against the *submitted* study, so a bench-invented tap would be undeclared and every record dropped. **Chosen instead of firmware: Core synthesizes the tap and renders the log frames it already receives** — no second channel, no new SRAM on a board 98.5% full | sd, core 30 |
| 37 | A trace tap declares the manifest CRC it was authored against → **the firmware cannot report one**, since the manifest is generated *from the linked image*. The field survived a rework that had already replaced it with a build ID — **and it is the write-ahead staleness pattern the topology crate exists to eliminate**, which that rework spent three paragraphs distinguishing itself from | outpost 9, sd 39 |
| 38 | One Kconfig symbol selects the four it needs, so it cannot be half-enabled → **one of the four is a *choice* member, and nothing outside a choice can select one.** Select warns and does nothing; so does the after-the-tree default; and reopening the choice is impossible because module Kconfig is sourced first. **Both were tried.** The *property* survives as an `#error`; the symbol count does not | outpost |
| 39 | A record is roughly 8 bytes, against the 29 that rejected the alternative → **roughly 12 under this design's own encoding**: a varint-encoded absolute counter spends its life needing five bytes, and a RAM pointer five more. **The rejection stands; the number quoted in the comparison was optimistic** | outpost 4 |
| 40 | A gap record orders like any other, so a backwards step means a counter wrap → **a gap record is the one record whose timestamp can precede the records printed after it**, and a naive unwrap turns that into a 2^32 jump. It is stamped when its losses *started* and emitted when the ring had room, **and a FIFO ring cannot make those the same moment.** 99 of them threw the first real capture out by 4.29 s each | outpost 5 |
| 41 | The marker table is in the image, so the generator can read it from the ELF → **nothing in the firmware reads it, so the linker garbage-collects it — and the build still succeeds**, yielding zero markers with no error to say why. The obvious iterable-section API **has no effect on the template linker-script path both real targets use**, and a keep directive is collected out again by the host link | outpost 6 |
| 42 | The software ISR table resolves an IRQ to its handler, defeated only by the three named variants → **none of the three is present, and a fourth the design never named defeats 8 of 13 IRQs**: the vendor's glue registers one shared trampoline per peripheral and passes the real handler as the entry's argument. **The same ELF read, one field over** | outpost 7 |
| 43 | The shared crate's `alloc` feature works; every host consumer exercises it → **building it alone failed outright, and had since the feature existed.** **Every real consumer unified that feature from a different dependency, so the crate's own single-feature build was the only configuration that could see it** — exactly what a feature matrix is for | sd 46 |
| 44 | A hand-mirrored firmware constant was correct, and the comment noting it had been four bumps stale would keep the next reader honest → **it went stale again one bump later. A note describing a gap is not a mechanism for closing one.** The build now reads the number out of the crate's own source | bench 20 |
| 45 | An unverified trace "is not rendered at all, only reported" → **too strong, and the previous phase had settled something better a day earlier**: the rows are written *unnamed* with the reason recorded, **because a timeline of numeric pointers is a real answer and is honestly distinguishable from a named one.** The requirement is **never render it as a *named* trace** | ui 10(b), core 30(c) |
| 46 | The live Core's signal route could be exercised safely — it is a read → **the live service answers `404`**, predating the whole phase. Discriminated by response shape rather than guessed. **The gap it exposed was worth more than the workaround:** folding that `404` into an empty list **would have made the tab's first sentence a false statement about the wiring** | ui 10 |
| 47 | A gap is a hole, so drawing it as a gap means drawing an empty interval → **sixteen surviving records fall inside the first band of the committed fixture.** The ring drops some of a window, not all of it, so the band is an **overlay** over what survived. **Drawing the hole would have erased real records to make the picture tidier** | ui 10(d) |
| 48 | The fix-it URL has had no live destination since the UI binary was deleted — carried into a plan as a trap → **already fixed four days earlier, with a test pinning the string. A stale premise carried into a plan reads exactly like a live one, and the only thing that separated them was running `grep` before believing it** | topo 19 |
| 49 | Capture could start once both boards were attached, the bridge being "the third VID-matching device on this bench" → **there is no host-visible path from the DUT's trace UART at all.** Established by construction: a build was flashed, **proved to be running** over BLE, and every port watched across a reset. The bridge returned only zeros at every baud — **a line held low, not a baud mismatch, since a mismatch yields varied garbage** | outpost 16 |
| 50 | The static GATT extraction is a bounded read that "fails loudly rather than silently under-extracting" → **it had been missing a third of that DUT's table the whole time**, a third service in a file it was never handed whose macros were in the header it already opened. **The loudness was real for everything inside its scope and vacuous about its scope, which is the part a bounded read structurally cannot fix** | sd 57 |
