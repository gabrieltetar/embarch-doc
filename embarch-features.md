# embarch: features

**Status:** draft, 2026-07-17. Hand-maintained. Todo rows should stay in sync with the source design docs' own "open questions" sections rather than duplicating their detail — link back, don't restate.

**Verification column** (added 2026-08-15, closing item 62 of that day's design-improvement review — `.claude/design-improvements-2026-08-15.md`, local working notes): replaces the "not yet hardware-revalidated"/"never against a real Core" qualifiers that used to live only in prose, scattered across almost every row's Status/Notes cell. One of: **unit** (mocked/synthetic tests only, no live process), **live-local** (a real running Core/repo/CI on this machine, but no physical debug probe or board involved), **real-hardware** (validated against an actual physical probe, board, or a genuine OS service install), or **n/a** (not shipped yet).

| Feature | Sub-project | Status | Verification | Notes |
|---|---|---|---|---|
| `GET /status` — list connected debug probes | embarch-core | Shipped | real-hardware | `embarch-core/design.md` §4 |
| `POST /flash` — flash a chip from a local firmware path | embarch-core | Shipped | real-hardware | `embarch-core/design.md` §4 |
| `POST /reset` — reset a chip | embarch-core | Shipped | real-hardware | `embarch-core/design.md` §4 |
| `GET /serial-log` — bounded-duration serial capture | embarch-core | Shipped | real-hardware | `embarch-core/design.md` §4 |
| `GET /dev-bench/port` — dev-bench serial-port auto-detection (SEGGER *or* Espressif VID + product/serial/interface match), plus the `detect-dev-bench` CLI subcommand | embarch-core | Shipped, real-hardware-confirmed (ESP32-C5, Milestone 2) | real-hardware | `embarch-core/design.md` §4, §5, §10; implements `embarch-dev-bench/design.md` §3 decision 12 |
| `POST /resolve-chip` — Zephyr SoC name → probe-rs chip target string, validated against probe-rs's own target registry | embarch-core | Shipped | live-local | `embarch-core/design.md` §3 decision 8, §4; `embarch-api/design.md` §3 decision 12 |
| Bearer-token auth on all endpoints | embarch-core | Shipped | real-hardware | `embarch-core/design.md` §6 |
| `hw_lock` — serializes all hardware access | embarch-core | Shipped | unit | `embarch-core/design.md` §3.4 |
| Cross-platform service install/control (`install`/`uninstall`/`start`/`stop`) | embarch-core | Shipped — all four need elevation on every OS, not just Windows | real-hardware (Windows, Linux); macOS untested | `embarch-core/design.md` §3.3 |
| `list_projects` MCP tool | embarch-api | Shipped | live-local | `embarch-api/design.md` §5 |
| `status` MCP tool | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §5 |
| `build` MCP tool | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §5 |
| `flash` MCP tool | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §5 |
| `build_and_flash` MCP tool | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §5 |
| `reset` MCP tool | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §5 |
| `serial_log` MCP tool | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §5 |
| `list_targets` MCP tool + `list-targets` CLI subcommand — live target discovery for a `discovery = "zephyr-west"` project, or a `[[projects.targets]]` menu for a `static` one | embarch-api | Shipped | live-local | `embarch-api/design.md` §3 decision 12, §5, §5a; not yet revalidated against the real reference-dut repo itself |
| `discovery = "zephyr-west"` — live, per-call `board`/`variant`/`revision`/`app` resolution (file-backing-validated against real overlay/defconfig files) on `build`/`flash`/`build_and_flash`/`reset`, instead of a static hand-maintained `[[projects]]` entry | embarch-api | Shipped | live-local | `embarch-api/design.md` §3 decision 12; `embarch-umbrella/milestone-6.md` §3.9; not yet revalidated against the real reference-dut repo itself |
| CLI subcommand interface (`build`/`flash`/`build-and-flash`/`reset`/`serial-log`/`list-projects` — kebab-case, unlike the snake_case MCP tools above) | embarch-api | Shipped | real-hardware | `embarch-api/design.md` §3.10, §5a |
| Artifact freshness check (mtime before/after build) | embarch-api | Shipped | unit | `embarch-api/design.md` §6; WSL2 wall-clock jitter fix applied 2026-07-22 (§12), not yet hardware-revalidated |
| Per-project build concurrency lock | embarch-api | Shipped | unit | `embarch-api/design.md` §6 |
| PATH/toolchain preflight validation | embarch-api | Todo — now expected to land as `embarch-umbrella`'s `doctor` check, not in the build path | n/a | `embarch-api/design.md` §12, §11a; `embarch-umbrella/design.md` §5 |
| Config hot-reload | embarch-api | Todo | n/a | `embarch-api/design.md` §12 |
| `artifact_path_for_core` UNC-path pass-through (WSL2-same-PC artifact transfer) | embarch-api | Shipped, validated against real hardware | real-hardware | `embarch-api/design.md` §4, §9; not yet populated for `project-a-board` specifically — see §12 |
| Artifact-transfer over a real network (Core on a genuinely separate machine, e.g. a future Pi) | embarch-api / embarch-core | Todo — design resolved 2026-08-15 (multipart upload), not yet implemented | n/a | `embarch-api/design.md` §3 decision 15; `embarch-core/design.md` §3 decision 10 |
| Multi-probe selection (beyond "first probe found") | embarch-core | Todo — design resolved 2026-08-15 (`probe_serial` selector), not yet implemented | n/a | `embarch-core/design.md` §3 decision 9, §5, §10 |
| ESP-IDF UART-bootloader flashing via `esptool` fallback | embarch-core | Todo — explicitly out of v1 scope as of 2026-08-15, no real ESP-IDF project driving it | n/a | `embarch-core/design.md` §10 |
| Per-caller identity (beyond one shared token) | embarch-core | Todo | n/a | `embarch-core/design.md` §6, §10 |
| Auto-generated, machine-wide token file (replaces `dev-token-change-me` fallback) | embarch-core | Shipped, Windows hardware-unvalidated | unit | `embarch-token.md` §2, §3.1, §5; `embarch-core/milestone-2.md` §3.5 |
| `sc.exe` service `EMBARCH_TOKEN` passthrough fix | embarch-core | Shipped, Windows hardware-unvalidated | unit | `embarch-token.md` §6; `embarch-core/milestone-2.md` §3.4/§3.5 |
| Token-file discovery + WSL2⟷Windows path translation | embarch-api | Shipped — WSL2⟷Windows translation exercised for real; end-to-end re-check against a live Core-generated file not yet done | live-local | `embarch-token.md` §3.1; `embarch-api/milestone-2.md` §3.5 |
| Stimulus/sensing hardware-in-the-loop rig | embarch-dev-bench | Proposed | n/a | See `embarch-roadmap.md` Next |
| Shared `app/` firmware: COBS/postcard serial protocol, `Hello`/`HelloAck`, `LogLine`, real per-`Study` dispatch | embarch-dev-bench | Shipped, real-hardware-confirmed (ESP32-C5, Milestone 2) | real-hardware | `embarch-dev-bench/design.md` §2, §3 decisions 7/16/20/21 |
| BLE bridge: advertise, connect (central/peripheral), GATT discover / monitor-all / monitor-selected, read/write/notify/indicate/subscribe/stream-capture | embarch-dev-bench | Shipped — `BleAdvertise` (Milestone 2), `BleConnect`/`GattDiscover`/`GattMonitorAll` all run for real against a real independent DUT (Milestone 3, 2026-08-24); `GattMonitorSelected`/`GattMonitorSelectedStart` added 2026-08-26 (schema v14) and **not yet run on hardware**; `DataExchange`'s targeted read/write/notify/indicate/stream-capture still compile-only, never exercised against a real characteristic | live-local, real-hardware | `embarch-dev-bench/design.md` §3 decision 16/26/27/40, §4 |
| Per-characteristic GATT capture: a tap per characteristic, decoded against an engineer-declared struct layout, plus an uncapped GATT transcript | embarch-study-designer, embarch-core, embarch-dev-bench, embarch-ui | Shipped 2026-08-26 (schema v14) — authorable in the Study Designer, rendered by Core into `streams/<tap>.csv`; **not yet run against real notification traffic** | live-local, real-hardware | `embarch-study-designer/design.md` §3 decisions 52–55, §4.8a |
| Characteristics **and services** named, not numbered, in every Study Designer picker: vendor names plus the C identifier the firmware declared each one under | embarch-study-designer, embarch-ui | **Shipped and validated 2026-08-26** against the real `reference-dut-fw` checkout — **18** characteristics and 3 services named from source with no engineer input (15 and 0 at first ship; the rest arrived with the repo-wide scan below), since `gatt_extract` already parsed the identifiers and was discarding them; vendor names win where both apply. Labels only, never semantics: the suffix that names a *variable* is trimmed and nothing else, and the UUID stays the identity in every tooltip and every payload | unit, live-local (headless-browser render against a running UI) | `embarch-study-designer/design.md` §3 decision 56 (amended), §4.3a; `embarch-ui/design.md` §3 decisions 16/17 |
| Static GATT extraction scans the whole firmware repo, scoped by that repo's own `.gitignore`, and reports what it scanned | embarch-study-designer, embarch-ui | **Shipped and validated 2026-08-26.** Replaces a hardcoded two-file read that had been missing a third of the reference DUT's GATT table (`lib/bds/bds.c`'s `sensor_bds`) for as long as it existed, with nothing able to say so. The ignore-aware walk is load-bearing, not tidy: a naive `**/*.{c,h}` glob finds `BT_GATT_SERVICE_DEFINE` six times on that repo — `.claude/worktrees/` holds two whole extra copies — which fits under the cap and would have emitted duplicated services silently. Plus a hard block on any directory named `embarch`, a `ScanReport` the extractor returns, and named `DuplicateService`/`AmbiguousSymbol`/`NoSourceFilesFound` failures. Real result: 3 services / 18 characteristics, up from 2 / 15 | unit (incl. a real temp-tree walk asserting gitignore + hard block + hidden-dir exclusion), live-local | `embarch-study-designer/design.md` §3 decision 57, §4.3a |
| Picking selective-monitor targets from a grouped dialog rather than a wall of inline checkboxes | embarch-ui | **Shipped and validated 2026-08-26.** The step row is one line whatever the DUT's table looks like; the picking happens in the `.dialog` pattern four other places already use, grouped by named service with per-group all/none and a filter. `MAX_MONITOR_TARGETS` is served rather than restated in the browser, and is now visible and enforced where the choice is made instead of only as a `400` after a round trip. Fixed two silent losses found on the way: a saved study's `targets`/`security_level` were discarded on reopen, and a target the current discovery no longer knows about was dropped without a word at submit | live-local (headless-browser: collapsed cell, open dialog, filter, at-cap, empty-selection refusal, stale-target warning) | `embarch-ui/design.md` §3 decision 17 |
| GATT capture window spanning steps (`GattMonitorStart`/`GattMonitorStop`) | embarch-dev-bench, embarch-study-designer | Shipped; flashed and run on hardware — `GattMonitorStart` passes against the real DUT | unit, ztest, real-hardware | `embarch-study-designer/design.md` §3 decision 36, §4.3; `embarch-dev-bench/design.md` §3 decision 28 |
| Exhaustive streamed GATT transcript (`streams/<tap>.csv`, `GET /study/{id}/stream/{name}` with `/gatt-data` as an alias, `study_gatt_data`) | embarch-dev-bench, embarch-core, embarch-api | Shipped; real transcript captured from the real DUT (connect + discovery + outbound write, both directions, per step) | unit, ztest, cross-language wire-byte contract, real-hardware | `embarch-study-designer/design.md` §3 decision 36, §4.3b, §5.2 |
| Vendor-defined GATT service catalog (`vendor.rs`, Nordic UART Service by id) | embarch-study-designer, embarch-ui | Shipped; resolves to the real NUS UUIDs end to end, surfaced as a UI option group | unit, real-hardware (submitted and encoded) | `embarch-study-designer/design.md` §3 decision 41 |
| Authored step timing (`Step.delay_before_ms`, UI "Wait first (ms)") | embarch-study-designer, embarch-dev-bench, embarch-ui | Shipped; honored by firmware on hardware | unit, ztest, real-hardware | `embarch-study-designer/design.md` §3 decision 42 |
| Connect to a named DUT (`Action::BleConnect.target_name`, advertised-name scan filter) | embarch-study-designer, embarch-dev-bench, embarch-ui | Shipped; filters correctly on hardware and reports the names actually on air when nothing matches | unit, ztest, real-hardware | `embarch-study-designer/design.md` §3 decision 43; `embarch-dev-bench/design.md` §3 decision 32 |
| Dev-bench `CONFIG_LOG` forwarded to Core and stored in a debug file (`dev-bench.log.<date>`, `embarch-core dev-bench-logs`), at a verbosity **each study asks for** (`Study.dev_bench_log_level`) | embarch-study-designer, embarch-dev-bench, embarch-core | Shipped, live-validated on the real ESP32-C5 bench against the deployed Windows service — a boot log record held across reset, flushed at handshake, framed as a `LogLine`, and stored with its `<inf>` level marker intact, plus a completed study's boundary markers. Verbosity went per-study the same day (wire **v13** / host **v15**): everything compiles in at `DBG` and is filtered at runtime, applied on dispatch and reverted on study end *and* `Hello`, with the app's own module held at `INF` unless a study says `Off`. The `LogLine` carrier itself needed **no** schema change; the one ordering constraint is that Core ships before the firmware is flashed, since only the new Core tolerates a `LogLine` ahead of `HelloAck`. Net **+736 bytes** of ESP32-C5 SRAM, because the pass also reclaimed ~10.5 KB from an inbound buffer sized for a message that cannot arrive ([embarch-decision-reversals.md](embarch-decision-reversals.md) rows 59–61) | ztest (6 new cases), unit, real-hardware | `embarch-dev-bench/design.md` §3 decision 38 (amending decision 7); `embarch-core/design.md` §3 decision 37 |
| Interrupt-driven inbound link RX (frames larger than the UART FIFO) | embarch-dev-bench | Shipped; fixes a silent 128-byte ceiling that dropped every larger `StudyStart` | ztest (incl. Core's real wire bytes), real-hardware size sweep | `embarch-dev-bench/design.md` §3 decision 30 |
| BLE security elevation as an authored step (`Action::BleSecurity { level }`, L1–L4) | embarch-study-designer, embarch-dev-bench, embarch-ui | **Shipped and hardware-validated, 2026-08-26** (wire schema v12) — dev-bench registers DisplayYesNo-class pairing callbacks and auto-confirms, so LE SC Numeric Comparison is selected and L4 is reachable; the **achieved** level is reported per step in `StepResult.security_level`, and a step that lands short fails. Against the real DUT: L4 reached, and a `GattDiscover` that had never once succeeded returned 7 services. The L4 it reaches carries **no real MITM protection** — nothing compares the passkey — which is the right trade for an unattended bench and is recorded so a result saying `L4` is not later read as a verified peer | n/a | `embarch-study-designer/design.md` §3 decision 44; `embarch-dev-bench/design.md` §3 decisions 34/37; `embarch-ui/design.md` §3 decision 12 |
| Dropping the bond mid-study (`Action::BleUnbond`), with a bond scoped to one study | embarch-study-designer, embarch-dev-bench, embarch-ui | **Shipped, 2026-08-26** (same wire bump) — "pair, do work, drop the bond, pair again" is authorable, and dev-bench clears bonds at study end explicitly rather than relying on the next `Hello`. Unbonding **disconnects**, because Zephyr's `bt_unpair` does; a study that unbonds needs its own `BleConnect` afterwards | n/a | `embarch-study-designer/design.md` §3 decision 50; `embarch-dev-bench/design.md` §3 decisions 11/37 |
| Opening a firmware repo from the Study Designer, and creating a study in it | embarch-ui | **Shipped and live-validated, 2026-08-26** — a typed path with server-side validation plus a recent-projects list, because a browser has no directory picker that yields a usable path. The `[study_designer].firmware_repo_path` config field stays the zero-click default; its absence no longer makes the tab unreachable. Switching projects moves the studies directory, the action registry and the static extractor together | n/a | `embarch-ui/design.md` §3 decision 14 |
| One-command deploy of a local `embarch-core` onto the live Windows service (`embarch deploy-core`) | embarch-umbrella | **Shipped and dogfooded, 2026-08-26** — sync, native build, stop/copy/start under one elevation, then **verify the binary actually changed**, because the supported `update` path exits `0` having done nothing when its UAC prompt never renders | n/a | `embarch-umbrella/design.md` §3 decision 32; `embarch-dev-workflow.md` §4a |
| Declared GATT table on a `Study`, reconciled against live discovery (`Study.gatt`, `GattSource`) | embarch-study-designer, embarch-core, embarch-ui | **Design-only, no code** — joins three sources of GATT knowledge that were never joined (`vendor.rs`, `gatt_extract.rs`, the registry) so "is this service actually on this build?" becomes answerable | n/a | `embarch-study-designer/design.md` §3 decision 45 |
| Erase on flash (`POST /flash` `erase`, `--erase`, MCP `erase`) | embarch-core, embarch-api | **Shipped, and the hazard is closed as of 2026-08-27 — no backend maps `erase` to a chip erase any more.** probe-rs chip-erase bricked the nRF54L15 (measured twice each way; recoverable only by `nrfutil` ERASEALL). The delegation decision 32 chose was recorded as shipped for two days while the tree held the option it had rejected (row 55); it is now built, as part of the wider per-family backend choice — nRF Util gets `ERASE_RANGES_TOUCHED_BY_FIRMWARE`, `nrfjprog` `--sectorerase`, J-Link a plain `erase`. **Unvalidated on hardware**: the DUT is unresponsive | real-hardware (the failure) | `embarch-core/design.md` §3 decisions 32, 36; `embarch-api/design.md` §3 decision 41; `embarch-decision-reversals.md` rows 19, 54, 55 |
| Flashing backend chosen per chip family, with the board's own runner preferred (`flash-backend` subcommand, `EMBARCH_FLASH_BACKEND`) | embarch-core | **Shipped 2026-08-27, unvalidated on hardware.** Core no longer flashes every target with probe-rs: for the `nrf54l` family — code in RRAM, which probe-rs models as one flat NVM region with no RRAM-aware erase/write — it **refuses** probe-rs and runs the vendor tool the board's own `board.cmake` already names (`nrfutil`, then `jlink`, then `nrfjprog`), or fails with an error naming all three, their install URLs, and the override variables. Tools are detected, never bundled (licensing). Every other family is unchanged | real-hardware (the failure that prompted it) | `embarch-core/design.md` §3 decision 36 |
| `DataExchange` `Read` result observability | embarch-dev-bench | **Open question, not a diagnosed gap** — one real run whose `Read` step reported `Pass` produced a `read_request` row in `gatt.csv` but no `read_response` row, and a null `captured_data`. The emit path exists (`ble_bridge_real.c` emits `BLE_GATT_EVT_READ_RESPONSE` with the value, and `GattEventKind::ReadResponse` is in the schema), so this needs diagnosis rather than implementation | real-hardware (observed once) | `embarch-roadmap.md` Milestone 6 |
| Free-text/raw GATT payload authoring alongside the registry (`RowAction::Raw`, `Uuid::parse`) | embarch-study-designer, embarch-ui | Shipped | unit | `embarch-study-designer/design.md` §3 decision 37, §4.3c |
| Saved-study library (`<firmware-repo>/embarch/studies/*.json`, also a `run-study --study-file` input) | embarch-ui, embarch-study-designer | Shipped — save/list/load verified against the real reference-dut repo, and the saved file accepted by the redeployed Core | live-local | `embarch-study-designer/design.md` §3 decision 38 |
| Study Designer tab UI (editable step table, registration form, discovery, run-and-watch, transcript download) | embarch-ui | Shipped 2026-08-25 — **the 2026-08-24 entry claiming this was already done covered its backend only; the frontend did not exist** | live-local | `embarch-ui/design.md` §6 Changelog; `embarch-ui/milestone-1.md` §4.6 |
| Shared `no_std` study data-types library (BLE interaction + power profiling) | embarch-study-designer | Shipped — a real Cargo dependency of both `embarch-core` and `embarch-api` as of Milestone 2 | unit, real-hardware | `embarch-study-designer/design.md` §3, §4; `embarch-study-designer/milestone-3.md` |
| `embarch-core` ↔ `embarch-dev-bench` serial bridge (`/study*`, `/dev-bench/hello`) | embarch-core | Shipped, real-hardware-confirmed (ESP32-C5, Milestone 2) | real-hardware | `embarch-core/design.md` §4, §8; `embarch-study-designer/design.md` §5 |
| `run_study`/`study_status`/`study_power_data`/`study_waveform_data`/`study_gatt_data` MCP tools + CLI subcommands | embarch-api | Shipped, real-hardware-confirmed (ESP32-C5, Milestone 2) — both CLI and MCP paths | real-hardware | `embarch-study-designer/design.md` §6; `embarch-api/design.md` §5, §5a, decision 31 |
| `doctor` check 13 — stale dev-bench firmware (`HelloAck.firmware_version` vs. local `git describe`) | embarch-umbrella | Shipped, real-hardware-confirmed (ESP32-C5, Milestone 2) | real-hardware | `embarch-umbrella/design.md` §3 decision 19, §5; `embarch-dev-bench/design.md` §3 decision 25 |
| `embarch setup` — one-time per-machine setup with topology auto-detection | embarch-umbrella | Shipped — smoke-tested on WSL2 for all three classes; the Windows-side install command has never been run | live-local | `embarch-umbrella/design.md` §3 decisions 3/6/7, §8 |
| `embarch init` — scaffold a firmware repo's `embarch/` config + local MCP registration, touching nothing tracked | embarch-umbrella | Shipped — verified against a repo shaped like the real one; run for real against the reference-dut repo | live-local | `embarch-umbrella/design.md` §3 decisions 10/12/13, §7 |
| `embarch init`/`doctor` support for `discovery = "zephyr-west"` — scaffolds the minimal schema instead of guessing one board; `doctor` checks 7-9 branch on `discovery` | embarch-umbrella | Shipped | live-local | `embarch-umbrella/design.md` §3 decision 17; `embarch-umbrella/milestone-6.md` §3.9; not yet revalidated against the real reference-dut repo itself |
| Topology auto-detection (ordered loopback → WSL2 gateway → explicit host, `401` counts as finding Core) | embarch-umbrella | Shipped — verified on WSL2 and against a mock, never against a real Core | live-local | `embarch-umbrella/design.md` §3 decisions 6/15; `embarch-umbrella/milestone-6.md` §3.2 |
| `embarch status` — where Core is, `--json` | embarch-umbrella | Partial — reports reachability, address, and topology class; no probe list and no token check yet | live-local | `embarch-umbrella/design.md` §3 decision 11 |
| `embarch doctor` — verify the whole chain, `--json` | embarch-umbrella | Shipped — all thirteen checks plus `--json`; smoke-tested against a live Core and the real reference-dut repo's config, check 13 also live-confirmed against real dev-bench hardware (Milestone 2) | live-local | `embarch-umbrella/design.md` §5; `embarch-umbrella/milestone-6.md` §3.4 |
| `embarch up` / `down` — fallback Core start/stop, including across the WSL2⟷Windows boundary | embarch-umbrella | Shipped — never started a real Core; the WSL2→Windows path prints the command rather than elevating | live-local | `embarch-umbrella/design.md` §3 decisions 4/7 |
| Suite release archive (all three binaries, four targets) | embarch-umbrella | Shipped — real `v0.1.0` releases across all three repos, plus a real assembled `suite-v0.1.0` archive that `doctor` check 1 has validated a manifest against | live-local | `embarch-umbrella/design.md` §3 decision 14; `embarch-umbrella/milestone-6.md` §3.7 |
| `base_url = "auto"` — Core address resolved per-process at first use, retiring the stale WSL2 gateway IP | embarch-api | Shipped — verified against a mock and with Core down; never against a real Core | live-local | `embarch-api/design.md` §3.11, §4, §7 |
| `start` / `stop` CLI subcommands | embarch-core | Shipped — smoke-tested on Linux, not on Windows/macOS | real-hardware (Linux); Windows/macOS untested | `embarch-core/design.md` §2, §3.3; `embarch-umbrella/milestone-6.md` §3.6 |
| Consolidated human-facing UI — Dashboard/Topology/Study Designer/Enroll/Trace/Debug tabs, no agent involved | embarch-ui | `milestone-1.md` CLOSED — its first five tabs live-validated against the real deployed Core; VS Code launcher extension built/packaged/installed for real and user-confirmed working (internal `.vsix` distribution); replaced `embarch-topology`'s `ui` subcommand, `embarch-study-designer`'s `study-designer-ui` binary, and `embarch-core`'s `/enroll` page, all now retired. **Pushed**, and a sixth tab added by Milestone 7 Phase D (2026-08-26): signal-route declaration in Topology, and a post-hoc **Trace** view of an outpost capture — validated against a dev Core built from `main` in an isolated namespace, not against anything deployed | live-local; unit (trace decode, `requires`/tap authoring, provenance) | `embarch-ui/design.md`; `embarch-ui/milestone-1.md`; see `embarch-roadmap.md` Milestone 7 |
| Curated firmware-specific skills/prompt library | embarch-promptu | Proposed | n/a | See `embarch-roadmap.md` Next; first concrete content identified 2026-08-15, `embarch-promptu/design.md` §2 |
| Agent-facing codebase structural analysis (successor to the old GUI's static analysis) | *(unnamed)* | Proposed | n/a | See `embarch-roadmap.md` Later |
| MCU load tracing on the DUT (`CONFIG_TRACING_USER` hooks + `OUTPOST_EVT` markers + GPIO callback dispatch, TX-only UART, build-ID-matched manifest, **two clocks — the DUT stamps each record lock-free from the GRTC SYSCOUNTER's low word, `embarch-core` stamps each frame on arrival**) | embarch-outpost, embarch-study-designer, embarch-core, embarch-api | **Built, integrated into a real DUT firmware, flashed to real silicon, and it has now put bytes through a real UART — 2026-08-27** (Phase C closed 2026-08-26). **Phase E closed 2026-08-27**: 437,789 bytes in 20 s off a real nRF54L15's UART (21.9 KB/s), decoding to 43,948 records with ten named ISR vectors and four named application threads resolved from the ELF plus 648 GPIO dispatch/callback pairs; a study then captured 92,724 bytes on its `outpost` tap and the Trace view rendered it `named`/`timed` — 9205 rows, 766 frames, 24 lanes, zero records lost, 4.0 ms resolution. Three diagnoses preceded that, two well-evidenced and wrong: the deciding fact was the owner naming his board, since every build had targeted `roadrunner@2` against an `dut_dev@7` bench and uart20 therefore sat on P1.00 instead of P1.05 ([embarch-decision-reversals.md](embarch-decision-reversals.md) rows 83–85). Record layout 2 was **withdrawn for layout 3** the same day: the DUT keeps a per-record clock again, read without the two nested `irq_lock`s `sys_clock_cycle_get_32()` takes ([embarch-decision-reversals.md](embarch-decision-reversals.md) row 80), and a rendered row now carries both clocks. The module builds for `native_sim` **and** for the real nRF54L15 reference-dut target; a `native_sim` run decodes cleanly (41 frames, 848 records, every record kind including gaps, zero CRC/framing/sequence failures); the post-link manifest generator resolves **13 real ISRs and 5 real threads out of the reference-dut's own ELF** — including the handler behind Nordic's shared `nrfx_isr` trampoline, which defeats 8 of those 13 if you only read the table's `isr` field. Closing Phase C fired the shared trigger holding the manifest halves, so **Core stores what a flash bound and decodes against it, and `embarch-api` sends it, both shipped the same pass**; a build-ID mismatch costs the *names*, never the capture. **`StreamEncoding::OutpostTrace` lost its `manifest_crc`** (schema **v11 wire / v13 host**) — the firmware cannot produce one. **Phase D closed 2026-08-26**: `embarch-ui` has a post-hoc **Trace** view (lanes per thread/ISR/CPU-idle, markers as ticks, gaps drawn as gaps), reading Core's rendered CSV through `embarch-study-designer`'s own `outpost` module so no trace knowledge lives in JavaScript — and reading Core's new `GET /study/{id}/streams` first, because until that route existed nothing outside Core's filesystem could tell a manifest-refused trace from a named one. A refused trace is never presented as a named one: every lane renders as the pointer or vector number it is. **What is not built or measured**: the DUT stops emitting after its boot burst, so no study-length real capture exists yet and the Trace view has still rendered only a simulator's; every Kconfig wire constant is an unmeasured default; and the instrumentation's own overhead is deliberately uncharacterised | ztest (15, literal wire bytes, incl. GPIO records and the ring's 32-bit reservation wraparound), native_sim end-to-end capture + decode, a cross-decoder check diffing the Python and Rust decoders over one set of bytes (831/831 rows), Rust unit tests against a real firmware capture | `embarch-outpost/design.md` §3, §4, §5; `embarch-core/design.md` §3 decision 30(c); `embarch-api/design.md` §3 decision 39 |
| **Engineer-declared DUT protocols** (`embarch/protocols/*.eap`, `Study.protocols` + `protocols_crc`, `Action::RunProtocol`, `ProtocolOutcome`) | embarch-study-designer, embarch-dev-bench, embarch-core | **Runnable end to end as of 2026-08-26** — types, grammar, validator, host-side reference interpreter, the C interpreter on dev-bench, and Core's third pre-flight seal all shipped the same day. **Not yet run against a real DUT:** what exists is a bench that can execute a manifest, and no engineer has authored one. Closes the write direction `embarch-study-designer/design.md` §3 decision 39 rejected in 2026-08-25 as premature, and gives `milestone-11.md` §3.8 — blocked since 2026-08-24 on "user-supplied DUT protocol knowledge" — the mechanism a user needs to supply any. A small purpose-built text grammar in the *firmware* repo, **resolved into the study at build time** rather than referenced by a manifest CRC (the pattern [embarch-decision-reversals.md](embarch-decision-reversals.md) row 37 had reversed the day before, and that decision 52 had independently ruled out because Core cannot read that repo). The primitive set is **split** (§3 decision 59): `select_if`, fixed-offset integer reads, span lengths, write templates and the expression set cross the wire and dev-bench executes them; `repeat`/`bitpack`/`crc32`/`fixed` are parsed and stay host-side, because no guard reads a bit-packed column and `ProtocolOutcome` reports only a state name. Schema **v14 → v15 wire, v16 → v17 host**; `StudyStart` carries the manifest, which the design pass had left it one field short of ([embarch-decision-reversals.md](embarch-decision-reversals.md) row 68). dev-bench's C keeps a manifest **minus every name but its state names** and packs `Operand`, which is what puts a real `ProtocolDef` at 4,376 bytes against the 17,496-byte `no_std` value the design sized against; `sram0_0_seg` 81.12% → **87.04%**. **What is still not built**: the render half of the four host-side primitives, any Study Designer authoring path, and `crc16`. Four things the design draft asked for were **not** built, each answered by a decision from the preceding eight days: the manifest CRC (52), `decoded_fields` in the result (54), reaching `Study.validations` (48 removed it outright), and a second `GattDecoded` rendering path (52 shipped the first one a day earlier — an `.eap` frame lowers into its `StructLayout` instead) | 12 worked-protocol integration tests over two real-shaped fixtures (a BDS-shaped flow-controlled download driven to pass, stalled to the watchdog, and retried; a GWF1-shaped self-describing batch record), plus 40 unit tests over the validator, evaluator and grammar; 6 Core pre-flight tests (the third seal named alone, both `RunProtocol` indices, `validate_protocol` called rather than reimplemented, and `StepResult.protocol` reaching `events.json`); and 15 dev-bench ztest cases — the pinned `StudyStart`-carrying-the-real-worked-protocol frame asserted field by field, a corrupt-span check proving the third seal fails alone, a pinned populated `StepResult.protocol` frame, eight interpreter-semantics cases mirroring the crate's own, and four capacity cases including a control at exactly the caps | `embarch-study-designer/design.md` §3 decisions 58–62, §4.9; `embarch-dev-bench/design.md` §3 decision 41; `embarch-core/design.md` §3 decision 39 |
| DUT signal links and the "dev-bench bypass" route (`SignalLink`, `Route::Direct`/`Route::ViaDevBench`) | embarch-topology, embarch-core, embarch-ui | Partially shipped: declared, stored, resolved and validated in `embarch-topology` (Milestone 7 Phase A); declarable through Core-only `POST /signals` + `GET /signals`, **no CLI mirror** (Phase B item 0); and **read for real since Phase B item 1, 2026-08-25** — a `Study`'s `StreamSource::Signal` tap with a `Route::Direct` route has Core open that signal's own serial port for the tap's declared scope, taking neither `hw_lock` nor `study_lock`, which gives `resolve_signal_port` its first caller anywhere. **Declarable by a human since Phase D, 2026-08-26** — `embarch-ui`'s Topology tab has signal-route rows over `POST`/`GET /signals` plus two routes Phase D had to add: `GET /serial-ports` (a `direct` route's carrier is picked from Core's own enumeration, and it cannot be VID-gated the way dev-bench's link is, or the gate hides the bridge) and `DELETE /signals/{name}` (the only human surface plus an idempotent-by-name declare means nothing could retract a wire). The diagram redraws from the same data, drawing a `direct` signal *around* the dev-bench node. Still unshipped, and honest about it: **no real port has ever been resolved**, and Phase E's bench attempt on 2026-08-26 did **not** settle why. A USB-UART bridge *is* enumerated on the bench; the conclusion that the DUT's trace pin is not wired to it was **withdrawn 2026-08-27**, because the evidence rested on a BLE connect that turned out to be to a different peripheral, so the board was never shown to be running, `validate_signal` still has no caller *by design* (resolving at the point of use is the validation), and the durable `alerts.jsonl` record for a `SignalMismatch` stays closed as not-needed-yet — **half its named trigger fired** (a signal is declarable now) but the other half has not (there is still no wire), so a declared-but-wrong signal surfaces in that tab's own carrier column or nowhere | unit, router-auth | `embarch-topology/design.md` §3 decision 18 (2026-08-25 amendment), §5; `embarch-core/design.md` §3 decision 30(a); `embarch-ui/design.md` §3 decision 10 |
| One generic inbound stream pipeline (`Study.streams`, `StreamTap`/`StreamSource`/`StreamEncoding`, `streams/`, `GET /study/{id}/stream/{name}`) | embarch-study-designer, embarch-dev-bench, embarch-core, embarch-api | Built end to end (Milestone 7 Phase B closed 2026-08-25), **never run on hardware** — the dev-bench half has not been flashed. Phase A (2026-08-25): the types, the `StreamOpen`/`StreamChunkBatch`/`StreamClose` wire triple, and the retirement of the four per-channel messages, schema **7 → 8** (not the 5 → 6 the decision was written against). Phase B item 0, schema **8 → 9**: the sibling `streams_crc`, `Step.power_sample`/`PowerSampleWindow` retired, `ValidationSource` split per-step/per-tap, and the schema constant split into `DEV_BENCH_WIRE_SCHEMA_VERSION` + `HOST_TYPE_SCHEMA_VERSION` (both 9) — **+654 bytes of dev-bench firmware, not net-negative**, because the C decoder never walked the `streams` span the new seal covers. **Phase B item 1, 2026-08-25 — Core's whole half, with no schema change at all**: `streams/` replaces the three fixed CSV paths (one file per tap, named by the tap, **raw bytes always written before any decode is attempted**), both retention knobs, `GET /study/{id}/stream/{name}` with `/power-data`/`/waveform-data`/`/gatt-data` kept as aliases for one release, and `StudyResult.streams`/`provenance` reaching `events.json` for the first time. **Phase B item 2, 2026-08-25 — `embarch-api`'s half**: `study_stream_data { study_id, name, raw? }` and `list_study_streams { study_id }` as MCP tools and CLI subcommands, with the three old tools/subcommands kept unchanged as aliases for one release (pinned by a Core test asserting each alias returns **byte-for-byte** what its replacement does). `list_study_streams` needed **no new Core route** — `GET /study/{id}` already returns the whole `StudyResult` — and exists chiefly to surface `truncated`, which the three aliases structurally cannot report. **Phase B item 3, 2026-08-25 — dev-bench's half, closing the phase.** Taps decoded and stored, opened and closed from their declared `StreamScope`, the GATT transcript riding `StreamChunkBatch` on its declared tap, and **the retired tag 10 gone from the encoder and from `main.c`** (its value left burned, not reused — the "never flashed" argument that licensed reusing tags 2/3/4 expires the moment this firmware is flashed). Two scoping corrections, both recorded rather than absorbed: **the payload-interpretation deletion this item was named for had already happened in Phase A**, so the item was purely additive and the phase ends net *bigger* (+128 bytes here, on top of item 0's +654) against a net-negative expectation; and **the reserved `dev-bench` log tap needed Core, not dev-bench** — `tap_for` resolved ids against the submitted `Study`, so a tap the bench invented would have been dropped as undeclared. It ships as Core synthesizing the tap and rendering the `LogLine` frames it already receives, which closes the asymmetry with no firmware at all. Two of Phase B's four items turned out not to be the one repo their scope named. **The manifest half shipped 2026-08-26** when Phase C fired its shared trigger — Core stores what a flash bound and decodes against it, `embarch-api` sends it, and `StreamEncoding::OutpostTrace` lost the `manifest_crc` the firmware could never have produced (schema **v11 wire / v13 host**; [embarch-decision-reversals.md](embarch-decision-reversals.md) row 37) | unit, router-auth, cross-language wire-byte contract (Rust + ztest) | `embarch-study-designer/design.md` §3 decisions 39, 19, 12 (all with 2026-08-25 amendments), §4.8, §5.2; `embarch-core/design.md` §3 decisions 30, 31 |
| Study version requirements (`Study.requires`), operator-selected reflash, and `StudyResult.provenance` | embarch-study-designer, embarch-core, embarch-api, embarch-ui | Shipped end to end, **never run against real hardware**. Phase A (2026-08-25): the types; `POST /study` rejects a blank/omitted `requires`. Phase B item 1: Core's `409` version gate against what the bench reports over `HelloAck`, **before `StudyStart` is sent so no step runs**, and `provenance` reaching `events.json`. Phase B item 2 (host schema **9 → 10**): `run_study --reflash none\|dev-bench\|dut\|both` and `--allow-version-mismatch`, sequenced check → build → flash → submit in `embarch-api` because Core has no build system; the override is **recorded** in `provenance.overrides`, never silently honoured; and `VersionSource::FlashedThisRun` became producible — by the process that did the flashing, since `POST /flash` and `POST /study` are separate calls Core deliberately does not link. **That last part also made `requires.firmware_version` checkable for the first time**, which no decision had anticipated. The DUT's version describes *the tree that was built* (a project-declared `version_command`, defaulting to `git describe`), not a readback — nothing in this suite can read a version off a DUT. `embarch-api` never runs `git checkout`, enforced against the config file's own `version_command` too | unit (including a Core test that no step runs on a rejection, and one that an override reaches the recorded result) | `embarch-study-designer/design.md` §3 decision 40, §4.1, §4.5; `embarch-core/design.md` §3 decision 31; `embarch-api/design.md` §3 decision 40 |

| `embarch-core chip-list [filter]` — enumerate probe-rs's target database, no hardware access | embarch-core | Scoped 2026-08-15, scheduled and decided 2026-08-25 (`embarch-core/design.md` §3 decision 34); not yet built. Kills the `cargo install probe-rs-tools` step onboarding hits on an unmapped SoC | n/a | `embarch-core/design.md` §3 decision 34; `embarch-umbrella/design.md` §10 |
| `base_address` as a per-project config field — a config-driven flash path for `flash_format = "bin"` projects | embarch-api | Shipped 2026-08-25. Closes the gap that forced the ESP32-C5 validation to bypass `embarch-api` with a hand-written `POST /flash` | unit (config parse, hex round trip, both `resolve` arms) | `embarch-api/design.md` §3 decision 42, §4; `embarch-core/design.md` §10 |
| Dev-bench link same-chip verification (`HelloAck.hardware_id`) | embarch-study-designer, embarch-dev-bench, embarch-core, embarch-topology | Built 2026-08-25 (wire schema 9 → 10, host 11 → 12), **not yet run on hardware** — no dev-bench board has been attached since. Closes `embarch-topology/design.md` §5's UART-vs-JTAG gap; that crate turned out to need a change after all (relating two encodings of one chip ID is chip knowledge) | unit both languages incl. a cross-language pinned `HelloAck` frame; ztest 33/33 on native_sim; real esp32c5 app links with `CONFIG_HWINFO_ESP32=y` | `embarch-core/design.md` §3 decision 35; `embarch-study-designer/design.md` §3 decision 47; `embarch-dev-bench/design.md` §3 decision 36 |
| `embarch-api` rolling **per-user** logfile, surfaced in `embarch-ui`'s Debug tab | embarch-api, embarch-ui | Shipped 2026-08-25. Core's endpoint pattern does not transfer to a per-session/one-shot process. Per-user, not per-machine: `embarch-api` runs as the engineer and `/var/lib` is root-owned | unit (path/read), live (real CLI runs read back through the UI's backlog route and SSE stream) | `embarch-api/design.md` §3 decision 43; `embarch-ui/design.md` §3 decision 13 |

## Changelog

- 2026-08-26 — **Two rows added and the naming row amended, after the GATT extraction turned out to have been reading two files and missing a third.** The extraction row is the notable one: it records a capability that had been quietly incomplete rather than one that is new, and the measurement that decided its exclusion rule, because "scan the repo" and "scan the repo the way the repo defines itself" produce three services and six respectively on the same real checkout. The picker row and the naming amendment are the human half of the same pass.

- 2026-08-26 — BLE bridge row extended with the selective monitor actions, and a new row for per-characteristic GATT capture (`StreamSource::GattNotify` taps decoded by a declared struct layout, plus the auto-declared uncapped transcript). Both marked shipped-but-not-yet-run-on-hardware, which is exactly what they are: every layer is implemented, tested and deployed, and no real notification has gone through the path yet.

- 2026-08-27 — **A new row for per-family flashing-backend selection, and the erase row's hazard closed.** Both from finding that Core had exactly one flashing implementation and used it in preference to the runner every Zephyr board declares for itself. On an RRAM-based nRF54L part that means writing through a path that does not model the storage — so probe-rs is now refused for that family rather than merely steered away from chip-erase. The erase row also stops claiming a redesign was unimplemented: it is implemented, and the two days it spent recorded as shipped-but-absent are rows 54 and 55.

- 2026-08-27 — **The MCU-load-tracing row's load-bearing clause finally moves**: "never having put a byte through a real UART" is no longer true, and the row says what replaced it — bytes arrived, the stream stopped, and the open problem is DUT-side emission rather than wiring. The same row's parenthetical changes from "no DUT clock" to two clocks, because layout 2 was withdrawn for layout 3 and the reasoning is worth carrying in the one-line description a reader of this table gets. Its test column gains the cross-decoder check, which diffs this suite's Python and Rust decoders over one set of bytes and caught a real drift the moment it was restored.

- 2026-08-26 — **The MCU-load-tracing and DUT-signal-link rows both updated from a bench attempt rather than a build** (Milestone 7 Phase E). Tracing moves from "never having put a byte through a real UART" to the same thing said more precisely: the module is now integrated into the real reference-dut firmware, pinned in that repo's `west.yml`, and **flashed onto real silicon where it runs** — the instrumented DUT still advertises, connects and serves GATT — and the byte still has not crossed, because the DUT's trace pin is not wired to anything the host can read. The signal-link row's caveat changes shape for the same reason: it used to say no bridge exists, and the accurate version is that one is plugged in and is not connected to the pin. Also added: the Trace view's **load repartition**, which is what "understand where the CPU's time went" actually resolves to, built against the simulator capture for want of a real one.

- 2026-08-26 — **The MCU-load-tracing row gained the sentence that changes what the feature is:** the DUT carries no clock, and `embarch-core` stamps each frame as it arrives ([embarch-outpost/design.md](embarch-outpost/design.md) §3 decisions 4, 17). The status word does not move — it was already "built, never having put a byte through a real UART" and still is — but what a built trace *can say* changed: a frame is the resolution, so this feature counts ISR entries and no longer times them, while gaining an axis shared with every other stream in a study. Recorded here rather than only in the design doc because the row's parenthetical is the whole description a reader of this table gets.

- 2026-08-26 — **Two Milestone 7 rows updated as Phase D closed, and neither status word moved** ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §5). The MCU-load-tracing row gains the Trace view and loses "the UI trace view (Phase D) ... has not started" from its own not-built list; its load-bearing clause is untouched, because it is still true — no outpost byte has crossed a real UART, so that view has rendered a simulator's capture and never a DUT's. The signal-links row gains "declarable by a human", the two routes Phase D had to add for it, and the fact that **half** the signal-mismatch alert trigger has now fired: a signal is declarable, a physically real `Route::Direct` still is not.

- 2026-08-26 — **The MCU-load-tracing row moves off `Proposed, design-only`** as Milestone 7 Phase C closed ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §4), to *built, `native_sim`-verified end to end, never having put a byte through a real UART* — phrased to match what Phase B's row already does, since the honest limit is the same shape. The stream-pipeline row's trailing "settled-and-deliberately-not-built" clause is updated in the same pass: closing Phase C fired that trigger, and leaving it would have been exactly the drift DOC-PROTOCOL.md §4's pre-close grep exists to catch.

- 2026-08-25 — No row changed status: `embarch-study-designer` §3 decisions 46 and 49 are internal representation fixes to shipped types (`Study`, `StudyResult`, `StepResult` shrink by 71×, 143× and 29× respectively, with no wire change and no schema bump), not features. Recorded here only so the absence of a row is a decision rather than an oversight.

- 2026-08-26 — **One row added for dev-bench's `CONFIG_LOG` reaching Core's new debug file**, marked shipped and live-validated rather than built: it ran on the real bench against the real deployed service, boot record and completed study included. Worth noting *why* it is one row across two sub-projects and not two: the firmware half is useless without Core's handshake tolerance, and Core's half without the firmware only ever stores the boundary markers it writes itself. Also worth noting what the row does not claim — the fatal-error dump this makes possible has never been provoked, so "a crash now reports itself" is designed and untested.

- 2026-08-25 — **`HelloAck.hardware_id` moves to Built, deliberately not to Shipped.** Every layer exists and is tested in both languages, but the gate has never met real silicon — no dev-bench board has been attached — so the row says "built, not yet run on hardware" rather than claiming a verification that has not happened. The row's Sub-projects column gained `embarch-dev-bench` and kept `embarch-topology`: the decision text predicted that crate needed no change, and it did.

- 2026-08-25 — **The `embarch-api` logfile row moves to Shipped, with its name corrected from "per-machine" to "per-user."** The location is not a detail the table can round off: the decision said machine-wide, and machine-wide is a directory `embarch-api` cannot write to, because it runs as the engineer and Core does not. Building it also fixed two pre-existing defects in `embarch-ui`'s Debug tab that are not separately listed features — ANSI escapes from Core's logfile rendered as garbage, and the log SSE stream replaying its last batch to every new subscriber.

- 2026-08-25 — **`base_address` moved from decided-not-built to Shipped**, the first of that day's four new rows to land. Note what the row's Tests column now says and what it doesn't: unit coverage only. The physical case that motivated the decision — a `flash_format = "bin"` ESP32-C5 flashed *through* `embarch-api` rather than around it — has not been re-run on hardware, so this row claims a working config path, not a re-validated board.

- 2026-08-25 — **Suite-wide design pass added four decided-but-unbuilt rows** (`chip-list`, `base_address`, `HelloAck.hardware_id`, the `embarch-api` logfile), each marked decided-not-built rather than shipped so this table cannot drift the other way. Nothing already in the table changed status; the pass's other outcomes were fixes to existing behaviour (`embarch-core` §3 decision 33's watchdog defect, `embarch-dev-bench` §3 decision 35's step decoding, `embarch-study-designer` §3 decision 46's `Study.steps`) and one retirement (`embarch-topology` §3 decision 19), none of which are separately listed features.

- 2026-08-25 — **Milestone 7 Phase B item 3 shipped, closing the phase; the stream-pipeline row moves off `Partially shipped`** — to *built end to end, never run on hardware*, not to shipped, because the dev-bench half has still never been flashed and saying otherwise would claim a verification nobody has done. The row now carries both of the item's scoping corrections rather than only its outcome: the payload-interpretation deletion it was named for had already landed in Phase A (so the phase ends net **bigger**, against its own net-negative expectation), and the reserved `dev-bench` log tap needed Core rather than dev-bench. **Two of Phase B's four items turned out not to be the single repo their one-word scope named** — item 2 moved three repos, item 3 moved two — which is worth stating as a pattern rather than twice as an accident.

- 2026-08-25 — **Milestone 7 Phase B item 2 shipped; two rows updated, and one of them was stale from the *previous* item.** The stream-pipeline row now records `embarch-api`'s half and narrows "still Phase B" to item 3 alone. The version-requirements row moves from `Partially shipped` to shipped-but-never-run-on-hardware — and it still read "**nothing compares a requirement against the bench yet... and nothing fills `provenance`**", which item 1 made false the day before and nobody's pre-close grep caught. Exactly the drift `DOC-PROTOCOL.md` §4 exists for, found this time by rewriting the row rather than by the check that should have caught it. Both rows now also carry the two things item 2 established that no decision had anticipated: supplying a flashed version is what makes `requires.firmware_version` checkable at all, and `embarch-api`'s manifest half is deferred under **the same** trigger as Core's rather than a trigger of its own.

- 2026-08-25 — **Milestone 7 Phase B item 1 shipped; the same two rows updated again, and again neither status word moved.** The stream-pipeline row now records Core's whole half as real — `streams/` replacing the three fixed CSV paths, raw-before-decode, both retention knobs, the parameterised route with the three old ones as aliases, and `provenance`/`streams` in `events.json` — and narrows "still Phase B" to items 2 and 3 alone. The signal-links row moves from "nothing calls it" to "`resolve_signal_port` has its first caller," while keeping the two things that are still true and easy to overstate: no real port has ever been resolved, and `validate_signal` still has no caller **by design**. The GATT-transcript row's path updated from `gatt.csv` to `streams/<tap>.csv` — the row shape is byte-for-byte unchanged, only the path moved, which is exactly the distinction that row would otherwise blur.

- 2026-08-25 — **Milestone 7 Phase B item 0 shipped; two rows updated, neither status word moved.** The stream-pipeline row now names schema v9 and the four type changes that landed, plus the honest size outcome (+654 bytes of firmware, against a net-negative expectation). The signal-links row's declaration surface moved from *settled* to *shipped* — a signal is declarable now — while its validation half is still called by nothing. Both rows stay `Partially shipped`, because items 1–3 (`streams/`, retention, the parameterised stream route, dev-bench's payload-interpretation deletion) are untouched.

- 2026-08-26 — **One row added: characteristic names.** Raised by the repo owner as "the option show up as numbers" one decision after the selective-monitor pickers shipped. Listed as its own row rather than folded into the per-characteristic-capture row above it because it is a different kind of claim: that row is about what a study *captures*, this one is about whether the person authoring it can tell two characteristics apart. Worth noting in a table of capabilities that this one cost nothing to acquire — the names were already being parsed and thrown away.

- 2026-08-26 — **The BLE-security row moves from "Design-only, no code" to shipped-and-hardware-validated, and three rows are added beside it.** The security row had said L4 was unreachable from the bench; it now is reachable, and the claim it carries is deliberately narrower than "L4" alone would suggest — authenticated in the stack's bookkeeping, no real MITM protection, because nothing compares the passkey. New rows: the unbond action and study-scoped bonding, the Study Designer's runtime project picker, and `embarch deploy-core`. That last one is a features row for something that is not a product capability at all, and it is listed because a suite whose own workflow doc calls a step "the single most-repeated undocumented step" and then watches it be re-typed for two weeks should be able to see, from this table, that it stopped.

- 2026-08-25 — Three rows added from Milestone 6's second blocker: BLE security elevation and the declared-GATT table (both design-only), and the `--erase` capability, recorded as **shipped and hazardous** rather than simply shipped — the deployed Core still carries the path that bricked the DUT, and the delegation that replaces it is neither implemented nor validated. A row that says "Shipped" about a capability that destroys the board is exactly the kind of drift `DOC-PROTOCOL.md` §4's pre-close check exists for.
- 2026-08-25 — Row added for study version requirements + operator-selected reflash + result provenance (`Proposed, design-only`). Worth noting what it makes visible rather than adds: a `StudyResult` could not previously say which firmware it ran against, so two runs of one study against two builds were indistinguishable afterwards.

- 2026-08-25 — **Two Milestone 7 rows re-stated after settling Phase A's five leftover questions** (decisions only; implementation is Phase B). The signal-links row now says the validation half has no caller anywhere, and that its declaration surface is Core-only with no CLI mirror while the alert record is closed as not-needed-yet rather than owed. The stream-pipeline row now names the four decisions Phase B carries with it. Neither row's status word moved — both are still `Partially shipped` — which is the point: a settled decision is not a shipped feature.

- 2026-08-25 — **Three of the Milestone 7 rows moved off `Proposed, design-only`** as Phase A landed ([embarch-outpost/milestone-1.md](embarch-outpost/milestone-1.md) §2): DUT signal links, the generic inbound stream pipeline, and study version requirements are all `Partially shipped`, each row now naming exactly what is *not* built rather than reading as done. Found by DOC-PROTOCOL.md §4's pre-close staleness grep: the pipeline row also still said `schema 5 → 6`, which stopped being true when decisions 42/43 took v6 and v7 ahead of it — the real bump was 7 → 8. The MCU-load-tracing row is unchanged and still correct: Phase C hasn't started.
- 2026-08-25 — **Three new rows, all `Proposed, design-only`**, from the [embarch-outpost](embarch-outpost/design.md) design pass: MCU load tracing on the DUT, DUT signal links plus the "dev-bench bypass" route, and one generic inbound stream pipeline. The third one is worth reading against the rows above it: the power, sensor-waveform, and GATT-transcript capture rows describe what is shipped **today** and stay accurate — the pipeline row is what replaces all three once built, and deliberately does not claim their ground before it exists.

- 2026-08-25 — **Five rows added and one corrected, closing `embarch-roadmap.md` Milestone 6 (Stimulate-and-Capture).** The correction is the notable one: the Study Designer tab UI row records that 2026-08-24's "implemented, live-validated" claim was true of its backend routes only — `assets/index.html` still had a placeholder and `assets/app.js` had no study-designer code at all. Found by trying to use the tab, not by the staleness script, which only cross-checks status words between tables and can't know whether a shipped-looking feature has a frontend.

*Older entries archived to [embarch-features.changelog-archive.md](embarch-features.changelog-archive.md).*

- 2026-08-24 — **`embarch-ui` row updated: `milestone-1.md` CLOSED — the user confirmed the installed VS Code extension works for real.** Start/stop/open-in-browser all function correctly in their actual Windows VS Code (Remote-WSL).
- 2026-08-24 — **`embarch-ui` row updated: VS Code launcher extension built/packaged/installed for real, not just scaffolded.** A portable Node install unblocked `npm install`/`compile`/`vsce package`; distribution decided as internal `.vsix` only; installed into a real VS Code via `code --install-extension`.
- 2026-08-24 — **`embarch-ui` row corrected again — found stale by this same pre-close grep a second time in one day (`DOC-PROTOCOL.md` §4): it still read "no tabs live yet," which all five tabs being real, live-validated code made false.** Status/verification columns updated to match `embarch-ui/milestone-1.md` §4.1–4.9: every tab live against the real deployed Core, VS Code launcher scaffolded (not build-verified), and the three surfaces this feature replaces (`embarch-topology`'s `ui` subcommand, `embarch-study-designer`'s `study-designer-ui`, `embarch-core`'s `/enroll` page) actually deleted, not just planned.
- 2026-08-24 — **Corrected the stale "EmbArch UI" row (found via this file's own pre-close staleness grep, `DOC-PROTOCOL.md` §4): it named an unnamed future project and `Proposed`, but `embarch-ui` is now a real sub-project with a design doc, a created repo, and a drafted execution plan.** Owner/status/doc columns updated to match; row description broadened from "no agent involved" to the actual five-tab scope.

- 2026-08-24 — BLE bridge row corrected (found via `DOC-PROTOCOL.md` §4's pre-close staleness grep, closing `embarch-roadmap.md` Milestone 3): `BleConnect`/`GattDiscover`/`GattMonitorAll` are no longer "compile-only, never run on a board" — all three ran for real against a real, independent reference-dut DUT. Narrowed the still-open gap to `DataExchange`'s targeted read/write/notify/indicate/stream-capture specifically, which is what's actually still unexercised against a real characteristic.

- 2026-08-20 — **Milestone 2 (Dev Bench Self-Test) closed: seven rows updated or added, most correcting real drift found closing it out, not just newly-shipped features.** `GET /dev-bench/port`, the shared `app/` firmware, and the Core↔dev-bench serial bridge rows all moved from "no real dev-bench"/`native_sim`-only/"Proposed, design-only" to real-hardware-confirmed — the bridge row specifically had sat at `Proposed, design-only` despite `/study*` shipping back on 2026-08-18, a real disagreement with `embarch-core/design.md`/`embarch-api/design.md` that `DOC-PROTOCOL.md` §5 exists to prevent. New rows for `run_study`/`study_status`/`study_power_data`/`study_waveform_data` (also previously stuck at "Proposed, design-only" under a since-inaccurate "Study-running MCP tool" name) and `doctor` check 13 (stale dev-bench firmware) — the latter genuinely new: check 13 had no code behind it in `embarch-umbrella` until this pass despite existing in that repo's own design doc since 2026-08-15. The `embarch-study-designer` row's "not yet a Cargo dependency" caveat, stale since 2026-08-18, is also corrected — same drift already flagged and fixed in `embarch-roadmap.md`'s **Next** bucket this same pass.

- 2026-08-19 — BLE bridge row's status corrected: `BleAdvertise` has now run for real on physical hardware (the ESP32-C5, Milestone 2's target board as of that day's board retarget from the nRF54L15DK — `embarch-decision-reversals.md` row 13) — no longer "never run on a board." `BleConnect`/`DataExchange` remain compile-only.
- 2026-08-15 — Added a **Verification** column (unit / live-local / real-hardware / n/a), closing item 62 of that day's design-improvement review: this was the most repeated caveat in the whole table, previously hand-rolled in prose on nearly every row ("never against a real Core," "not yet hardware-revalidated," "compiles... never run on a board"). Also noted, in three Notes cells, where a design has been resolved since the same review (artifact-transfer over a real network, multi-probe selection, ESP-IDF scope) without yet being implemented in code — Status stays `Todo` until it is.
- 2026-08-14 — Added four new Shipped rows for Zephyr/west live target discovery, implemented across all three repos same day: `embarch-core`'s `POST /resolve-chip`, `embarch-api`'s `list_targets` tool/subcommand and `discovery = "zephyr-west"` itself, and `embarch-umbrella`'s matching `init`/`doctor` support. Also updated the `embarch init` row's caveat — it's now been run for real against the reference-dut repo, not just a repo shaped like it.
- 2026-08-11 — Corrected status drift: the `embarch doctor` and suite-release-archive rows were still marked `Proposed, design-only` even though `embarch-umbrella/milestone-6.md` §3.4 (doctor, done 2026-08-10) and §3.7 (release CI, done 2026-08-11, all three repos released clean) had both closed — violated `DOC-PROTOCOL.md` §5's "sub-project doc and suite-level docs should never disagree about status" rule. Flipped both to Shipped with links to the specific milestone section rather than restating its detail here.
- 2026-08-05 — `embarch init` flipped to Shipped. `doctor` is now the only unimplemented command.
- 2026-08-05 — `embarch setup` and `embarch up`/`down` flipped to Shipped, with status wording kept specific about what has *not* been exercised: no real Core has been started by either, and the Windows-side install/start commands are printed but have never been run.
- 2026-08-05 — `embarch-core`'s `start`/`stop` flipped to Shipped, and the service-install row now records that elevation is required on every OS (a systemd system unit wants root/polkit, not just Windows wanting Administrator).
- 2026-08-05 — `base_url = "auto"` flipped to Shipped (`embarch-umbrella/milestone-6.md` §3.5). Corrected the CLI-subcommand row, which had listed snake_case names the CLI has never actually had — `clap`'s derive renames to kebab-case, so it's `list-projects`, not `list_projects` (`embarch-api/design.md` §5a).
- 2026-08-05 — Split the `doctor`/`status` row now that they've diverged in status: topology auto-detection is Shipped (verified on WSL2 and against a mock, never against a real Core), `status` is Partial (reachability/address/class, no probe list or token check yet), `doctor` stays design-only.
- 2026-08-05 — Added eight design-only rows for Milestone 6 (Onboarding): five for the new `embarch-umbrella` sub-project (`setup`, `init`, `doctor`/`status`, `up`/`down`, the suite release archive), one each for `embarch-api`'s `base_url = "auto"` and `embarch-core`'s `start`/`stop`, and one Proposed row for the future EmbArch UI. Note what did *not* move: `embarch-api`'s PATH/toolchain-preflight row stays `Todo` but its Notes now point at umbrella's `doctor` as the intended home rather than at a build-path check.
- 2026-08-04 — Added three rows for work that shipped on the Core↔dev-bench hop: `embarch-core`'s dev-bench port auto-detection (`GET /dev-bench/port` + `detect-dev-bench`), and two `embarch-dev-bench` firmware rows (the shared serial/protocol application, and the BLE bridge now covering the full `Action`/`GattOperation` surface). Status wording is deliberately specific about *how far* each is verified — `native_sim` only, or uncompiled — since neither has met real hardware — the BLE-bridge row was updated the same day once `workspaces/nordic` was fetched and built, moving it from "uncompiled" to "compiles for nRF54L15DK, never run on a board." See each design doc's own open questions. Also corrected pre-existing status drift on the `embarch-study-designer` row, still marked `Proposed, design-only` even though `embarch.md` §3 and that crate's own changelog have recorded it as implemented and tested since 2026-07-29 (`DOC-PROTOCOL.md` §5's never-disagree rule).

