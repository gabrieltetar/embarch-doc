| `doctor` check 1 — binaries found **by a reading, not a guess**: Core via the Windows service's `BINARY_PATH_NAME`, `embarch-api` via the agent CLI's registration or `setup`'s install directory as well as `PATH`; a missing local `embarch-core` warns where none belongs | Shipped; the `sc.exe qc` read located the live service's binary 2026-09-05, the API half only unit-tested | hw | 38, 42 |
