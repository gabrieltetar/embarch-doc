| Study Designer authors every field a `Study` carries — `.eap` protocols and `RunProtocol` steps, per-tap record framing, the dev-bench log level, payload layouts and registered actions (edit/delete, refused while a saved study uses one), and running a saved study as it is on disk | Shipped — **legs E and J unrun: the caps note against a live bench, and a `Debug` run's clamp on the bench's own log stream** | unit, local | 29, 30 |
