| `doctor` check 17 — Core's bind address against what this topology needs: the class `setup` recorded versus the address `/status` answered at, and versus the service's registered `--bind` when nothing answered at all | Shipped — turns "Core unreachable" into "Core bound where you cannot reach it". **Never met a real narrow-bound Core**: both Fail branches are unit-tested against synthetic evidence, and this bench registers `--bind 0.0.0.0` | unit | 22 |
