| `embarch doctor` — the whole chain, `--json` | Shipped, seventeen checks. 22(b) firewall and 22(c) disk are **retired unbuilt** (2026-09-05); the log-tail row is designed and unbuilt; `--prune` is deferred inside a shipping command (decision 26) | local | §5, 18, 22, 26 |
