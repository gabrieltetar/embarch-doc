| `doctor` check 11 — schema skew: Core's served host version against the **located `embarch-api`**'s, via `embarch-api --json versions`; unaskable is a warn naming why, never a fall back to `embarch`'s own constant, which survives as a mixed-install warn | Shipped; bench half **read live: `compatible: true`, v15 accepted** [2026-09-07], handshake 0.73 s (decision 44) | bench | 33, 35, 36, 44 |
